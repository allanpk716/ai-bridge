---
phase: 04-real-time-chat
plan: 05
type: execute
wave: 2
depends_on: [04-03]
files_modified:
  - web/package.json
  - web/src/components/chat/CodeBlock.tsx
  - web/src/components/chat/StreamingMessage.tsx
  - web/src/components/chat/index.ts
autonomous: true

must_haves:
  truths:
    - "Code blocks have syntax highlighting for common languages"
    - "Code blocks show language label"
    - "Code blocks have copy button"
    - "Dark theme uses dark syntax highlighting theme"
  artifacts:
    - path: "web/src/components/chat/CodeBlock.tsx"
      provides: "Syntax-highlighted code block component"
      min_lines: 60
      contains: "SyntaxHighlighter|Prism|hljs"
    - path: "web/package.json"
      provides: "react-syntax-highlighter dependency"
      contains: "react-syntax-highlighter"
  key_links:
    - from: "web/src/components/chat/CodeBlock.tsx"
      to: "react-syntax-highlighter"
      via: "import { Prism as SyntaxHighlighter }"
      pattern: "SyntaxHighlighter"
    - from: "web/src/components/chat/StreamingMessage.tsx"
      to: "web/src/components/chat/CodeBlock.tsx"
      via: "custom code component or direct render"
      pattern: "CodeBlock"
---

<objective>
Create a CodeBlock component with syntax highlighting using react-syntax-highlighter. The component will render code blocks with language detection, syntax highlighting, copy button, and theme support for light/dark modes.

Purpose: Code blocks are a core part of Claude Code responses. Syntax highlighting improves readability, and the copy button enables quick code extraction. This integrates with the streaming markdown renderer.

Output: CodeBlock component with syntax highlighting, copy button, and language label, integrated with StreamingMessage.
</objective>

<execution_context>
@C:\Users\allan716\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\allan716\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-real-time-chat/04-RESEARCH.md
@.planning/phases/04-real-time-chat/04-CONTEXT.md

# Existing components
@web/src/components/chat/StreamingMessage.tsx
@web/src/components/ui/button.tsx
@web/src/components/ui/tooltip.tsx
</context>

<tasks>

<task type="auto">
  <name>Install react-syntax-highlighter</name>
  <files>web/package.json</files>
  <action>
    Run: `npm install react-syntax-highlighter @types/react-syntax-highlighter` in the web directory

    react-syntax-highlighter: Main library for syntax highlighting
    @types/react-syntax-highlighter: TypeScript type definitions

    Note: If @types package fails (sometimes not needed), retry without it.
  </action>
  <verify>package.json contains react-syntax-highlighter</verify>
  <done>Syntax highlighting dependency installed</done>
</task>

<task type="auto">
  <name>Create CodeBlock component</name>
  <files>web/src/components/chat/CodeBlock.tsx</files>
  <action>
    Create web/src/components/chat/CodeBlock.tsx with:

    1. Component interface:
       - code: string (the code content)
       - language?: string (detected from ```lang or default to 'text')
       - className?: string

    2. Syntax highlighting:
       - Import: `{ Prism as SyntaxHighlighter } from 'react-syntax-highlighter'`
       - Import themes: `{ vscDark, vs } from 'react-syntax-highlighter/dist/esm/styles/prism'`
       - Use theme based on current theme (useTheme from 'next-themes')
       - Handle unknown languages gracefully (fallback to 'text')

    3. Styling per CONTEXT.md:
       - Card-style code block: dark background container
       - Top bar with: language label (left), copy button (right)
       - Copy button: use Button from @/components/ui with Copy/Check icons
       - Rounded corners, border, shadow

    4. Copy functionality:
       - useState for copied state (resets after 2 seconds)
       - navigator.clipboard.writeText() for copying
       - Show Check icon when copied, Copy icon otherwise

    5. Language detection:
       - Default to 'text' if no language specified
       - Handle common aliases: js -> javascript, ts -> typescript, etc.
       - Display language in top-left badge

    6. Layout structure:
       ```
       ┌─────────────────────────────────┐
       │ javascript          [Copy]     │
       ├─────────────────────────────────┤
       │ const hello = "world";          │
       │ console.log(hello);             │
       └─────────────────────────────────┘
       ```

    Follow RESEARCH.md Pattern 4 for code block styling.
  </action>
  <verify>
    1. Component imports SyntaxHighlighter
    2. Copy button works with visual feedback
    3. Language label displays in top bar
    4. Dark theme uses vscDark, light uses vs
  </verify>
  <done>
    CodeBlock renders syntax-highlighted code with:
    - Language detection and label
    - Copy button with visual feedback
    - Theme-aware styling
  </done>
</task>

<task type="auto">
  <name>Integrate CodeBlock with StreamingMessage</name>
  <files>web/src/components/chat/StreamingMessage.tsx</files>
  <action>
    Update web/src/components/chat/StreamingMessage.tsx:

    1. For react-markdown fallback (or main renderer):
       - Add custom code component to components prop
       - Parse className for language (language-xxx pattern)
       - Render CodeBlock with extracted code and language

    2. For Streamdown:
       - If Streamdown supports custom components, use CodeBlock
       - Otherwise, post-process or use react-markdown for code blocks

    3. Integration pattern:
       ```tsx
       <Markdown components={{
         code({node, inline, className, children, ...props}) {
           const match = /language-(\w+)/.exec(className || '')
           return !inline && match ? (
             <CodeBlock code={String(children).replace(/\n$/, '')} language={match[1]} />
           ) : (
             <code className={className} {...props}>{children}</code>
           )
         }
       }}>
         {content}
       </Markdown>
       ```

    4. Inline code:
       - Use regular `<code>` tag for inline code
       - Apply prose styling (background, padding, monospace)

    This ensures all code blocks (both streaming and complete) use CodeBlock.
  </action>
  <verify>
    1. StreamingMessage renders CodeBlock for fenced code blocks
    2. Inline code uses regular code tag
    3. Language is extracted from ```lang syntax
  </verify>
  <done>StreamingMessage displays syntax-highlighted code blocks</done>
</task>

<task type="auto">
  <name>Export CodeBlock from barrel</name>
  <files>web/src/components/chat/index.ts</files>
  <action>
    Update web/src/components/chat/index.ts to export:
    - CodeBlock as named export
    - Keep existing exports
  </action>
  <verify>CodeBlock is available via '@/components/chat'</verify>
  <done>All chat components available from barrel export</done>
</task>

</tasks>

<verification>
1. react-syntax-highlighter installed
2. CodeBlock component renders with syntax highlighting
3. Copy button copies code to clipboard
4. Language label displays correctly
5. Dark/light theme switches syntax theme
6. StreamingMessage uses CodeBlock for code blocks
</verification>

<success_criteria>
CodeBlock can be imported and renders:
- Syntax-highlighted code (test with javascript, python, typescript)
- Language label in top bar
- Working copy button with Check icon feedback
- Correct theme for dark/light mode
</success_criteria>

<output>
After completion, create `.planning/phases/04-real-time-chat/04-05-SUMMARY.md`
</output>
