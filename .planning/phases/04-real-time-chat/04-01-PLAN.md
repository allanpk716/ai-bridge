---
phase: 04-real-time-chat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/package.json
  - web/src/components/chat/ChatMessageList.tsx
  - web/src/components/chat/index.ts
autonomous: true

must_haves:
  truths:
    - "Message list renders messages with variable height content"
    - "Virtualization handles 100+ messages without performance issues"
    - "Scroll position is maintained when new messages arrive"
    - "Messages are selectable with user/assistant role styling"
  artifacts:
    - path: "web/src/components/chat/ChatMessageList.tsx"
      provides: "Virtualized message list component"
      min_lines: 80
      contains: "Virtuoso"
    - path: "web/package.json"
      provides: "react-virtuoso dependency"
      contains: "react-virtuoso"
  key_links:
    - from: "web/src/components/chat/ChatMessageList.tsx"
      to: "react-virtuoso"
      via: "import { Virtuoso } from 'react-virtuoso'"
      pattern: "import.*Virtuoso.*from.*react-virtuoso"
---

<objective>
Create a virtualized message list component using react-virtuoso that handles 10,000+ messages with smooth scrolling and variable height content. This is the foundation of the chat interface, enabling efficient rendering of large conversation histories.

Purpose: Standard virtualization is required for performance with large message counts. react-virtuoso is the research-validated choice for chat interfaces with auto-scroll behavior.

Output: ChatMessageList component that renders messages efficiently with followOutput behavior for auto-scrolling to new messages.
</objective>

<execution_context>
@C:\Users\allan716\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\allan716\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-real-time-chat/04-RESEARCH.md
@.planning/phases/03-session-management/03-06a-SUMMARY.md

# Message type from existing API
@web/src/types/api.ts
@web/src/lib/api/messages.ts
</context>

<tasks>

<task type="auto">
  <name>Install react-virtuoso dependency</name>
  <files>web/package.json</files>
  <action>
    Run: `npm install react-virtuoso` in the web directory

    This installs the virtualization library required for efficient message list rendering.
  </action>
  <verify>package.json contains "react-virtuoso" in dependencies</verify>
  <done>Dependency installed and available for import</done>
</task>

<task type="auto">
  <name>Create ChatMessageList component with Virtuoso</name>
  <files>web/src/components/chat/ChatMessageList.tsx</files>
  <action>
    Create web/src/components/chat/ChatMessageList.tsx with:

    1. Component interface accepting:
       - messages: Message[] array (from @/types/api)
       - className?: string for styling flexibility
       - onScrollToTop?: callback for historical pagination (for 04-04)

    2. Use Virtuoso with these configurations:
       - followOutput="smooth" for auto-scroll to new messages
       - initialTopMostItemIndex={Math.max(0, messages.length - 1)} to start at bottom
       - style={{ height: '100%' }} for full container height
       - increaseViewportBy={{ top: 100, bottom: 100 }} for smoother overscan

    3. itemContent rendering:
       - User messages (role: 'user'): right-aligned, blue/primary background
       - Assistant messages (role: 'assistant'): left-aligned, gray/secondary background
       - System messages (role: 'system'): centered, small, muted text

    4. Empty state: Centered "No messages yet" text when messages array is empty

    5. Loading state: Optional isLoading prop with spinner at bottom

    Follow CONTEXT.md specifications for bubble-style layout (user right, AI left).

    Use existing Message type from @/types/api.ts (has seq, role, content, timestamp).
  </action>
  <verify>
    1. Component compiles without TypeScript errors
    2. Imports Virtuoso from 'react-virtuoso'
    3. Exports ChatMessageList as default export
  </verify>
  <done>
    ChatMessageList component renders messages with:
    - Virtualization (only visible messages in DOM)
    - Bubble layout (user right, assistant left)
    - Auto-scroll to new messages via followOutput
  </done>
</task>

<task type="auto">
  <name>Create chat component barrel export</name>
  <files>web/src/components/chat/index.ts</files>
  <action>
    Create web/src/components/chat/index.ts with:
    - Export ChatMessageList as default
    - This enables clean imports: import ChatMessageList from '@/components/chat'
  </action>
  <verify>index.ts file exists and exports ChatMessageList</verify>
  <done>Barrel export created for clean import paths</done>
</task>

</tasks>

<verification>
1. Component renders without TypeScript errors
2. Virtuoso is properly imported and configured with followOutput
3. Empty state displays when no messages
4. Message roles have distinct visual styles (user vs assistant)
</verification>

<success_criteria>
ChatMessageList component can be imported and renders a list of messages with:
- Virtualization (check DevTools Elements for limited DOM nodes with 100+ messages)
- Bubble-style layout per CONTEXT.md
- Auto-scroll to new messages via followOutput="smooth"
</success_criteria>

<output>
After completion, create `.planning/phases/04-real-time-chat/04-01-SUMMARY.md`
</output>
