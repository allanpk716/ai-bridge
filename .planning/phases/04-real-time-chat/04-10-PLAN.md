---
phase: 04-real-time-chat
plan: 10
type: execute
wave: 4
depends_on: [04-09]
files_modified:
  - web/src/components/commands/CommandDetail.tsx
  - web/src/components/commands/CommandList.tsx
  - web/src/components/commands/CommandPalette.tsx
  - web/src/components/commands/index.ts
autonomous: true

must_haves:
  truths:
    - "User can view command description and examples"
    - "Category badges show command source (builtin/user/project)"
    - "Command list shows all commands with badges"
    - "Examples are formatted as code blocks"
  artifacts:
    - path: "web/src/components/commands/CommandDetail.tsx"
      provides: "Command detail view with examples"
      min_lines: 50
    - path: "web/src/components/commands/CommandList.tsx"
      provides: "Browsable command list with category badges"
      min_lines: 60
    - path: "web/src/components/commands/CommandPalette.tsx"
      contains: "CommandDetail|CommandList"
  key_links:
    - from: "web/src/components/commands/CommandDetail.tsx"
      to: "web/src/components/chat/CodeBlock.tsx"
      via: "component reuse for examples"
      pattern: "CodeBlock"
    - from: "web/src/components/commands/CommandList.tsx"
      to: "web/src/lib/api/commands.ts"
      via: "useCommands hook"
      pattern: "useCommands"
---

<objective>
Create command detail and list views that show command descriptions, examples, and category badges. The detail view provides comprehensive information about a selected command, while the list view enables browsing all available commands with visual category indicators.

Purpose: CONTEXT.md specifies "命令分类 — 徽章标记" and users need to see command details before execution. Examples help users understand proper command syntax.

Output: CommandDetail component with description/examples, CommandList with badges, and CommandPalette integration.
</objective>

<execution_context>
@C:\Users\allan716\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\allan716\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-real-time-chat/04-CONTEXT.md

# Existing components
@web/src/components/commands/CommandPalette.tsx
@web/src/components/chat/CodeBlock.tsx
@web/src/components/ui/badge.tsx
@web/src/lib/api/commands.ts
@web/src/types/api.ts
</context>

<tasks>

<task type="auto">
  <name>Create CommandDetail component</name>
  <files>web/src/components/commands/CommandDetail.tsx</files>
  <action>
    Create web/src/components/commands/CommandDetail.tsx with:

    1. Component interface:
       - command: Command (from @/types/api)
       - className?: string

    2. Content sections:
       - Command path: Large, bold, monospace font
       - Category badge: Small badge showing category
       - Description: Paragraph text
       - Examples: List of example usages

    3. Category badge styling:
       - Use Badge from @/components/ui/badge
       - Color coding:
         * builtin: blue/primary
         * user: green/secondary
         * project: orange/accent
       - Position: Next to command path

    4. Examples display:
       - Map over command.examples array
       - Each example in a CodeBlock (reuse from @/components/chat)
       - Language: "bash" for shell commands
       - Label: "Example {n+1}"

    5. Layout structure:
       ```
       /commit                    [builtin]
       Create a git commit

       Examples:
       ┌─────────────────────────────┐
       │ Example 1                   │
       ├─────────────────────────────┤
       │ /commit                     │
       └─────────────────────────────┘

       ┌─────────────────────────────┐
       │ Example 2                   │
       ├─────────────────────────────┤
       │ /commit Fix bug in auth     │
       └─────────────────────────────┘
       ```

    6. Empty state:
       - If no examples, show "No examples available"

    Use CodeBlock from @/components/chat for syntax highlighting.
  </action>
  <verify>
    1. Component displays command path and description
    2. Category badge shows with color coding
    3. Examples render in CodeBlock components
    4. Empty state shows when no examples
  </verify>
  <done>CommandDetail shows command info with examples</done>
</task>

<task type="auto">
  <name>Create CommandList component</name>
  <files>web/src/components/commands/CommandList.tsx</files>
  <action>
    Create web/src/components/commands/CommandList.tsx with:

    1. Component interface:
       - sessionId?: string (for project-specific commands)
       - onSelectCommand?: (command: Command) => void
       - className?: string

    2. Data fetching:
       - useCommands(sessionId) for grouped commands
       - Handle loading/error states

    3. List structure:
       - Flatten grouped commands into single list
       - Each item shows:
         * Command path (monospace, bold)
         * Category badge (color-coded)
         * Description (truncated if long)
       - Hover effect: background highlight
       - Click: calls onSelectCommand

    4. Category badges:
       - Same color coding as CommandDetail
       - Position: Right side of item
       - Small size (badge-sm)

    5. Filtering (optional):
       - Add search input at top
       - Filter by path or description
       - Real-time as user types

    6. Empty states:
       - Loading: Show spinner
       - No commands: "No commands available"
       - No search results: "No matching commands"

    7. Layout:
       - Max-height container with overflow-y-auto
       - Compact spacing between items
       - Border and rounded corners

    This provides a browsable alternative to the command palette.
  </action>
  <verify>
    1. Component fetches commands via useCommands
    2. Commands show with category badges
    3. Click triggers onSelectCommand callback
    4. Search filters commands (if implemented)
  </verify>
  <done>CommandList shows browsable command list with badges</done>
</task>

<task type="auto">
  <name>Integrate detail views into CommandPalette</name>
  <files>web/src/components/commands/CommandPalette.tsx</files>
  <action>
    Update web/src/components/commands/CommandPalette.tsx:

    1. Add detail view mode:
       - selectedCommand state: Command | null
       - When user selects command (but doesn't confirm), show detail

    2. Two-view layout:
       - Default: Command list with groups (existing)
       - Detail: CommandDetail for selected command
       - Back button to return to list

    3. Update item selection:
       - Command.Item onSelect shows detail first
       - Detail view has "Insert" or "Execute" button
       - Button calls original onSelectCommand callback

    4. Keyboard navigation:
       - Enter on item: show detail
       - Enter in detail: confirm and close
       - Escape: go back to list or close

    5. Optional: Add preview pane
       - Split view: list on left, detail on right
       - For desktop widths only

    This adds depth to command browsing without complexity.
  </action>
  <verify>
    1. Selecting command shows CommandDetail
    2. Detail view has back button
    3. Confirm button closes palette and calls callback
    4. Keyboard navigation works in both views
  </verify>
  <done>CommandPalette shows detail view before confirmation</done>
</task>

<task type="auto">
  <name>Export command components from barrel</name>
  <files>web/src/components/commands/index.ts</files>
  <action>
    Update web/src/components/commands/index.ts to export:
    - CommandPalette as default export
    - CommandDetail as named export
    - CommandList as named export
  </action>
  <verify>All command components available from barrel</verify>
  <done>Command components cleanly exported</done>
</task>

</tasks>

<verification>
1. CommandDetail displays command with category badge
2. Examples shown in code blocks
3. CommandList shows all commands with badges
4. CommandPalette integrates detail view
5. Keyboard navigation works for detail flow
</verification>

<success_criteria>
CommandPalette:
- Shows list grouped by category
- Selecting item shows CommandDetail
- Detail has back and confirm buttons
- Confirm closes palette with selected command

CommandDetail:
- Shows command path with category badge
- Displays description
- Lists examples in code blocks
</success_criteria>

<output>
After completion, create `.planning/phases/04-real-time-chat/04-10-SUMMARY.md`
</output>
