---
phase: 04-real-time-chat
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/components/chat/ChatInput.tsx
  - web/src/components/chat/index.ts
  - web/src/lib/api/messages.ts
autonomous: true

must_haves:
  truths:
    - "User can type text into the chat input"
    - "Send button transmits message to backend API"
    - "Input clears after successful send"
    - "Send is disabled when input is empty or sending"
  artifacts:
    - path: "web/src/components/chat/ChatInput.tsx"
      provides: "Message input with send functionality"
      min_lines: 60
    - path: "web/src/lib/api/messages.ts"
      provides: "sendMessage function and hook"
      exports: ["sendMessage", "useSendMessage"]
  key_links:
    - from: "web/src/components/chat/ChatInput.tsx"
      to: "/api/v1/sessions/:sessionId/messages"
      via: "useSendMessage hook"
      pattern: "useSendMessage|sendMessage"
---

<objective>
Create a chat input component with textarea for typing messages and send button that posts to the backend API. The input should support multi-line messages, keyboard shortcuts (Enter to send, Shift+Enter for new line), and provide visual feedback during sending.

Purpose: Users need a way to send messages to Claude Code. This component provides the primary input mechanism for the chat interface.

Output: ChatInput component with textarea, send button, and integration with the messages API for sending.
</objective>

<execution_context>
@C:\Users\allan716\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\allan716\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-real-time-chat/04-CONTEXT.md

# Existing API structure
@web/src/lib/api/messages.ts
@web/src/types/api.ts
</context>

<tasks>

<task type="auto">
  <name>Add sendMessage function and hook to messages API</name>
  <files>web/src/lib/api/messages.ts</files>
  <action>
    Add to web/src/lib/api/messages.ts:

    1. sendMessage function:
       - POST to `sessions/${sessionId}/messages`
       - Accept: { content: string }
       - Returns: Message with assigned seq number
       - Use fetchWithErrorHandling for error handling

    2. useSendMessage hook:
       - Takes sessionId parameter
       - Returns useMutation with sendMessage
       - onSuccess: Invalidate messages query for refetch
       - onError: Show error toast via sonner

    Follow existing patterns from useApprovePermission/useDenyPermission for mutation structure.
  </action>
  <verify>
    1. sendMessage is exported from messages.ts
    2. useSendMessage hook is exported
    3. Hook invalidates messages query on success
  </verify>
  <done>API layer supports sending messages with automatic query refresh</done>
</task>

<task type="auto">
  <name>Create ChatInput component</name>
  <files>web/src/components/chat/ChatInput.tsx</files>
  <action>
    Create web/src/components/chat/ChatInput.tsx with:

    1. Component interface:
       - sessionId: string (required)
       - onSent?: callback after successful send
       - disabled?: boolean for external disable
       - placeholder?: string for custom placeholder text

    2. State:
       - messageText: string for textarea value
       - Use useSendMessage(sessionId) for mutation

    3. UI elements:
       - Textarea with:
         * auto-resize using rows attribute (min 1, max 10)
         * Enter key sends, Shift+Enter for new line
         * placeholder: "Type a message..."
       - Send button with:
         * Send icon from lucide-react
         * Disabled when empty or sending
         * Loading state during mutation

    4. Styling:
       - Flex row layout with textarea flex-1
       - Bottom-fixed position or sticky footer
       - Border-top separator from message list
       - Consistent padding (4)

    5. Keyboard handling:
       - onKeyDown: if Enter && !Shift -> send
       - Prevent default send behavior when Shift held

    6. Send flow:
       - Call sendMessage.mutate({ content: messageText.trim() })
       - Clear textarea on success
       - Call onSent callback if provided

    Use existing Input and Button components from @/components/ui.
  </action>
  <verify>
    1. Component accepts sessionId prop
    2. Textarea supports Enter to send, Shift+Enter for newline
    3. Send button disabled when input empty
    4. Component clears after successful send
  </verify>
  <done>
    ChatInput component renders textarea with send button that:
    - Sends messages to backend API
    - Clears on success
    - Handles keyboard shortcuts
  </done>
</task>

<task type="auto">
  <name>Export ChatInput from barrel</name>
  <files>web/src/components/chat/index.ts</files>
  <action>
    Update web/src/components/chat/index.ts to also export:
    - ChatInput as named export
    - Keep ChatMessageList as default
  </action>
  <verify>ChatInput is available via '@/components/chat'</verify>
  <done>Both chat components available from barrel export</done>
</task>

</tasks>

<verification>
1. sendMessage API function exists with correct endpoint
2. useSendMessage hook properly invalidates messages query
3. ChatInput component handles keyboard shortcuts
4. Input clears after successful send
5. Send button state reflects mutation status
</verification>

<success_criteria>
ChatInput can be imported from '@/components/chat' and:
- Renders textarea with send button
- Sends messages via POST /api/v1/sessions/:id/messages
- Clears on success
- Handles Enter/Shift+Enter correctly
</success_criteria>

<output>
After completion, create `.planning/phases/04-real-time-chat/04-02-SUMMARY.md`
</output>
