---
phase: 04-real-time-chat
plan: 06
type: execute
wave: 2
depends_on: [04-04]
files_modified:
  - web/src/components/chat/StreamingIndicator.tsx
  - web/src/components/chat/TypingIndicator.tsx
  - web/src/components/chat/StreamingErrorCard.tsx
  - web/src/components/chat/ChatMessageList.tsx
  - web/src/components/chat/index.ts
autonomous: true

must_haves:
  truths:
    - "Loading spinner shows when fetching initial messages"
    - "Typing indicator displays when assistant is generating response"
    - "Stop button appears during streaming"
    - "Error card shows with retry button when streaming fails"
    - "Indicators don't cause layout shift"
  artifacts:
    - path: "web/src/components/chat/StreamingIndicator.tsx"
      provides: "Streaming state indicator with stop button"
      min_lines: 40
    - path: "web/src/components/chat/TypingIndicator.tsx"
      provides: "Animated typing indicator"
      min_lines: 30
    - path: "web/src/components/chat/StreamingErrorCard.tsx"
      provides: "Error card with retry for failed streams"
      min_lines: 40
    - path: "web/src/components/chat/ChatMessageList.tsx"
      provides: "Message list with loading and error states"
      contains: "StreamingIndicator|TypingIndicator|StreamingErrorCard|isLoading"
  key_links:
    - from: "web/src/components/chat/ChatMessageList.tsx"
      to: "web/src/components/chat/StreamingIndicator.tsx"
      via: "render prop or direct component"
      pattern: "StreamingIndicator"
    - from: "web/src/components/chat/ChatMessageList.tsx"
      to: "web/src/components/chat/StreamingErrorCard.tsx"
      via: "render on error state"
      pattern: "StreamingErrorCard"
---

<objective>
Create loading and streaming indicators for visual feedback during message operations. This includes a loading skeleton for initial fetch, a typing indicator for AI responses, a streaming indicator with stop button, and an error card with retry for failed streams.

Purpose: Visual feedback is critical for UX. Users need to know when the system is working, when AI is thinking, should be able to stop long-running responses, and should see actionable error messages when streaming fails. CONTEXT.md specifies "错误处理 — 显示错误卡片 — 流式过程中断时,在消息位置显示错误提示+重试按钮".

Output: StreamingIndicator (stop button), TypingIndicator (animated dots), StreamingErrorCard (error + retry), and ChatMessageList integration.
</objective>

<execution_context>
@C:\Users\allan716\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\allan716\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-real-time-chat/04-CONTEXT.md

# Existing components
@web/src/components/chat/ChatMessageList.tsx
@web/src/components/ui/button.tsx
@web/src/hooks/useChatMessages.ts
</context>

<tasks>

<task type="auto">
  <name>Create TypingIndicator component</name>
  <files>web/src/components/chat/TypingIndicator.tsx</files>
  <action>
    Create web/src/components/chat/TypingIndicator.tsx with:

    1. Component interface:
       - No props required (self-contained indicator)

    2. Visual design:
       - Three animated dots (bounce animation)
       - Gray/muted color
       - Horizontal layout with spacing
       - Compact size (16px dots)

    3. Animation using Tailwind:
       - Use animate-bounce or custom @keyframes
       - Stagger animation delays for wave effect
       - CSS: `@keyframes typing { 0%, 60%, 100% { transform: translateY(0); } 30% { transform: translateY(-4px); } }`

    4. Layout:
       - Flex row, gap-1
       - Each dot: w-2 h-2 rounded-full bg-muted-foreground
       - Animation delays: 0ms, 150ms, 300ms

    Follow CONTEXT.md "显示光标动画 — 流式过程中显示动画光标或三个跳动的圆点"
  </action>
  <verify>
    1. Component renders three dots
    2. Dots have staggered bounce animation
    3. Component is compact and centered
  </verify>
  <done>TypingIndicator displays animated bouncing dots</done>
</task>

<task type="auto">
  <name>Create StreamingIndicator component</name>
  <files>web/src/components/chat/StreamingIndicator.tsx</files>
  <action>
    Create web/src/components/chat/StreamingIndicator.tsx with:

    1. Component interface:
       - isStreaming: boolean (show when true)
       - onStop?: () => void callback for stop button
       - className?: string

    2. Visual design:
       - Thin horizontal bar above streaming message
       - Left side: "Claude is thinking..." or similar text
       - Right side: Stop button with Square icon
       - Border bottom separator from message content
       - Muted background color

    3. Stop button:
       - Use Button from @/components/ui
       - variant="ghost" or "outline"
       - Square icon from lucide-react
       - Tooltip: "Stop generation (Esc)"
       - Calls onStop callback

    4. Animation:
       - Optional: subtle pulse on text
       - Or show TypingIndicator inline with text

    5. Keyboard shortcut:
       - Document handles Escape key to trigger onStop
       - This is handled by parent component

    Follow CONTEXT.md "停止生成 — 流式过程中消息上方显示「停止生成」按钮"
  </action>
  <verify>
    1. Component shows when isStreaming=true
    2. Stop button triggers onStop callback
    3. Component is hidden when isStreaming=false
  </verify>
  <done>StreamingIndicator shows stop button during streaming</done>
</task>

<task type="auto">
  <name>Integrate indicators into ChatMessageList</name>
  <files>web/src/components/chat/ChatMessageList.tsx</files>
  <action>
    Update web/src/components/chat/ChatMessageList.tsx:

    1. Add loading state props:
       - isLoading: boolean (initial fetch)
       - isStreaming: boolean (AI response in progress)
       - onStopStreaming?: () => void (stop button callback)

    2. Loading state (initial fetch):
       - When isLoading=true and messages.length===0:
         * Show centered loading spinner
         * Use Loader2 icon with animate-spin
         * "Loading messages..." text below

    3. Streaming state:
       - When isStreaming=true:
         * Render StreamingIndicator above last (streaming) message
         * Or add as component in message list
         * Pass onStop callback to StreamingIndicator

    4. Typing indicator:
       - When waiting for first token of streaming response:
         * Show TypingIndicator after last message
         * Small, unobtrusive

    5. Layout considerations:
       - Indicators shouldn't cause scroll jump
       - Use absolute positioning or fixed height
       - Stop button positioned near streaming message

    6. Virtuoso integration:
       - Use components={<BottomItem />} for loading indicator
       - Or use ItemContent to render indicators inline
  </action>
  <verify>
    1. ChatMessageList shows loading spinner when isLoading
    2. StreamingIndicator appears when isStreaming
    3. Stop button is clickable and triggers callback
    4. TypingIndicator shows during token wait
  </verify>
  <done>
    ChatMessageList displays:
    - Loading state for initial fetch
    - Streaming indicator with stop button
    - Typing indicator during AI thinking
  </done>
</task>

<task type="auto">
  <name>Create StreamingErrorCard component</name>
  <files>web/src/components/chat/StreamingErrorCard.tsx</files>
  <action>
    Create web/src/components/chat/StreamingErrorCard.tsx with:

    1. Component interface:
       - error: Error | string (the error message)
       - onRetry: () => void callback
       - className?: string

    2. Visual design:
       - Card with error styling (red/danger theme)
       - Error icon (AlertCircle from lucide-react)
       - Error message (truncate long messages)
       - Retry button with RefreshCw icon

    3. Layout:
       - Horizontal layout: icon + message (flex-1) + retry button
       - Compact height (not a full message bubble)
       - Appears at position where streaming failed

    4. Retry behavior:
       - Calls onRetry callback
       - Button shows loading state during retry
       - Optional: auto-hide after successful retry

    5. Error messages:
       - Network errors: "Connection lost. Retry?"
       - Timeout: "Request timed out. Retry?"
       - Generic: "Something went wrong. Retry?"

    Follow CONTEXT.md: "错误处理 — 显示错误卡片 — 流式过程中断时,在消息位置显示错误提示+重试按钮"
  </action>
  <verify>
    1. Component displays error message
    2. Retry button triggers onRetry callback
    3. Error icon shows with AlertCircle
    4. Card has red/danger styling
  </verify>
  <done>StreamingErrorCard shows error with retry button</done>
</task>

<task type="auto">
  <name>Export indicators from barrel</name>
  <files>web/src/components/chat/index.ts</files>
  <action>
    Update web/src/components/chat/index.ts to export:
    - StreamingIndicator as named export
    - TypingIndicator as named export
    - StreamingErrorCard as named export
    - Keep existing exports
  </action>
  <verify>All three indicators available via '@/components/chat'</verify>
  <done>All chat components available from barrel export</done>
</task>

</tasks>

<verification>
1. TypingIndicator animates three dots with staggered timing
2. StreamingIndicator shows stop button during streaming
3. ChatMessageList shows loading state for initial fetch
4. Stop button callback works
5. StreamingErrorCard displays on stream error
6. Error card retry button triggers callback
7. Indicators don't cause layout shifts
</verification>

<success_criteria>
ChatMessageList with isLoading=true shows centered spinner.
ChatMessageList with isStreaming=true shows StreamingIndicator with stop button.
ChatMessageList with streamingError shows StreamingErrorCard at error position.
Stop button click triggers onStop callback.
Retry button triggers onRetry callback.
</success_criteria>

<output>
After completion, create `.planning/phases/04-real-time-chat/04-06-SUMMARY.md`
</output>
