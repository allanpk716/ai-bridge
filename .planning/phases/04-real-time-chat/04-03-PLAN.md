---
phase: 04-real-time-chat
plan: 03
type: execute
wave: 1
depends_on: [04-01]
files_modified:
  - web/package.json
  - web/src/components/chat/StreamingMessage.tsx
  - web/src/components/chat/ChatMessageList.tsx
  - web/src/components/chat/index.ts
autonomous: true

must_haves:
  truths:
    - "Streaming messages render character-by-character as content arrives"
    - "Incomplete markdown doesn't break formatting during stream"
    - "Streaming indicator shows during active stream"
    - "Memoization prevents unnecessary re-renders"
  artifacts:
    - path: "web/src/components/chat/StreamingMessage.tsx"
      provides: "Streaming markdown message renderer"
      min_lines: 50
      contains: "Streamdown|streamdown"
    - path: "web/package.json"
      provides: "streamdown dependency"
      contains: "streamdown"
  key_links:
    - from: "web/src/components/chat/StreamingMessage.tsx"
      to: "streamdown"
      via: "import { Streamdown } from 'streamdown'"
      pattern: "import.*Streamdown.*from.*streamdown"
---

<objective>
Create a streaming message component using streamdown that handles incomplete markdown during AI responses. The component will render character-by-character updates with proper markdown parsing, code highlighting, and a visual streaming indicator.

Purpose: AI responses stream token-by-token. Standard markdown parsers break on incomplete syntax (e.g., `**bold` without closing). Streamdown handles this gracefully with animation and memoization.

Output: StreamingMessage component that renders streaming content with incomplete markdown support and visual indicators.
</objective>

<execution_context>
@C:\Users\allan716\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\allan716\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-real-time-chat/04-RESEARCH.md
@.planning/phases/04-real-time-chat/04-CONTEXT.md

# Existing components
@web/src/components/chat/ChatMessageList.tsx
@web/src/types/api.ts
</context>

<tasks>

<task type="auto">
  <name>Install streamdown and related dependencies</name>
  <files>web/package.json</files>
  <action>
    Run: `npm install streamdown react-markdown remark-gfm` in the web directory

    streamdown: For streaming markdown with incomplete syntax support
    react-markdown: Fallback if streamdown doesn't cover all use cases
    remark-gfm: GitHub Flavored Markdown support (tables, strikethrough, etc.)
  </action>
  <verify>package.json contains streamdown, react-markdown, remark-gfm</verify>
  <done>Dependencies installed for markdown streaming</done>
</task>

<task type="auto">
  <name>Create StreamingMessage component</name>
  <files>web/src/components/chat/StreamingMessage.tsx</files>
  <action>
    Create web/src/components/chat/StreamingMessage.tsx with:

    1. Component interface:
       - content: string (the streaming message content)
       - isStreaming: boolean (true while streaming, false when complete)
       - className?: string for custom styling

    2. Use Streamdown component with:
       - parseIncompleteMarkdown={true} for handling incomplete syntax
       - isAnimating={isStreaming} for cursor animation
       - caret="block" when streaming to show cursor
       - mode="streaming" for streaming-optimized rendering
       - className: prose classes for markdown styling
         * "prose dark:prose-invert max-w-none" for dark mode support

    3. Fallback to react-markdown:
       - Try Streamdown first
       - Export StreamdownMessage as primary
       - Add MarkdownMessage as fallback using react-markdown
       - Use conditional logic or try/catch for graceful fallback

    4. Code block handling:
       - Streamdown with @streamdown/code plugin if available
       - Or defer to 04-05 for code highlighting integration
       - For now, render code blocks with basic styling (pre/code tags)

    5. Streaming indicator:
       - When isStreaming=true, show animated cursor at content end
       - Streamdown's caret prop handles this natively

    6. Memoization:
       - Use React.memo for component to prevent unnecessary re-renders
       - Streamdown has built-in memoization for content

    Follow CONTEXT.md "逐字追加" pattern for streaming effect.
  </action>
  <verify>
    1. Component compiles without TypeScript errors
    2. Imports Streamdown from 'streamdown'
    3. Exports StreamingMessage as default
    4. Component is memoized with React.memo
  </verify>
  <done>
    StreamingMessage component renders:
    - Markdown content with incomplete syntax support
    - Animated cursor during streaming
    - Memoized for performance
  </done>
</task>

<task type="auto">
  <name>Integrate StreamingMessage into ChatMessageList</name>
  <files>web/src/components/chat/ChatMessageList.tsx</files>
  <action>
    Update ChatMessageList to use StreamingMessage for assistant messages:

    1. Add optional streamingMessage prop:
       - streamingContent?: string for partial content
       - streamingSeq?: number for sequence number

    2. In itemContent, for assistant messages:
       - If message.seq === streamingSeq, use StreamingMessage with streamingContent
       - Otherwise, use regular markdown rendering (or defer to 04-05)

    3. For now, use StreamingMessage for all assistant messages:
       - It handles both streaming and complete content
       - Pass isStreaming={message.seq === streamingSeq}

    This allows 04-04 (SSE) to drive streaming updates via props.
  </action>
  <verify>
    1. ChatMessageList accepts streaming props
    2. Assistant messages render via StreamingMessage
    3. Streaming indicator shows for matching seq
  </verify>
  <done>
    ChatMessageList supports streaming display with visual indicator
  </done>
</task>

<task type="auto">
  <name>Export StreamingMessage from barrel</name>
  <files>web/src/components/chat/index.ts</files>
  <action>
    Update web/src/components/chat/index.ts to export:
    - StreamingMessage as named export
    - Keep existing exports (ChatMessageList default, ChatInput named)
  </action>
  <verify>StreamingMessage is available via '@/components/chat'</verify>
  <done>All chat components available from barrel export</done>
</task>

</tasks>

<verification>
1. streamdown package installed
2. StreamingMessage component handles incomplete markdown
3. ChatMessageList integrates streaming display
4. Memoization prevents unnecessary re-renders
5. Cursor animation shows during streaming
</verification>

<success_criteria>
StreamingMessage can be imported and renders:
- Markdown with incomplete syntax (e.g., `**bold without close)
- Animated cursor when isStreaming=true
- Proper markdown when complete
</success_criteria>

<output>
After completion, create `.planning/phases/04-real-time-chat/04-03-SUMMARY.md`
</output>
