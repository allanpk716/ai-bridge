---
phase: 04-real-time-chat
plan: 08
type: execute
wave: 3
depends_on: [04-07]
files_modified:
  - web/src/components/permissions/ScopeSelector.tsx
  - web/src/components/permissions/PermissionModal.tsx
  - web/src/components/permissions/index.ts
autonomous: true

must_haves:
  truths:
    - "User can select different permission scopes"
    - "Scope options include file-read, file-write, command-exec, network"
    - "Selected scope is sent with approve request"
    - "Scope descriptions help user understand implications"
  artifacts:
    - path: "web/src/components/permissions/ScopeSelector.tsx"
      provides: "Permission scope selection interface"
      min_lines: 60
    - path: "web/src/components/permissions/PermissionModal.tsx"
      contains: "ScopeSelector"
  key_links:
    - from: "web/src/components/permissions/PermissionModal.tsx"
      to: "web/src/components/permissions/ScopeSelector.tsx"
      via: "component composition"
      pattern: "ScopeSelector"
---

<objective>
Create a scope selector component that allows users to choose the permission scope (file-read, file-write, command-exec, network) when approving a permission request. The selector should show descriptions for each scope to help users understand the implications.

Purpose: CONTEXT.md specifies "作用域选择 — 多选复选框 — 用户可选择特定文件或「全部允许」". Scope selection is critical for security - users should grant minimum required permissions.

Output: ScopeSelector component with radio buttons for scope selection, integrated into PermissionModal.
</objective>

<execution_context>
@C:\Users\allan716\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\allan716\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-real-time-chat/04-CONTEXT.md

# Existing components
@web/src/components/permissions/PermissionModal.tsx
@web/src/components/ui/radio-group.tsx
@web/src/types/api.ts
</context>

<tasks>

<task type="auto">
  <name>Add RadioGroup UI component if needed</name>
  <files>web/src/components/ui/radio-group.tsx</files>
  <action>
    Check if RadioGroup exists in @/components/ui. If not:
    - Run: npx shadcn@latest add radio-group
    - This installs the Radix UI radio group component

    RadioGroup is needed for single-choice scope selection.
  </action>
  <verify>RadioGroup component exists in web/src/components/ui/</verify>
  <done>RadioGroup UI component available</done>
</task>

<task type="auto">
  <name>Create ScopeSelector component</name>
  <files>web/src/components/permissions/ScopeSelector.tsx</files>
  <action>
    Create web/src/components/permissions/ScopeSelector.tsx with:

    1. Component interface:
       - value: string (current selected scope)
       - onChange: (scope: string) => void
       - disabled?: boolean

    2. Scope options with descriptions:
       - file-read: "Read file contents only"
       - file-write: "Read and modify files"
       - command-exec: "Execute shell commands"
       - network: "Allow network requests"

    3. Visual design:
       - Use RadioGroup from @/components/ui
       - Vertical layout, one option per row
       - Each option:
         * Radio button on left
         * Scope name in medium font
         * Description in smaller, muted text below
         * Subtle background on hover
       - Selected option has accent border

    4. Layout structure:
       ```
       (o) file-read
           Read file contents only

       ( ) file-write
           Read and modify files

       ( ) command-exec
           Execute shell commands

       ( ) network
           Allow network requests
       ```

    5. Optional: visual icons per scope:
       - file-read: File/Eye icon
       - file-write: FileEdit/Pen icon
       - command-exec: Terminal icon
       - network: Globe icon

    Use lucide-react for icons.

    Follow CONTEXT.md "作用域选择 — 多选复选框" pattern (using radio for single scope).
  </action>
  <verify>
    1. Component uses RadioGroup
    2. Four scope options displayed
    3. Each option has name and description
    4. onChange callback fires on selection
  </verify>
  <done>ScopeSelector displays four scope options with descriptions</done>
</task>

<task type="auto">
  <name>Integrate ScopeSelector into PermissionModal</name>
  <files>web/src/components/permissions/PermissionModal.tsx</files>
  <action>
    Update web/src/components/permissions/PermissionModal.tsx:

    1. Add scope state:
       - Add scope prop: string (default 'file-read')
       - Add onScopeChange prop: (scope: string) => void
       - Or manage internally with useState

    2. Add ScopeSelector to modal:
       - Place between resources list and action buttons
       - Label: "Select permission scope:"
       - Pass current scope and onChange handler

    3. Update approve handler:
       - Pass selected scope to onApprove callback
       - Modal parent should pass scope to API

    4. Visual layout:
       - Operation (top)
       - Resources (middle, scrollable)
       - Scope selector (below resources)
       - Action buttons (bottom)

    5. Optional: smart defaults
       - Suggest scope based on operation type
       - file-read for read operations
       - file-write for write operations
  </action>
  <verify>
    1. PermissionModal renders ScopeSelector
    2. Scope selection updates local state
    3. Approve action uses selected scope
    4. Layout is clean and not cluttered
  </verify>
  <done>PermissionModal includes scope selection in approval flow</done>
</task>

<task type="auto">
  <name>Export ScopeSelector from barrel</name>
  <files>web/src/components/permissions/index.ts</files>
  <action>
    Update web/src/components/permissions/index.ts to also export:
    - ScopeSelector as named export
  </action>
  <verify>ScopeSelector is available via '@/components/permissions'</verify>
  <done>All permission components available from barrel export</done>
</task>

</tasks>

<verification>
1. ScopeSelector component exists with four options
2. Each option has name and description
3. RadioGroup enforces single selection
4. PermissionModal includes ScopeSelector
5. Selected scope passed to approve action
</verification>

<success_criteria>
Opening permission modal shows:
- Operation type
- Resource list
- Scope selector with four options
- Selected scope highlighted
- Descriptions help understand each scope
</success_criteria>

<output>
After completion, create `.planning/phases/04-real-time-chat/04-08-SUMMARY.md`
</output>
