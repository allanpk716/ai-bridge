---
phase: 04-real-time-chat
plan: 07
type: execute
wave: 3
depends_on: [04-04, 04-06]
files_modified:
  - web/src/components/permissions/PermissionCard.tsx
  - web/src/components/permissions/index.ts
  - web/src/hooks/usePermissionModal.ts
  - web/src/components/chat/ChatMessageList.tsx
autonomous: true

must_haves:
  truths:
    - "Permission card appears in message stream when request received"
    - "Card is non-blocking (user can scroll past it)"
    - "User can approve permission with selected scope"
    - "User can deny permission request"
    - "Card shows approved/denied state after action"
  artifacts:
    - path: "web/src/components/permissions/PermissionCard.tsx"
      provides: "Embedded permission request card with approve/deny"
      min_lines: 80
    - path: "web/src/hooks/usePermissionModal.ts"
      provides: "Permission state management from SSE"
      min_lines: 60
    - path: "web/src/components/chat/ChatMessageList.tsx"
      contains: "PermissionCard"
  key_links:
    - from: "web/src/hooks/usePermissionModal.ts"
      to: "/api/v1/sessions/:sessionId/messages/stream"
      via: "SSE message stream (not WebSocket)"
      pattern: "permission.*request|EventSource"
    - from: "web/src/components/permissions/PermissionCard.tsx"
      to: "web/src/lib/api/permissions.ts"
      via: "useApprovePermission|useDenyPermission"
      pattern: "useApprovePermission|useDenyPermission"
    - from: "web/src/components/chat/ChatMessageList.tsx"
      to: "web/src/components/permissions/PermissionCard.tsx"
      via: "render in message list"
      pattern: "PermissionCard"
---

<objective>
Create an embedded permission request card that appears in the message stream when Claude Code requires user approval. The card is non-blocking (users can scroll past), shows permission details, and provides approve/deny buttons. This honors the CONTEXT.md decision: "权限请求交互 — 嵌入式卡片 — 消息流中显示权限请求卡片,非阻塞".

Purpose: Permissions arrive via SSE message stream (not WebSocket events). The embedded card pattern allows users to review permissions at their own pace without blocking the conversation flow.

Output: PermissionCard component embedded in message stream, usePermissionModal hook for SSE-triggered state, ChatMessageList integration.
</objective>

<execution_context>
@C:\Users\allan716\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\allan716\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-real-time-chat/04-CONTEXT.md
@.planning/phases/04-real-time-chat/04-RESEARCH.md

# Existing API and types
@web/src/lib/api/permissions.ts
@web/src/types/api.ts
@web/src/components/ui/card.tsx
@web/src/components/ui/button.tsx
</context>

<tasks>

<task type="auto">
  <name>Create PermissionCard component</name>
  <files>web/src/components/permissions/PermissionCard.tsx</files>
  <action>
    Create web/src/components/permissions/PermissionCard.tsx with:

    1. Component interface:
       - permission: Permission (the request details)
       - onApprove: (scope: string) => void
       - onDeny: () => void
       - isLoading: boolean (mutation in progress)
       - status?: 'pending' | 'approved' | 'denied' (for historical state)

    2. Use Card from @/components/ui/card:
       - Card with CardHeader, CardContent
       - Embedded in message stream (NOT a modal/dialog)
       - Non-blocking user can scroll past

    3. Permission details display:
       - Header: "Permission Required" badge + operation type
       - Resources: List permission.resources in a scrollable area
         * Use ul/li with max-h-40 overflow-y-auto
         * Truncate long paths with text-ellipsis
       - Command content (折叠可展开):
         * Show first line truncated
         * "Show details" button to expand

    4. Action buttons:
       - Deny: Button variant="destructive" (red)
       - Approve: Button variant="default" (primary)
       - Both disabled during isLoading
       - Show Loader2 icon on buttons during mutation
       - For approved/denied status: show badge instead of buttons

    5. Styling per CONTEXT.md:
       - "精简+可展开 — 操作类型+文件路径,命令内容折叠可展开查看详情"
       - "嵌入式卡片 — 消息流中显示权限请求卡片,非阻塞"
       - Compact layout with clear visual hierarchy
       - Warning/yellow accent to indicate attention needed

    6. Status display:
       - pending: Show approve/deny buttons
       - approved: Green badge "Approved", no buttons
       - denied: Red badge "Denied", no buttons

    Use existing Permission type from @/types/api.ts.
  </action>
  <verify>
    1. Component uses Card from shadcn/ui (NOT Dialog)
    2. Displays operation and resources
    3. Has approve and deny buttons
    4. Buttons respect isLoading state
    5. Shows status badge for approved/denied
  </verify>
  <done>PermissionCard displays embedded permission request with approve/deny actions</done>
</task>

<task type="auto">
  <name>Create usePermissionModal hook (SSE-based)</name>
  <files>web/src/hooks/usePermissionModal.ts</files>
  <action>
    Create web/src/hooks/usePermissionModal.ts with:

    1. Hook interface:
       - sessionId: string | undefined
       - Returns: { permissions, handleApprove, handleDeny }

    2. State management:
       - useState for permissions array (Permission[])
       - Track permission by requestId for deduplication

    3. SSE integration for permission requests:
       - Listen to message stream via useChatMessages or directly
       - Parse SSE messages for permission type
       - When permission message arrives: add to permissions array
       - DO NOT use useSocketEvent - permissions are in SSE message stream

    4. Mutation hooks:
       - useApprovePermission(sessionId)
       - useDenyPermission(sessionId)

    5. Handlers:
       - handleApprove: (requestId: string, scope: string) => void
         * Calls approvePermission.mutate
         * Updates permission status to 'approved'
       - handleDeny: (requestId: string) => void
         * Calls denyPermission.mutate
         * Updates permission status to 'denied'

    6. Return permissions array for rendering in ChatMessageList
       - ChatMessageList renders PermissionCard for each pending permission
       - This is the "embedded card in message stream" pattern

    IMPORTANT: Permissions come through SSE message stream, NOT WebSocket events.
    The message type will be 'permission' with requestId, operation, resources.

    7. Cleanup:
       - Clear permissions on unmount
       - Reset state when session changes
  </action>
  <verify>
    1. Hook listens to SSE message stream (not useSocketEvent)
    2. Permissions added to array when received
    3. Approve calls useApprovePermission mutation
    4. Deny calls useDenyPermission mutation
    5. Permission status updates after action
  </verify>
  <done>usePermissionModal manages permission state from SSE stream</done>
</task>

<task type="auto">
  <name>Integrate PermissionCard into ChatMessageList</name>
  <files>web/src/components/chat/ChatMessageList.tsx</files>
  <action>
    Update web/src/components/chat/ChatMessageList.tsx:

    1. Add permissions prop:
       - permissions?: Permission[]
       - onApprovePermission?: (requestId: string, scope: string) => void
       - onDenyPermission?: (requestId: string) => void

    2. Render permission cards:
       - After the relevant message (associated with the permission)
       - Or append to end of message list
       - Each permission renders PermissionCard

    3. Permission placement:
       - Find message with matching seq (if available)
       - Insert PermissionCard after that message
       - For pending permissions, show before latest message

    4. Styling:
       - Full-width card in message stream
       - Warning color scheme to draw attention
       - Non-blocking (doesn't prevent scrolling)

    5. Historical permissions:
       - Permissions with status 'approved'/'denied' still show
       - Display as collapsed card with status badge
       - User can expand to see what was approved/denied

    This implements the CONTEXT.md "嵌入式卡片" pattern - permission requests appear as cards in the message stream, not blocking modals.
  </action>
  <verify>
    1. ChatMessageList accepts permissions prop
    2. PermissionCard renders for each permission
    3. Cards appear inline in message stream
    4. Approved/denied permissions show status badge
    5. Cards don't block scrolling (non-blocking)
  </verify>
  <done>PermissionCard integrated into ChatMessageList as embedded cards</done>
</task>

<task type="auto">
  <name>Create permissions barrel export</name>
  <files>web/src/components/permissions/index.ts</files>
  <action>
    Create web/src/components/permissions/index.ts to export:
    - PermissionCard as default export
    - Enables: import PermissionCard from '@/components/permissions'
  </action>
  <verify>index.ts exports PermissionCard</verify>
  <done>Permissions components available from barrel export</done>
</task>

</tasks>

<verification>
1. PermissionCard component exists with Card structure (NOT Dialog)
2. usePermissionModal hook listens to SSE message stream (NOT useSocketEvent)
3. Approve button calls permission API with scope
4. Deny button calls permission API
5. Permission cards render embedded in ChatMessageList
6. Cards are non-blocking (user can scroll past)
7. Status badges show for approved/denied permissions
</verification>

<success_criteria>
When permission message arrives via SSE:
1. Permission added to state array
2. PermissionCard renders in ChatMessageList
3. Card shows operation type and resource list
4. Approve button sends approval to API
5. Deny button sends denial to API
6. Card updates to show status badge after action
7. User can scroll past pending permissions
</success_criteria>

<output>
After completion, create `.planning/phases/04-real-time-chat/04-07-SUMMARY.md`
</output>
