---
phase: 04-real-time-chat
plan: 11
type: execute
wave: 4
depends_on: [04-02, 04-09, 04-10]
files_modified:
  - web/src/components/commands/CommandExecutor.tsx
  - web/src/components/commands/CommandExecutor.test.tsx
  - web/src/components/commands/index.ts
  - web/src/components/chat/ChatInput.tsx
  - web/src/pages/SessionDetail.tsx
autonomous: true

must_haves:
  truths:
    - "Selected command populates chat input"
    - "User can edit command before sending"
    - "Command executes via API and shows result"
    - "Execution result displays in message stream"
  artifacts:
    - path: "web/src/components/commands/CommandExecutor.tsx"
      provides: "Command execution integration with chat input"
      min_lines: 50
    - path: "web/src/pages/SessionDetail.tsx"
      contains: "CommandPalette|CommandExecutor"
  key_links:
    - from: "web/src/components/commands/CommandExecutor.tsx"
      to: "web/src/lib/api/commands.ts"
      via: "useExecuteCommand hook"
      pattern: "useExecuteCommand"
    - from: "web/src/components/commands/CommandExecutor.tsx"
      to: "web/src/components/chat/ChatInput.tsx"
      via: "ref or callback for populating input"
      pattern: "ChatInput"
---

<objective>
Create command execution interface that populates the chat input with selected slash commands, allows editing before sending, and displays execution results. This completes the slash command feature allowing users to discover, select, and execute commands through the UI.

Purpose: CONTEXT.md specifies "命令执行 — 填充到输入框 — 选择命令后先填充到输入框,用户可编辑参数后手动发送". Users should be able to discover commands, preview them, edit parameters, then execute.

Output: CommandExecutor component that integrates CommandPalette with ChatInput, SessionDetail integration with command trigger button.
</objective>

<execution_context>
@C:\Users\allan716\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\allan716\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-real-time-chat/04-CONTEXT.md

# Existing components
@web/src/components/commands/CommandPalette.tsx
@web/src/components/commands/CommandDetail.tsx
@web/src/components/chat/ChatInput.tsx
@web/src/lib/api/commands.ts
@web/src/pages/SessionDetail.tsx
</context>

<tasks>

<task type="auto">
  <name>Create CommandExecutor component</name>
  <files>web/src/components/commands/CommandExecutor.tsx</files>
  <action>
    Create web/src/components/commands/CommandExecutor.tsx with:

    1. Component interface:
       - sessionId: string
       - onCommandInserted?: (commandText: string) => void
       - trigger: React.ReactNode (button/icon that opens palette)
       - className?: string

    2. State:
       - paletteOpen: boolean for CommandPalette visibility
       - useExecuteCommand(sessionId) for execution

    3. Command selection handler:
       - onCommandSelected: (command: Command) => void
       - Inserts command text into input via onCommandInserted
       - Format: `{command.path}` or `{command.path} {args}`
       - For commands with examples, use first example as template

    4. CommandPalette integration:
       - Render CommandPalette with open/onOpenChange
       - Pass sessionId for project-specific commands
       - Pass onSelectCommand handler

    5. Direct execution mode (optional):
       - If onCommandInserted not provided, execute directly
       - Call executeCommand.mutate with command path
       - Show toast with result

    6. Trigger button:
       - Render trigger prop as clickable element
       - onClick opens palette
       - Default: button with "/" icon or "Commands" text

    Use useExecuteCommand from @/lib/api/commands.
  </action>
  <verify>
    1. Component renders trigger button
    2. Clicking opens CommandPalette
    3. Selecting command calls onCommandInserted
    4. Command text formatted correctly
  </verify>
  <done>CommandExecutor provides command selection with input population</done>
</task>

<task type="auto">
  <name>Update ChatInput to support external value setting</name>
  <files>web/src/components/chat/ChatInput.tsx</files>
  <action>
    Update web/src/components/chat/ChatInput.tsx:

    1. Add inputRef prop:
       - ref: React.RefObject<HTMLTextAreaElement>
       - Allows parent to directly set textarea value

    2. Forward ref to textarea:
       - Use React.forwardRef for component
       - Attach ref to underlying textarea element
       - Export ref type for TypeScript consumers

    3. Alternative: add value prop with controlled mode:
       - value?: string for external control
       - onChange?: (value: string) => void
       - When value provided, use controlled input

    4. Focus management:
       - When value set externally, also focus textarea
       - Place cursor at end of inserted text
       - Use setSelectionRange for cursor positioning

    5. For command insertion:
       - CommandExecutor calls: inputRef.current.value = commandText
       - Or: setValue(commandText) if using controlled mode
       - Then: inputRef.current.focus()
       - Then: inputRef.current.setSelectionRange(text.length, text.length)

    This enables CommandExecutor to populate input after command selection.
  </action>
  <verify>
    1. ChatInput is wrapped with forwardRef
    2. Ref forwards to textarea element
    3. External code can set textarea value
    4. Textarea focuses when value set
    5. Cursor position at end of text
  </verify>
  <done>ChatInput supports external value population via ref</done>
</task>

<task type="auto">
  <name>Test CommandExecutor to ChatInput integration</name>
  <files>web/src/components/commands/CommandExecutor.test.tsx</files>
  <action>
    Create web/src/components/commands/CommandExecutor.test.tsx with integration tests:

    1. Command selection populates input:
       - Render CommandExecutor with mock ChatInput ref
       - Simulate command selection from CommandPalette
       - Verify ChatInput textarea value updated with command text
       - Verify textarea received focus()

    2. Ref forwarding test:
       - Render ChatInput with ref
       - Verify ref.current points to textarea element
       - Verify ref.current.value can be set externally
       - Verify ref.current.focus() can be called

    3. Cursor position test:
       - Set value via ref
       - Verify selectionStart === selectionEnd === value.length
       - Ensures cursor at end of inserted text

    4. Edit after insertion test:
       - Insert command via ref
       - Simulate user typing additional text
       - Verify value concatenates correctly
       - Verify send still works with edited command

    Use @testing-library/react, vi.fn for mocks, userEvent for interactions.
  </action>
  <verify>
    1. CommandExecutor.test.ts exists with 4+ test cases
    2. Ref forwarding test passes
    3. Command population test verifies value set and focus called
    4. Edit test confirms user can modify inserted command
  </verify>
  <done>
    CommandExecutor -> ChatInput integration verified:
    - Ref forwarding works correctly
    - Commands populate input with focus
    - Cursor positioned at end
    - User can edit before sending
  </done>
</task>

<task type="auto">
  <name>Integrate CommandExecutor into SessionDetail</name>
  <files>web/src/pages/SessionDetail.tsx</files>
  <action>
    Update web/src/pages/SessionDetail.tsx:

    1. Add command trigger button:
       - Place in SessionHeader or near ChatInput
       - Icon: Terminal or Slash icon from lucide-react
       - Button variant: "outline" or "ghost"
       - Tooltip: "Browse commands (Ctrl+K)"

    2. Render CommandExecutor:
       - With sessionId prop
       - Custom trigger button (from step 1)
       - onCommandInserted handler

    3. Command insertion handler:
       - Create inputRef for ChatInput using useRef
       - Pass to ChatInput ref prop
       - onCommandInserted: (text) => {
           if (inputRef.current) {
             inputRef.current.value = text
             inputRef.current.focus()
             inputRef.current.setSelectionRange(text.length, text.length)
           }
         }

    4. ChatInput ref update:
       - Pass inputRef to ChatInput
       - Ensure ChatInput forwards ref to textarea (tested in Task 2)

    5. Layout:
       - Trigger button in header: opens palette
       - Ctrl+K also opens palette (CommandPalette handles this)
       - Selected command populates input
       - User edits and sends manually

    6. Alternative: Add "/" button to ChatInput:
       - Small button inside or near textarea
       - Opens CommandPalette directly
       - More discoverable than header button

    Follow CONTEXT.md: "/触发快速命令面板,工具栏按钮提供完整浏览"
  </action>
  <verify>
    1. SessionDetail renders CommandExecutor
    2. Trigger button opens CommandPalette
    3. Selecting command populates ChatInput
    4. User can edit command before sending
    5. Ctrl+K keyboard shortcut works
  </verify>
  <done>SessionDetail has integrated command execution</done>
</task>

<task type="auto">
  <name>Export CommandExecutor from barrel</name>
  <files>web/src/components/commands/index.ts</files>
  <action>
    Update web/src/components/commands/index.ts to export:
    - CommandExecutor as named export
    - Keep existing exports
  </action>
  <verify>CommandExecutor available from barrel</verify>
  <done>All command components available</done>
</task>

</tasks>

<verification>
1. CommandExecutor component exists with CommandPalette
2. ChatInput supports external value setting via ref (TESTED)
3. ChatInput forwards ref to textarea (TESTED)
4. CommandExecutor populates ChatInput when command selected (TESTED)
5. Cursor positioned at end of inserted text (TESTED)
6. SessionDetail renders command trigger button
7. Selecting command populates chat input
8. User can edit command text before sending
9. Ctrl+K opens command palette
10. Send button executes the command
</verification>

<success_criteria>
Command execution flow:
1. Click command button or press Ctrl+K
2. CommandPalette opens with all commands
3. Select command (shows detail view)
4. Confirm selection
5. Command text populates ChatInput (ref forwarding verified)
6. Cursor at end of text (selectionRange verified)
7. User edits if needed
8. User sends message
9. Command executes and result shows in messages
</success_criteria>

<output>
After completion, create `.planning/phases/04-real-time-chat/04-11-SUMMARY.md`
</output>
