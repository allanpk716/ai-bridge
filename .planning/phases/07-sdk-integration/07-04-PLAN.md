---
phase: 07-sdk-integration
plan: 04
type: execute
wave: 3
depends_on: ["07-01", "07-02", "07-03"]
files_modified:
  - web/src/sdk-bridge/handlers.ts
  - web/src/sdk-bridge/messageIntegration.ts
  - web/src/api/messages.ts
  - sdk/src/core/client.ts
autonomous: false

must_haves:
  truths:
    - "外部应用可以通过 SDK.sendMessage(text) 发送消息"
    - "消息通过 postMessage 传递到 Web 应用"
    - "Web 应用调用现有 API 发送消息到后端"
    - "Claude 响应通过 postMessage 返回给 SDK"
  artifacts:
    - path: "web/src/sdk-bridge/messageIntegration.ts"
      provides: "SDK 消息与 API 集成层"
      exports: ["sendSdkMessageToBackend", "handleStreamingResponse"]
    - path: "web/src/api/messages.ts"
      contains: "sendSdkMessage"
      exports: ["useSdkSendMessage"]
    - path: "sdk/src/core/client.ts"
      contains: "sendMessage"
      exports: ["AIBridgeSDK"]
  key_links:
    - from: "web/src/sdk-bridge/messageIntegration.ts"
      to: "web/src/api/messages.ts"
      via: "import sendSdkMessage"
      pattern: "import.*sendSdkMessage"
    - from: "web/src/sdk-bridge/handlers.ts"
      to: "web/src/sdk-bridge/messageIntegration.ts"
      via: "import sendSdkMessageToBackend"
      pattern: "import.*sendSdkMessageToBackend"
---

<objective>
实现文本消息 API,使外部应用能够通过简单的 `sendMessage(text: string)` 接口发送消息到 Claude。

Purpose: 提供简单易用的 API 接口,隐藏 postMessage 通信的复杂性,让外部应用只需调用 `sendMessage(text)` 即可与 Claude 交互。
Output: 完整的消息发送流程,从 SDK 到 Web 应用到后端 API,以及响应回传。
</objective>

<execution_context>
@C:\Users\allan716\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\allan716/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-sdk-integration/07-RESEARCH.md
@C:\WorkSpace\ai-bridge\web\src\api\messages.ts
@C:\WorkSpace\ai-bridge\web\src\sdk-bridge\handlers.ts
</context>

<tasks>

<task type="auto">
  <name>创建 SDK 消息与 API 集成层</name>
  <files>
    web/src/sdk-bridge/messageIntegration.ts
  </files>
  <action>
    创建 SDK 消息与后端 API 的集成层:

    **web/src/sdk-bridge/messageIntegration.ts**:
    ```typescript
      { useCallback } from 'react';
      type { SdkIncomingMessage } from './types';
      type { Message, MessageResponse } from '@/types/messages';

    /**
     * SDK 消息处理结果
     */
    export interface SdkMessageResult {
      messageId: string;
      success: boolean;
      content?: string;
      error?: string;
      metadata?: {
        model: string;
        tokensUsed: number;
        duration: number;
      };
    }

    /**
     * 流式响应处理器接口
     */
    export interface StreamingHandler {
      onStart: (messageId: string) => void;
      onChunk: (messageId: string, content: string) => void;
      onComplete: (result: SdkMessageResult) => void;
      onError: (messageId: string, error: Error) => void;
    }

    /**
     * 发送 SDK 消息到后端
     *
     * 将来自 SDK 的消息转发到后端 API,
     * 处理流式响应并返回结果。
     */
    export async function sendSdkMessageToBackend(
      text: string,
      sessionId: string | undefined,
      signal?: AbortSignal
    ): Promise<SdkMessageResult> {
      const startTime = Date.now();

      try {
        // 导入 API 函数
        const { sendMessage } = await import('@/api/messages');

        // 发送消息到后端
        const response = await sendMessage(text, sessionId, signal);

        return {
          messageId: response.id,
          success: true,
          content: response.content,
          metadata: {
            model: response.model || 'haiku',
            tokensUsed: response.tokens || 0,
            duration: Date.now() - startTime,
          },
        };
      } catch (error) {
        return {
          messageId: `error_${Date.now()}`,
          success: false,
          error: error instanceof Error ? error.message : 'Unknown error',
        };
      }
    }

    /**
     * 处理流式响应的 Hook
     *
     * 用于处理 SDK 消息的流式响应。
     */
    export function useSdkStreamingHandler(
      sessionId: string | undefined
    ): StreamingHandler {
      const onStart = useCallback((messageId: string) => {
        console.log('[SdkStreaming] Started:', messageId);
        // TODO: 可以在这里触发 UI 更新
      }, []);

      const onChunk = useCallback((messageId: string, content: string) => {
        console.log('[SdkStreaming] Chunk:', messageId, content.slice(0, 50));
        // TODO: 可以在这里将 chunk 发送给 SDK
      }, []);

      const onComplete = useCallback((result: SdkMessageResult) => {
        console.log('[SdkStreaming] Complete:', result);
        // 将最终结果发送给 SDK
        sendResultToSdk(result);
      }, []);

      const onError = useCallback((messageId: string, error: Error) => {
        console.error('[SdkStreaming] Error:', messageId, error);
        // 将错误发送给 SDK
        sendErrorToSdk(error.message, 'STREAM_ERROR');
      }, []);

      return { onStart, onChunk, onComplete, onError };
    }

    /**
     * 发送结果到 SDK
     */
    function sendResultToSdk(result: SdkMessageResult): void {
      // 导入发送函数
      import('./handlers').then(({ sendToSdk }) => {
        sendToSdk({
          type: 'messageResponse',
          payload: {
            messageId: result.messageId,
            success: result.success,
            content: result.content,
            error: result.error,
            metadata: result.metadata ? {
              model: result.metadata.model,
              tokensUsed: result.metadata.tokensUsed,
            } : undefined,
          },
        });
      });
    }

    /**
     * 创建 SDK 消息处理器
     *
     * 用于在 SDK 桥接层处理发送消息请求。
     */
    export function createSdkMessageHandler() {
      const activeRequests = new Map<string, AbortController>();

      return {
        async handle(
          text: string,
          sessionId: string | undefined,
          messageId: string
        ): Promise<SdkMessageResult> {
          // 创建取消控制器
          const controller = new AbortController();
          activeRequests.set(messageId, controller);

          try {
            const result = await sendSdkMessageToBackend(
              text,
              sessionId,
              controller.signal
            );
            return result;
          } finally {
            activeRequests.delete(messageId);
          }
        },

        cancel(messageId: string): void {
          const controller = activeRequests.get(messageId);
          if (controller) {
            controller.abort();
            activeRequests.delete(messageId);
          }
        },

        cancelAll(): void {
          activeRequests.forEach((controller) => controller.abort());
          activeRequests.clear();
        },
      };
    }
    ```

    注意: 需要确保类型导入正确,可能需要调整导入路径。
  </action>
  <verify>运行 npm run typecheck 确保类型定义无错误</verify>
  <done>SDK 消息与 API 集成层创建完成</done>
</task>

<task type="auto">
  <name>更新 SDK 桥接层使用集成层</name>
  <files>
    web/src/sdk-bridge/handlers.ts
  </files>
  <action>
    更新 SDK 消息处理器,使用新的集成层:

    修改 **web/src/sdk-bridge/handlers.ts**:

    1. 导入集成层函数:
    ```typescript
    import { createSdkMessageHandler, sendResultToSdk } from './messageIntegration';
    ```

    2. 创建全局消息处理器实例:
    ```typescript
    // 在文件顶部,状态定义之后添加
    const sdkMessageHandler = createSdkMessageHandler();
    ```

    3. 替换 handleSendMessageRequest 函数:
    ```typescript
    /**
     * 处理发送消息请求
     */
    async function handleSendMessageRequest(payload: SdkIncomingMessage & { type: 'sendMessage' }): Promise<void> {
      const { payload: msgPayload } = payload;

      try {
        // 使用集成层发送消息
        const result = await sdkMessageHandler.handle(
          msgPayload.text,
          msgPayload.sessionId,
          msgPayload.messageId
        );

        // 发送结果到 SDK(集成层会处理)
        sendResultToSdk(result);
      } catch (error) {
        // 发送错误到 SDK
        sendToSdk({
          type: 'messageResponse',
          payload: {
            messageId: msgPayload.messageId,
            success: false,
            error: error instanceof Error ? error.message : 'Unknown error',
          },
        });
      }
    }
    ```

    4. 确保 sendToSdk 函数正确定义(如果之前是占位实现):
    ```typescript
    /**
     * 发送消息到 SDK
     */
    function sendToSdk(message: SdkOutgoingMessage): boolean {
      if (!state.parentOrigin || !window.parent) {
        console.warn('[SdkBridge] Cannot send message: no parent window');
        return false;
      }

      try {
        window.parent.postMessage(message, state.parentOrigin);
        return true;
      } catch (error) {
        console.error('[SdkBridge] Failed to send message:', error);
        return false;
      }
    }

    // 导出供集成层使用
    export { sendToSdk };
    ```
  </action>
  <verify>运行 npm run typecheck 确保类型定义无错误</verify>
  <done>SDK 桥接层更新完成,使用集成层</done>
</task>

<task type="auto">
  <name>增强 SDK 客户端的消息 API</name>
  <files>
    sdk/src/core/client.ts
  </files>
  <action>
    增强 SDK 客户端类,提供更友好的消息 API:

    修改 **sdk/src/core/client.ts**,添加便捷方法:

    1. 添加简单消息发送方法:
    ```typescript
    /**
     * 发送文本消息(简化版)
     *
     * @param text - 要发送的文本内容
     * @returns Promise<MessageResponse> Claude 的响应
     *
     * @example
     * const response = await sdk.chat('Hello, Claude!');
     * console.log(response.content);
     */
    async chat(text: string): Promise<MessageResponse> {
      return this.sendMessage(text);
    }

    /**
     * 批量发送消息
     *
     * @param messages - 消息数组
     * @returns Promise<MessageResponse[]> 所有响应
     *
     * @example
     * const responses = await sdk.batch([
     *   'First message',
     *   'Second message',
     * ]);
     */
    async batch(messages: string[]): Promise<MessageResponse[]> {
      const responses: MessageResponse[] = [];

      for (const message of messages) {
        const response = await this.sendMessage(message);
        responses.push(response);
      }

      return responses;
    }

    /**
     * 流式发送消息(带回调)
     *
     * @param text - 要发送的文本
     * @param callbacks - 回调函数
     * @returns Promise<MessageResponse> 最终响应
     *
     * @example
     * await sdk.stream('Long message...', {
     *   onChunk: (content) => console.log('Received:', content),
     *   onComplete: (response) => console.log('Done:', response),
     * });
     */
    async stream(
      text: string,
      callbacks?: {
        onProgress?: (delta: string) => void;
        onComplete?: (response: MessageResponse) => void;
        onError?: (error: Error) => void;
      }
    ): Promise<MessageResponse> {
      try {
        const response = await this.sendMessage(text);
        callbacks?.onComplete?.(response);
        return response;
      } catch (error) {
        callbacks?.onError?.(error as Error);
        throw error;
      }
    }
    ```

    2. 添加消息历史管理:
    ```typescript
    // 在类属性中添加
    private messageHistory: MessageResponse[] = [];

    /**
     * 获取消息历史
     */
    getMessageHistory(): MessageResponse[] {
      return [...this.messageHistory];
    }

    /**
     * 清空消息历史
     */
    clearHistory(): void {
      this.messageHistory = [];
    }

    /**
     * 修改 sendMessage 方法,记录历史
     */
    async sendMessage(text: string): Promise<MessageResponse> {
      // ... 现有代码

      try {
        const response = await this.bridge.sendAndWait(message, 30000);

        if (response.type === 'messageResponse') {
          // 记录到历史
          this.messageHistory.push(response.payload);

          // 触发回调
          this.config.onMessage?.(response.payload);

          return response.payload;
        }
      } catch (error) {
        // ... 错误处理
        throw error;
      }
    }
    ```
  </action>
  <verify>运行 npm run build 确保 SDK 构建成功</verify>
  <done>SDK 客户端消息 API 增强完成</done>
</task>

<task type="checkpoint:human-verify">
  <what-built>完整的文本消息 API,支持通过 SDK 发送消息到 Claude</what-built>
  <how-to-verify>
    1. 创建测试 HTML 文件(test-sdk.html)在 examples/ 目录:
       ```html
       <!DOCTYPE html>
       <html>
       <head>
         <title>SDK Test</title>
       </head>
       <body>
         <div id="container" style="height: 600px; width: 800px;"></div>
         <button onclick="testMessage()">Send Test Message</button>
         <script type="module">
           import { AIBridgeSDK } from '../sdk/dist/ai-bridge-sdk.es.js';

           const sdk = new AIBridgeSDK({
             url: 'http://localhost:3000',
             targetOrigin: 'http://localhost:3000',
             context: { theme: 'light' },
             onMessage: (msg) => console.log('Received:', msg),
             onStateChange: (state) => console.log('State:', state),
           });

           document.getElementById('container').appendChild(sdk.iframe);

           window.testMessage = async () => {
             try {
               const response = await sdk.sendMessage('Hello from SDK!');
               console.log('Response:', response);
             } catch (error) {
               console.error('Error:', error);
             }
           };
         </script>
       </body>
       </html>
       ```

    2. 启动 Web 应用开发服务器
    3. 在浏览器中打开测试 HTML 文件
    4. 点击 "Send Test Message" 按钮
    5. 验证:
       - iframe 正确加载 ai-bridge-web
       - 控制台显示连接状态变化
       - 消息发送成功
       - 收到 Claude 响应
  </how-to-verify>
  <resume-signal>测试通过,SDK 能够成功发送消息并接收响应</resume-signal>
</task>

</tasks>

<verification>
1. SDK 消息与 API 集成层正确转发消息到后端
2. 流式响应正确处理并返回给 SDK
3. SDK 提供简化的消息发送接口
4. 消息历史正确记录和管理
</verification>

<success_criteria>
1. SDK.sendMessage(text) 能够成功发送消息
2. 消息通过 postMessage 到达 Web 应用
3. Web 应用调用后端 API 发送消息
4. 响应正确返回给 SDK
</success_criteria>

<output>
完成创建 `.planning/phases/07-sdk-integration/07-04-SUMMARY.md`
</output>
