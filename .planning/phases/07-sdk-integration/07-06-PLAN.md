---
phase: 07-sdk-integration
plan: 06
type: execute
wave: 4
depends_on: ["07-01", "07-02", "07-03", "07-04", "07-05"]
files_modified:
  - sdk/src/core/Connection.ts
  - sdk/src/core/client.ts
  - web/src/sdk-bridge/handlers.ts
  - sdk/examples/error-handling/index.html
autonomous: false

must_haves:
  truths:
    - "SDK 在网络错误时自动尝试重连"
    - "SDK 提供错误回调通知外部应用"
    - "连接断开时 SDK 进入正确状态并通知"
    - "消息超时时 SDK 返回错误而不是无限等待"
  artifacts:
    - path: "sdk/src/core/Connection.ts"
      contains: "attemptReconnect"
      exports: ["ConnectionManager"]
    - path: "sdk/src/core/client.ts"
      contains: "onError"
      exports: ["AIBridgeSDK"]
    - path: "web/src/sdk-bridge/handlers.ts"
      contains: "sendErrorToSdk"
      exports: ["sendErrorToSdk"]
    - path: "sdk/examples/error-handling/index.html"
      provides: "错误处理示例"
      contains: "onError"
  key_links:
    - from: "sdk/src/core/Connection.ts"
      to: "sdk/src/core/client.ts"
      via: "connection.on()"
      pattern: "connection\\.on"
    - from: "web/src/sdk-bridge/handlers.ts"
      to: "window.parent.postMessage"
      via: "error message"
      pattern: "postMessage.*error"
---

<objective>
实现 SDK 错误处理、重连机制和恢复逻辑,确保在网络问题或其他异常情况下 SDK 能够优雅处理并尝试恢复。

Purpose: 提供生产级别的错误处理和恢复能力,使 SDK 在各种异常情况下都能保持稳定,并为外部应用提供清晰的错误信息。
Output: 完善的错误处理机制、自动重连逻辑和错误处理示例。
</objective>

<execution_context>
@C:\Users\allan716\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\allan716/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-sdk-integration/07-RESEARCH.md
@C:\WorkSpace\ai-bridge\sdk\src\core\Connection.ts
@C:\WorkSpace\ai-bridge\sdk\src\core\client.ts
@C:\WorkSpace\ai-bridge\web\src\sdk-bridge\handlers.ts
</context>

<tasks>

<task type="auto">
  <name>增强连接管理器的错误处理和重连机制</name>
  <files>
    sdk/src/core/Connection.ts
  </files>
  <action>
    更新连接管理器,添加更强大的错误处理和重连逻辑:

    修改 **sdk/src/core/Connection.ts**,增强错误处理:

    1. 添加错误类型枚举:
    ```typescript
    /**
     * 错误类型
     */
    export enum ConnectionErrorType {
      /** 网络错误 */
      NETWORK = 'network',
      /** 超时 */
      TIMEOUT = 'timeout',
      /** iframe 加载失败 */
      IFRAME_LOAD_FAILED = 'iframe_load_failed',
      /** 未授权 */
      UNAUTHORIZED = 'unauthorized',
      /** 未知错误 */
      UNKNOWN = 'unknown',
    }

    /**
     * 连接错误
     */
    export class ConnectionError extends Error {
      constructor(
        public type: ConnectionErrorType,
        message: string,
        public retryable: boolean = true
      ) {
        super(message);
        this.name = 'ConnectionError';
      }
    }
    ```

    2. 更新 ConnectionEvent 类型,包含更多错误信息:
    ```typescript
    /**
     * 连接事件类型
     */
    export type ConnectionEvent =
      | { type: 'stateChange'; state: ConnectionState }
      | { type: 'error'; error: ConnectionError; attempts?: number }
      | { type: 'heartbeat' }
      | { type: 'reconnecting'; attempt: number; maxAttempts: number }
      | { type: 'reconnectFailed'; error: ConnectionError };
    ```

    3. 增强重连逻辑:
    ```typescript
    /**
     * 尝试重连
     */
    private attemptReconnect(): void {
      if (this.reconnectAttempts >= this.config.maxReconnectAttempts) {
        const error = new ConnectionError(
          ConnectionErrorType.NETWORK,
          'Max reconnection attempts reached',
          false
        );
        this.setState(ConnectionState.ERROR);
        this.emit({
          type: 'reconnectFailed',
          error,
        });
        return;
      }

      this.reconnectAttempts++;

      // 发送重连事件
      this.emit({
        type: 'reconnecting',
        attempt: this.reconnectAttempts,
        maxAttempts: this.config.maxReconnectAttempts,
      });

      // 计算延迟时间(指数退避)
      const baseDelay = this.config.reconnectDelay;
      const exponentialDelay = Math.min(
        baseDelay * Math.pow(2, this.reconnectAttempts - 1),
        30000 // 最大 30 秒
      );

      // 添加随机抖动(避免雷鸣效应)
      const jitter = Math.random() * 1000;
      const finalDelay = exponentialDelay + jitter;

      console.log(
        `[ConnectionManager] Reconnect attempt ${this.reconnectAttempts}/${this.config.maxReconnectAttempts} in ${finalDelay}ms`
      );

      this.reconnectTimer = setTimeout(() => {
        this.setState(ConnectionState.CONNECTING);
        this.startHeartbeat();
      }, finalDelay);
    }

    /**
     * 处理致命错误
     */
    private handleFatalError(errorType: ConnectionErrorType, message: string): void {
      const error = new ConnectionError(errorType, message, false);
      this.setState(ConnectionState.ERROR);
      this.stopHeartbeat();
      this.stopReconnect();
      this.emit({
        type: 'error',
        error,
      });
    }

    /**
     * 处理可恢复错误
     */
    private handleRecoverableError(errorType: ConnectionErrorType, message: string): void {
      const error = new ConnectionError(errorType, message, true);
      this.emit({
        type: 'error',
        error,
      });
      // 不立即断开,让心跳机制处理
    }

    /**
     * 重置重连计数
     */
    public resetReconnectAttempts(): void {
      this.reconnectAttempts = 0;
    }

    /**
     * 手动触发重连
     */
    public reconnect(): void {
      this.stopReconnect();
      this.reconnectAttempts = 0;
      this.setState(ConnectionState.CONNECTING);
      this.startHeartbeat();
    }
    ```

    4. 添加连接健康检查:
    ```typescript
    /**
     * 检查连接是否健康
     */
    public isHealthy(): boolean {
      return this.state === ConnectionState.CONNECTED &&
        this.missedHeartbeats < Math.floor(this.config.maxMissedHeartbeats / 2);
    }

    /**
     * 获取连接统计信息
     */
    public getStats() {
      return {
        state: this.state,
        missedHeartbeats: this.missedHeartbeats,
        reconnectAttempts: this.reconnectAttempts,
        isHealthy: this.isHealthy(),
      };
    }
    ```
  </action>
  <verify>运行 npm run typecheck 确保类型定义无错误</verify>
  <done>连接管理器错误处理增强完成</done>
</task>

<task type="auto">
  <name>增强 SDK 客户端的错误处理</name>
  <files>
    sdk/src/core/client.ts
  </files>
  <action>
    更新 SDK 客户端类,添加完善的错误处理:

    修改 **sdk/src/core/client.ts**,增强错误处理:

    1. 添加错误类型定义:
    ```typescript
    /**
     * SDK 错误类型
     */
    export enum SDKErrorType {
      /** iframe 未就绪 */
      IFRAME_NOT_READY = 'IFRAME_NOT_READY',
      /** 连接断开 */
      DISCONNECTED = 'DISCONNECTED',
      /** 消息超时 */
      MESSAGE_TIMEOUT = 'MESSAGE_TIMEOUT',
      /** 无效响应 */
      INVALID_RESPONSE = 'INVALID_RESPONSE',
      /** 发送失败 */
      SEND_FAILED = 'SEND_FAILED',
    }

    /**
     * SDK 错误
     */
    export class SDKError extends Error {
      constructor(
        public type: SDKErrorType,
        message: string,
        public originalError?: unknown
      ) {
        super(message);
        this.name = 'SDKError';
      }
    }
    ```

    2. 更新 sendMessage 方法,添加详细错误处理:
    ```typescript
    /**
     * 发送消息到 Claude
     */
    async sendMessage(text: string, options?: {
      timeout?: number;
      retry?: number;
    }): Promise<MessageResponse> {
      const timeout = options?.timeout ?? 30000;
      const maxRetries = options?.retry ?? 1;

      // 检查连接状态
      if (!this.connection.isConnected()) {
        throw new SDKError(
          SDKErrorType.DISCONNECTED,
          'SDK not connected. Wait for the connection to be established.',
        );
      }

      // 验证输入
      if (!text || text.trim().length === 0) {
        throw new SDKError(
          SDKErrorType.INVALID_RESPONSE,
          'Message text cannot be empty',
        );
      }

      if (text.length > 10000) {
        throw new SDKError(
          SDKErrorType.INVALID_RESPONSE,
          'Message text too long (max 10000 characters)',
        );
      }

      let lastError: Error | null = null;

      // 重试逻辑
      for (let attempt = 0; attempt <= maxRetries; attempt++) {
        try {
          const messageId = `msg_${Date.now()}_${this.messageId++}`;

          const message: SdkMessage = {
            type: 'sendMessage',
            payload: {
              text,
              sessionId: this.config.context?.sessionId,
              messageId,
            },
          };

          const response = await this.bridge.sendAndWait(message, timeout);

          if (response.type === 'messageResponse') {
            // 记录到历史
            this.messageHistory.push(response.payload);

            // 触发回调
            this.config.onMessage?.(response.payload);

            return response.payload;
          } else {
            throw new SDKError(
              SDKErrorType.INVALID_RESPONSE,
              `Unexpected response type: ${response.type}`,
            );
          }
        } catch (error) {
          lastError = error as Error;

          // 如果是最后一次尝试,不继续重试
          if (attempt === maxRetries) {
            break;
          }

          // 某些错误不应该重试
          if (error instanceof SDKError) {
            if (error.type === SDKErrorType.INVALID_RESPONSE) {
              break;
            }
          }

          // 等待后重试
          await new Promise(resolve => setTimeout(resolve, 1000 * (attempt + 1)));
        }
      }

      // 所有重试都失败
      throw new SDKError(
        SDKErrorType.SEND_FAILED,
        `Failed to send message after ${maxRetries + 1} attempts`,
        lastError
      );
    }
    ```

    3. 添加连接事件监听器增强:
    ```typescript
    // 在构造函数中增强连接事件监听
    this.connection.on((event) => {
      switch (event.type) {
        case 'stateChange':
          this.config.onStateChange?.(event.state);

          // 如果从连接状态变为断开,通知等待中的消息
          if (event.state === ConnectionState.DISCONNECTED ||
              event.state === ConnectionState.ERROR) {
            this.rejectPendingMessages(new SDKError(
              SDKErrorType.DISCONNECTED,
              'Connection lost while waiting for response'
            ));
          }
          break;

        case 'error':
          // 转换连接错误为 SDK 错误
          this.config.onError?.(new SDKError(
            SDKErrorType.DISCONNECTED,
            event.error.message,
            event.error
          ));
          break;

        case 'reconnecting':
          console.log(`[AIBridgeSDK] Reconnecting... (${event.attempt}/${event.maxAttempts})`);
          break;

        case 'reconnectFailed':
          this.config.onError?.(new SDKError(
            SDKErrorType.DISCONNECTED,
            'Reconnection failed',
            event.error
          ));
          break;
      }
    });
    ```

    4. 添加拒绝所有待处理消息的方法:
    ```typescript
    /**
     * 拒绝所有待处理的消息
     */
    private rejectPendingMessages(error: SDKError): void {
      // 注意: 这需要在 Bridge 类中实现获取待处理消息的方法
      // 或者在这里维护一个额外的消息队列
      this.config.onError?.(error);
    }
    ```

    5. 添加便捷的错误处理方法:
    ```typescript
    /**
     * 检查 SDK 是否可用
     */
    isAvailable(): boolean {
      return this.connection.isConnected() &&
        this.bridge.isActive();
    }

    /**
     * 等待 SDK 连接就绪
     */
    async ready(timeout = 30000): Promise<void> {
      return new Promise((resolve, reject) => {
        if (this.connection.isConnected()) {
          resolve();
          return;
        }

        const timeoutId = setTimeout(() => {
          cleanup();
          reject(new SDKError(
            SDKErrorType.MESSAGE_TIMEOUT,
            'SDK ready timeout'
          ));
        }, timeout);

        const unsubscribe = this.connection.on((event) => {
          if (event.type === 'stateChange' &&
              event.state === ConnectionState.CONNECTED) {
            cleanup();
            resolve();
          } else if (event.type === 'reconnectFailed') {
            cleanup();
            reject(new SDKError(
              SDKErrorType.DISCONNECTED,
              'Failed to connect'
            ));
          }
        });

        const cleanup = () => {
          clearTimeout(timeoutId);
          unsubscribe();
        };
      });
    }

    /**
     * 获取诊断信息
     */
    getDiagnostics() {
      return {
        state: this.connection.getState(),
        stats: this.connection.getStats(),
        pendingMessages: this.bridge.getPendingCount(),
        messageHistoryLength: this.messageHistory.length,
        iframeAttached: !!this.iframe.parentNode,
      };
    }
    ```
  </action>
  <verify>运行 npm run build 确保 SDK 构建成功</verify>
  <done>SDK 客户端错误处理增强完成</done>
</task>

<task type="auto">
  <name>增强 Web 应用端的错误处理</name>
  <files>
    web/src/sdk-bridge/handlers.ts
  </files>
  <action>
    更新 Web 应用 SDK 桥接层的错误处理:

    修改 **web/src/sdk-bridge/handlers.ts**,添加完善的错误处理:

    1. 添加错误类型定义:
    ```typescript
    /**
     * SDK 桥接错误类型
     */
    export enum SdkBridgeErrorType {
      /** 消息验证失败 */
      VALIDATION_ERROR = 'VALIDATION_ERROR',
      /** API 调用失败 */
      API_ERROR = 'API_ERROR',
      /** 消息发送失败 */
      SEND_ERROR = 'SEND_ERROR',
      /** 未授权 */
      UNAUTHORIZED = 'UNAUTHORIZED',
    }

    /**
     * SDK 桥接错误
     */
    export class SdkBridgeError extends Error {
      constructor(
        public type: SdkBridgeErrorType,
        message: string,
        public details?: unknown
      ) {
        super(message);
        this.name = 'SdkBridgeError';
      }
    }
    ```

    2. 增强 sendErrorToSdk 函数:
    ```typescript
    /**
     * 发送错误到 SDK
     */
    export function sendErrorToSdk(
      message: string,
      code?: string,
      details?: unknown
    ): boolean {
      if (!state.parentOrigin || !window.parent) {
        console.warn('[SdkBridge] Cannot send error: no parent window');
        return false;
      }

      try {
        window.parent.postMessage(
          {
            type: 'error',
            payload: {
              message,
              code,
              details,
              timestamp: Date.now(),
            },
          },
          state.parentOrigin
        );
        return true;
      } catch (error) {
        console.error('[SdkBridge] Failed to send error:', error);
        return false;
      }
    }
    ```

    3. 增强消息处理器的错误处理:
    ```typescript
    /**
     * 处理发送消息请求(带完整错误处理)
     */
    async function handleSendMessageRequest(
      payload: SdkIncomingMessage & { type: 'sendMessage' }
    ): Promise<void> {
      const { payload: msgPayload } = payload;

      try {
        // 验证消息内容
        if (!msgPayload.text || msgPayload.text.trim().length === 0) {
          throw new SdkBridgeError(
            SdkBridgeErrorType.VALIDATION_ERROR,
            'Message text cannot be empty'
          );
        }

        if (msgPayload.text.length > 10000) {
          throw new SdkBridgeError(
            SdkBridgeErrorType.VALIDATION_ERROR,
            'Message text too long (max 10000 characters)'
          );
        }

        // 使用集成层发送消息
        const result = await sdkMessageHandler.handle(
          msgPayload.text,
          msgPayload.sessionId,
          msgPayload.messageId
        );

        // 发送结果到 SDK
        sendResultToSdk(result);

      } catch (error) {
        // 记录详细错误
        console.error('[SdkBridge] Message send error:', error);

        // 发送错误到 SDK
        const errorCode = error instanceof SdkBridgeError
          ? error.type
          : 'UNKNOWN_ERROR';

        sendToSdk({
          type: 'messageResponse',
          payload: {
            messageId: msgPayload.messageId,
            success: false,
            error: error instanceof Error ? error.message : 'Unknown error',
          },
        });

        // 也可以发送专门的错误消息
        sendErrorToSdk(
          error instanceof Error ? error.message : 'Unknown error',
          errorCode,
          error instanceof SdkBridgeError ? error.details : undefined
        );
      }
    }
    ```

    4. 添加全局错误处理:
    ```typescript
    /**
     * 处理并记录错误
     */
    export function handleBridgeError(
      error: unknown,
      context?: string
    ): void {
      const message = error instanceof Error ? error.message : 'Unknown error';

      console.error(`[SdkBridge] ${context || 'Error'}:`, error);

      // 如果是已连接状态,尝试发送错误给 SDK
      if (state.isConnected) {
        sendErrorToSdk(message, context);
      }

      // 可以在这里添加错误上报逻辑
      // 例如发送到错误追踪服务
    }

    /**
     * 设置全局错误处理
     */
    export function setupGlobalErrorHandling(): void {
      // 处理未捕获的 Promise 错误
      window.addEventListener('unhandledrejection', (event) => {
        handleBridgeError(event.reason, 'Unhandled Promise Rejection');
      });

      // 处理未捕获的错误
      window.addEventListener('error', (event) => {
        handleBridgeError(event.error, 'Unhandled Error');
      });
    }
    ```
  </action>
  <verify>运行 npm run typecheck 确保类型定义无错误</verify>
  <done>Web 应用端错误处理增强完成</done>
</task>

<task type="auto">
  <name>创建错误处理示例</name>
  <files>
    sdk/examples/error-handling/index.html
  </files>
  <action>
    创建错误处理示例,展示如何处理各种 SDK 错误:

    **sdk/examples/error-handling/index.html**:
    ```html
    <!DOCTYPE html>
    <html lang="zh-CN">
      <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>AI-Bridge SDK - Error Handling Example</title>
        <style>
          * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
          }

          body {
            font-family: system-ui, -apple-system, sans-serif;
            padding: 20px;
            max-width: 800px;
            margin: 0 auto;
          }

          h1 {
            margin-bottom: 20px;
          }

          .status {
            padding: 12px 16px;
            border-radius: 8px;
            margin-bottom: 20px;
          }

          .status.connected {
            background: #d1fae5;
            color: #065f46;
          }

          .status.disconnected {
            background: #fee2e2;
            color: #991b1b;
          }

          .status.connecting {
            background: #fef3c7;
            color: #92400e;
          }

          .section {
            background: white;
            border: 1px solid #e5e7eb;
            border-radius: 8px;
            padding: 16px;
            margin-bottom: 16px;
          }

          .section h2 {
            font-size: 16px;
            margin-bottom: 12px;
          }

          button {
            padding: 8px 16px;
            border: none;
            border-radius: 6px;
            background: #3b82f6;
            color: white;
            cursor: pointer;
            margin-right: 8px;
            margin-bottom: 8px;
          }

          button:disabled {
            opacity: 0.5;
            cursor: not-allowed;
          }

          button.danger {
            background: #ef4444;
          }

          .log {
            background: #1f2937;
            color: #f3f4f6;
            padding: 12px;
            border-radius: 6px;
            font-family: monospace;
            font-size: 12px;
            max-height: 300px;
            overflow-y: auto;
          }

          .log-entry {
            margin-bottom: 4px;
          }

          .log-error { color: #f87171; }
          .log-warn { color: #fbbf24; }
          .log-info { color: #60a5fa; }
          .log-success { color: #34d399; }

          input {
            padding: 8px 12px;
            border: 1px solid #d1d5db;
            border-radius: 6px;
            width: 100%;
            margin-bottom: 12px;
          }
        </style>
      </head>
      <body>
        <h1>AI-Bridge SDK - 错误处理示例</h1>

        <div id="status" class="status connecting">连接中...</div>

        <div class="section">
          <h2>连接控制</h2>
          <button id="btn-reconnect">重新连接</button>
          <button id="btn-diagnostics">获取诊断信息</button>
        </div>

        <div class="section">
          <h2>消息测试</h2>
          <input type="text" id="message-input" placeholder="输入消息..." />
          <button id="btn-send">发送消息</button>
          <button id="btn-send-empty">发送空消息(触发错误)</button>
          <button id="btn-send-long">发送过长消息(触发错误)</button>
        </div>

        <div class="section">
          <h2>错误模拟</h2>
          <button id="btn-simulate-disconnect" class="danger">模拟断开连接</button>
          <button id="btn-simulate-timeout">模拟超时</button>
        </div>

        <div class="section">
          <h2>日志</h2>
          <div id="log" class="log"></div>
        </div>

        <script type="module">
          import { AIBridgeSDK, ConnectionState, SDKError } from '../../dist/ai-bridge-sdk.es.js';

          // UI 元素
          const statusEl = document.getElementById('status');
          const logEl = document.getElementById('log');
          const inputEl = document.getElementById('message-input');

          let sdk = null;

          // 日志函数
          function log(message, type = 'info') {
            const entry = document.createElement('div');
            entry.className = `log-entry log-${type}`;
            entry.textContent = `[${new Date().toLocaleTimeString()}] ${message}`;
            logEl.insertBefore(entry, logEl.firstChild);

            // 限制日志数量
            while (logEl.children.length > 100) {
              logEl.removeChild(logEl.lastChild);
            }
          }

          // 更新状态显示
          function updateStatus(state) {
            statusEl.className = 'status';
            statusEl.classList.add(state);

            switch (state) {
              case ConnectionState.CONNECTING:
                statusEl.textContent = '连接中...';
                break;
              case ConnectionState.CONNECTED:
                statusEl.textContent = '已连接';
                break;
              case ConnectionState.DISCONNECTED:
                statusEl.textContent = '已断开';
                break;
              case ConnectionState.ERROR:
                statusEl.textContent = '错误状态';
                break;
            }
          }

          // 初始化 SDK
          function init() {
            try {
              sdk = new AIBridgeSDK({
                url: 'http://localhost:3000',
                targetOrigin: 'http://localhost:3000',
                onStateChange: (state) => {
                  log(`状态变化: ${state}`, 'info');
                  updateStatus(state);
                },
                onMessage: (message) => {
                  log(`收到消息: ${message.content?.slice(0, 50)}...`, 'success');
                },
                onError: (error) => {
                  log(`错误: ${error.message}`, 'error');
                  if (error.type) {
                    log(`错误类型: ${error.type}`, 'warn');
                  }
                },
              });

              log('SDK 初始化成功', 'success');
            } catch (error) {
              log(`SDK 初始化失败: ${error.message}`, 'error');
            }
          }

          // 发送消息
          async function sendMessage(text) {
            if (!sdk) {
              log('SDK 未初始化', 'error');
              return;
            }

            try {
              log(`发送消息: ${text}`, 'info');
              const response = await sdk.sendMessage(text);
              log(`收到响应: ${response.content?.slice(0, 50)}...`, 'success');
            } catch (error) {
              log(`发送失败: ${error.message}`, 'error');
              if (error.type) {
                log(`错误类型: ${error.type}`, 'warn');
              }
            }
          }

          // 获取诊断信息
          function showDiagnostics() {
            if (!sdk) {
              log('SDK 未初始化', 'error');
              return;
            }

            const diagnostics = sdk.getDiagnostics();
            log('诊断信息:', 'info');
            log(JSON.stringify(diagnostics, null, 2), 'info');
          }

          // 按钮事件
          document.getElementById('btn-reconnect').addEventListener('click', () => {
            log('触发重新连接', 'info');
            // SDK 内部会自动重连,这里只是演示
            if (sdk && sdk.connection) {
              sdk.connection.reconnect();
            }
          });

          document.getElementById('btn-diagnostics').addEventListener('click', showDiagnostics);

          document.getElementById('btn-send').addEventListener('click', () => {
            const text = inputEl.value.trim();
            if (text) {
              sendMessage(text);
              inputEl.value = '';
            }
          });

          document.getElementById('btn-send-empty').addEventListener('click', () => {
            sendMessage(''); // 触发空消息错误
          });

          document.getElementById('btn-send-long').addEventListener('click', () => {
            sendMessage('a'.repeat(11000)); // 触发过长消息错误
          });

          document.getElementById('btn-simulate-disconnect').addEventListener('click', () => {
            log('模拟断开连接', 'warn');
            if (sdk) {
              sdk.destroy();
            }
          });

          document.getElementById('btn-simulate-timeout').addEventListener('click', async () => {
            log('发送超时测试消息...', 'info');
            try {
              // 发送一个会超时的消息
              if (sdk) {
                await sdk.sendMessage('timeout test', { timeout: 1 });
              }
            } catch (error) {
              log(`超时错误(预期): ${error.message}`, 'warn');
            }
          });

          // 初始化
          init();

          // 页面卸载时清理
          window.addEventListener('beforeunload', () => {
            if (sdk) {
              sdk.destroy();
            }
          });
        </script>
      </body>
    </html>
    ```

    创建配套的 README:
    **sdk/examples/error-handling/README.md**:
    ```markdown
    # 错误处理示例

    本示例演示了如何处理 AI-Bridge SDK 中的各种错误情况。

    ## 运行示例

    1. 确保 AI-Bridge Web 应用运行在 http://localhost:3000
    2. 在浏览器中打开此 HTML 文件
    3. 尝试各种按钮来触发不同的错误情况

    ## 错误类型

    ### SDKErrorType

    - `IFRAME_NOT_READY` - iframe 未就绪
    - `DISCONNECTED` - 连接断开
    - `MESSAGE_TIMEOUT` - 消息超时
    - `INVALID_RESPONSE` - 无效响应
    - `SEND_FAILED` - 发送失败

    ## 最佳实践

    1. **始终检查连接状态**
       ```javascript
       if (!sdk.connection.isConnected()) {
         // 处理未连接状态
       }
       ```

    2. **使用 try-catch 包裹异步调用**
       ```javascript
       try {
         const response = await sdk.sendMessage(text);
       } catch (error) {
         if (error.type === SDKErrorType.MESSAGE_TIMEOUT) {
           // 处理超时
         } else if (error.type === SDKErrorType.DISCONNECTED) {
           // 处理断开连接
         }
       }
       ```

    3. **实现错误回调**
       ```javascript
       const sdk = new AIBridgeSDK({
         // ...
         onError: (error) => {
           console.error('SDK Error:', error.message, error.type);
           // 发送到错误追踪服务
           trackError(error);
         },
       });
       ```

    4. **监听状态变化**
       ```javascript
       const sdk = new AIBridgeSDK({
         // ...
         onStateChange: (state) => {
           if (state === ConnectionState.ERROR) {
             // 显示错误 UI
           }
         },
       });
       ```
    ```
  </action>
  <verify>检查 HTML 文件格式正确,示例代码语法正确</verify>
  <done>错误处理示例创建完成</done>
</task>

<task type="checkpoint:human-verify">
  <what-built>完整的错误处理、重连机制和恢复逻辑</what-built>
  <how-to-verify>
    1. 启动 Web 应用开发服务器: npm run dev
    2. 在浏览器中打开 sdk/examples/error-handling/index.html
    3. 验证以下场景:
       - 正常连接和消息发送
       - 发送空消息时的错误提示
       - 发送过长消息时的错误提示
       - 模拟断开连接后的状态变化
       - 点击"重新连接"按钮恢复连接
       - 查看诊断信息是否正确显示
    4. 检查控制台日志确认错误处理正确
  </how-to-verify>
  <resume-signal>所有错误场景正确处理,重连机制工作正常</resume-signal>
</task>

</tasks>

<verification>
1. 连接管理器支持指数退避重连
2. SDK 客户端提供详细错误类型和信息
3. Web 应用正确处理和转发错误
4. 错误处理示例完整可运行
</verification>

<success_criteria>
1. 网络断开后 SDK 自动尝试重连
2. 消息发送失败时返回明确的错误类型
3. onStateChange 回调正确反映连接状态
4. 错误示例演示所有错误场景
</success_criteria>

<output>
完成创建 `.planning/phases/07-sdk-integration/07-06-SUMMARY.md`
</output>
