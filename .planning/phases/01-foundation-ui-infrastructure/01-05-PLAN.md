---
wave: 5
depends_on: [01-01, 01-02, 01-03, 01-04]
files_modified:
  - package.json
  - src/components/MobileDrawer.tsx
  - src/components/Sidebar.tsx
  - src/layouts/MainLayout.tsx
  - src/components/TopNav.tsx
  - src/pages/SessionList.tsx
  - src/pages/SessionDetail.tsx
autonomous: true
gap_closure: false
---

# Plan 01-05: Responsive Layout Framework

<objective>
Implement master-detail layout with desktop sidebar, mobile drawer navigation with swipe gestures, and auto-hiding top navigation bar.
</objective>

<context>
@C:\WorkSpace\ai-bridge\.planning\phases\01-foundation-ui-infrastructure\01-CONTEXT.md
@C:\WorkSpace\ai-bridge\.planning\phases\01-foundation-ui-infrastructure\01-RESEARCH.md
@C:\WorkSpace\ai-bridge\.planning\phases\01-foundation-ui-infrastructure\01-04-PLAN.md
@C:\WorkSpace\ai-bridge\.planning\REQUIREMENTS.md
</context>

<tasks>
<task type="auto">
  <name>Install react-swipeable for gesture handling</name>
  <files>package.json, package-lock.json</files>
  <action>
    1. Navigate to web directory
    2. Install react-swipeable: npm install react-swipeable
    3. Verify installation completes
  </action>
  <verify>
    - package.json contains react-swipeable
    - npm ls react-swipeable shows version
  </verify>
  <done>react-swipeable installed</done>
</task>

<task type="auto">
  <name>Create MobileDrawer component with swipe gestures</name>
  <files>src/components/MobileDrawer.tsx</files>
  <action>
    1. Create src/components/MobileDrawer.tsx:
       - Accept isOpen, onClose props
       - Import useSwipeable from react-swipeable
       - Configure swipe handlers:
         * onSwipedLeft: Close drawer
         * onSwiping: Track swipe distance for visual feedback
         * trackMouse: true (desktop support)
         * swipeEdgeWidth: 20 (left edge trigger zone)
       - Create overlay with click to close
       - Drawer panel with:
         * Fixed position (inset-y-0 left-0)
         * Width 280px (mobile optimized)
         * Slide animation (translate-x transition)
         * z-index: 50
       - Import Sidebar component for content
       - Hide on desktop (md:hidden)
    2. Export component
  </action>
  <verify>
    - MobileDrawer.tsx exists
    - Uses useSwipeable hook
    - Has overlay and drawer panel
    - Hidden on desktop (md:hidden)
  </verify>
  <done>MobileDrawer with swipe gestures created</done>
</task>

<task type="auto">
  <name>Create useScrollDirection hook for auto-hiding nav</name>
  <files>src/hooks/useScrollDirection.ts</files>
  <action>
    1. Create src/hooks/useScrollDirection.ts:
       - Use useState for 'up' | 'down' direction
       - Use useState for lastScrollY
       - Use useEffect with scroll listener:
         * Compare currentScrollY with lastScrollY
         * Set direction based on comparison
         * Update lastScrollY
         * Use { passive: true } for performance
       - Clean up listener on unmount
       - Return scrollDirection
    2. Export hook
  </action>
  <verify>
    - useScrollDirection.ts exists
    - Returns 'up' | 'down' direction
    - Event listener properly cleaned up
  </verify>
  <done>Scroll direction hook created</done>
</task>

<task type="auto">
  <name>Update TopNav with mobile hamburger menu and auto-hide</name>
  <files>src/components/TopNav.tsx</files>
  <action>
    1. Edit src/components/TopNav.tsx:
       - Import useScrollDirection hook
       - Import Menu icon from lucide-react
       - Add state for drawer open/close
       - Add hamburger menu button (visible only on mobile: md:hidden)
       - On click: set drawer open
       - Add auto-hide behavior:
         * Track scroll direction
         * Apply transform: -translate-y-full when scrolling down
         * Apply transform: translate-y-0 when scrolling up
         * Only on mobile (max-md:)
       - Update structure:
         * Left: Hamburger menu (mobile) / Logo (desktop)
         * Center: Breadcrumb (desktop only: hidden md:flex)
         * Right: Connection status placeholder
    2. Pass onMenuClick prop to parent for drawer control
  </action>
  <verify>
    - TopNav has hamburger menu on mobile
    - Auto-hides on scroll down (mobile only)
    - Shows on scroll up
    - Breadcrumb hidden on mobile
  </verify>
  <done>TopNav updated with mobile features</done>
</task>

<task type="auto">
  <name>Update Sidebar for mobile drawer use</name>
  <files>src/components/Sidebar.tsx</files>
  <action>
    1. Edit src/components/Sidebar.tsx:
       - Make width responsive (w-80 on desktop, w-full in drawer)
       - Remove right border (handled by drawer)
       - Ensure full height
       - ThemeToggle at bottom remains
       - Add close button for mobile (md:hidden)
    2. Export updated component
  </action>
  <verify>
    - Sidebar works in both desktop and drawer contexts
    - Close button visible only on mobile
  </verify>
  <done>Sidebar updated for mobile compatibility</done>
</task>

<task type="auto">
  <name>Update MainLayout with responsive structure</name>
  <files>src/layouts/MainLayout.tsx</files>
  <action>
    1. Edit src/layouts/MainLayout.tsx:
       - Add state for mobile drawer open/close
       - Pass drawer state to TopNav
       - Structure layout:
         * Desktop: Flex row with fixed Sidebar (md:flex)
         * Mobile: Content only, drawer overlays
       - Main content area:
         * margin-left: 320px on desktop (md:ml-80)
         * margin-left: 0 on mobile
         * Full height (h-screen)
         * Overflow handling
       - Import and render MobileDrawer
    2. Ensure proper z-index layering
  </action>
  <verify>
    - MainLayout has drawer state
    - MobileDrawer component rendered
    - Desktop sidebar visible (md:block)
    - Mobile sidebar hidden by default
  </verify>
  <done>MainLayout updated with responsive structure</done>
</task>

<task type="auto">
  <name>Update SessionList for sidebar display</name>
  <files>src/pages/SessionList.tsx</files>
  <action>
    1. Edit src/pages/SessionList.tsx:
       - Add placeholder session list items
       - Show "No sessions yet" empty state
       - Add "New Session" button using Button component
       - Use full height container
       - Add padding and spacing
    2. Style to look like sidebar content
  </action>
  <verify>
    - SessionList displays empty state
    - "New Session" button is visible
    - Responsive padding
  </verify>
  <done>SessionList page updated</done>
</task>

<task type="auto">
  <name>Update SessionDetail for main content display</name>
  <files>src/pages/SessionDetail.tsx</files>
  <action>
    1. Edit src/pages/SessionDetail.tsx:
       - Display session ID from URL params
       - Add back button for mobile (visible only on mobile)
       - Use full height container
       - Add placeholder message area
       - Show "Session messages will appear here" in center
    2. Style for main content area
  </action>
  <verify>
    - SessionDetail displays session ID
    - Back button visible on mobile only
    - Content centered appropriately
  </verify>
  <done>SessionDetail page updated</done>
</task>

<task type="auto">
  <name>Add left edge swipe trigger for drawer</name>
  <files>src/layouts/MainLayout.tsx</files>
  <action>
    1. Edit src/layouts/MainLayout.tsx:
       - Add useSwipeable to main content area (mobile only)
       - Configure edge swipe detection:
         * Detect swipes from left edge (clientX < 30)
         * Trigger drawer open on right swipe
         * Only when drawer is closed
       - Apply swipe handlers to mobile wrapper
       - Prevent default browser back gesture on iOS
    2. Test edge swipe behavior
  </action>
  <verify>
    - Swiping from left edge opens drawer
    - Swipe only triggers in edge zone
    - Drawer doesn't open when already open
  </verify>
  <done>Left edge swipe trigger added</done>
</task>

<task type="auto">
  <name>Test responsive layout across all breakpoints</name>
  <files></files>
  <action>
    1. Start dev server: npm run dev
    2. Navigate to http://localhost:3000
    3. Test desktop layout (> 1024px):
       - Verify sidebar is fixed on left
       - Verify main content has margin-left: 320px
       - Verify theme toggle at bottom of sidebar
       - Verify breadcrumb visible
    4. Test mobile layout (< 768px):
       - Verify sidebar hidden
       - Verify hamburger menu visible
       - Click hamburger, verify drawer opens
       - Swipe from left edge, verify drawer opens
       - Swipe left on drawer, verify drawer closes
       - Scroll down, verify nav hides
       - Scroll up, verify nav shows
    5. Test tablet (768px - 1024px):
       - Verify sidebar fixed (like desktop)
       - Verify responsive spacing
    6. Stop server
  </action>
  <verify>
    - Desktop: Sidebar fixed, layout works
    - Mobile: Drawer opens/closes correctly
    - Mobile: Edge swipe triggers drawer
    - Mobile: Nav auto-hides on scroll
    - Tablet: Layout maintains structure
    - No horizontal scroll at any breakpoint
  </verify>
  <done>Responsive layout verified across breakpoints</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete responsive layout framework with master-detail view

Desktop (> 1024px):
- Fixed sidebar (320px) on left
- Main content area with margin-left offset
- TopNav visible with breadcrumb
- Theme toggle at bottom of sidebar

Mobile (< 768px):
- Sidebar hidden by default
- Hamburger menu in TopNav
- Drawer slides from left (280px width)
- Left edge swipe opens drawer
- Swipe left on drawer closes it
- TopNav auto-hides on scroll down
- TopNav appears on scroll up

Tablet (768px - 1024px):
- Desktop layout (fixed sidebar)

Components:
- MobileDrawer: Overlay drawer with swipe gestures
- TopNav: Auto-hiding on scroll (mobile only)
- Sidebar: Works in both desktop and drawer contexts
- MainLayout: Manages drawer state and responsive classes

Gestures:
- Left edge swipe (20-30px zone) → Open drawer
- Swipe left on drawer → Close drawer
- Hamburger menu click → Open drawer
- Overlay click → Close drawer</what-built>
  <how-to-verify>
    1. Navigate to web directory: cd C:\WorkSpace\ai-bridge\web
    2. Start dev server: npm run dev
    3. Visit http://localhost:3000

    Desktop testing (> 1024px):
    4. Verify: Sidebar is visible on left (320px wide)
    5. Verify: Main content is to the right of sidebar
    6. Verify: Breadcrumb shows in TopNav center
    7. Verify: Theme toggle at bottom of sidebar

    Mobile testing (resize to < 768px):
    8. Verify: Sidebar is hidden
    9. Verify: Hamburger menu (☰) visible in TopNav left
    10. Click hamburger menu
    11. Verify: Drawer slides in from left
    12. Click overlay or swipe left on drawer
    13. Verify: Drawer closes
    14. Touch left edge of screen and swipe right
    15. Verify: Drawer opens
    16. Scroll page down
    17. Verify: TopNav slides up (hides)
    18. Scroll page up
    19. Verify: TopNav slides down (appears)

    General checks:
    20. Try all screen sizes: 375px, 768px, 1024px, 1280px
    21. Verify: No horizontal scroll appears
    22. Verify: All elements remain visible/accessible
    23. Check browser console: No errors
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>
</tasks>

<verification>
After all tasks complete, verify:
- [ ] Desktop layout: Fixed sidebar (320px) on left
- [ ] Desktop layout: Main content with proper margin
- [ ] Mobile layout: Sidebar hidden by default
- [ ] Mobile layout: Hamburger menu visible
- [ ] Drawer: Opens from left (280px width)
- [ ] Drawer: Closes on overlay click
- [ ] Drawer: Closes on swipe left gesture
- [ ] Edge swipe: Left edge swipe opens drawer
- [ ] TopNav: Auto-hides on scroll down (mobile)
- [ ] TopNav: Shows on scroll up (mobile)
- [ ] Tablet: Uses desktop layout
- [ ] No horizontal scroll at any breakpoint
- [ ] All components render correctly in both themes
</verification>

<must_haves>
## From Phase Goal (Foundation & UI Infrastructure)

The following MUST be TRUE after this plan:

1. **Master-Detail Layout Works**
   - Desktop: Sidebar left, content right
   - Mobile: Content only, drawer overlays
   - Sidebar and content areas are clearly defined

2. **Mobile Drawer Navigation**
   - Drawer slides from left (280px width)
   - Opens via hamburger menu click
   - Opens via left edge swipe (20-30px trigger zone)
   - Closes via overlay click
   - Closes via swipe left gesture
   - Smooth animations (300ms)

3. **Auto-Hiding TopNav (Mobile)**
   - Hides when scrolling down
   - Shows when scrolling up
   - Uses transform translate for smooth animation
   - Only applies on mobile (< 768px)

4. **Responsive Design Across Breakpoints**
   - Mobile (< 640px): Drawer navigation
   - Tablet (640-1024px): Desktop layout
   - Desktop (> 1024px): Fixed sidebar
   - No horizontal scroll at any breakpoint
   - All content remains accessible

5. **Decision Compliance**
   - Sidebar fixed on desktop (per CONTEXT.md)
   - Drawer navigation on mobile (per CONTEXT.md)
   - Left edge swipe gesture supported (per CONTEXT.md)
   - TopNav auto-hides on scroll (per CONTEXT.md)
   - Theme toggle in sidebar footer (per 01-04 decision)

6. **Theme System Integration**
   - All layouts work in light and dark themes
   - Drawer respects theme colors
   - Transitions remain smooth with theme changes
</must_haves>

<success_criteria>
- [ ] All tasks executed successfully
- [ ] Desktop layout functions correctly
- [ ] Mobile drawer works with all gestures
- [ ] TopNav auto-hides on scroll (mobile)
- [ ] No layout breaks at any breakpoint
- [ ] User verified responsive layout
- [ ] Phase 01 complete
</success_criteria>

<phase_completion>
This is the final plan of Phase 01. Upon completion:

1. All Phase 01 success criteria should be met:
   - [ ] Application renders on mobile, tablet, and desktop
   - [ ] User can toggle dark/light themes
   - [ ] All navigation works without page reload
   - [ ] Base components render correctly

2. Update .planning/ROADMAP.md to mark Phase 01 as complete

3. Generate 01-SUMMARY.md with phase accomplishments
</phase_completion>
