---
wave: 3
depends_on: [01-01, 01-02]
files_modified:
  - package.json
  - tailwind.config.js
  - src/index.css
  - components.json
  - src/components/ui/button.tsx
  - src/components/ui/input.tsx
  - src/components/ui/card.tsx
  - src/components/ui/badge.tsx
  - src/components/ui/dialog.tsx
  - src/lib/utils.ts
  - src/App.tsx (test content)
autonomous: true
gap_closure: false
---

# Plan 01-03: shadcn/ui Integration

<objective>
Install and configure Tailwind CSS v4 and shadcn/ui component library, add base components, and verify rendering across all breakpoints.
</objective>

<context>
@C:\WorkSpace\ai-bridge\.planning\phases\01-foundation-ui-infrastructure\01-CONTEXT.md
@C:\WorkSpace\ai-bridge\.planning\phases\01-foundation-ui-infrastructure\01-RESEARCH.md
@C:\WorkSpace\ai-bridge\.planning\phases\01-foundation-ui-infrastructure\01-01-PLAN.md
@C:\WorkSpace\ai-bridge\.planning\REQUIREMENTS.md
</context>

<tasks>
<task type="auto">
  <name>Install Tailwind CSS v4</name>
  <files>package.json, package-lock.json</files>
  <action>
    1. Navigate to web directory
    2. Install Tailwind CSS v4: npm install -D tailwindcss@latest @tailwindcss/vite@latest
    3. Verify installation completes
  </action>
  <verify>
    - package.json contains tailwindcss and @tailwindcss/vite
    - npm ls tailwindcss shows v4.x
  </verify>
  <done>Tailwind CSS v4 installed</done>
</task>

<task type="auto">
  <name>Configure Vite to use Tailwind plugin</name>
  <files>vite.config.ts</files>
  <action>
    1. Edit vite.config.ts:
       - Import tailwindcss from @tailwindcss/vite
       - Add tailwindcss() to plugins array
    2. Ensure plugin is loaded before react()
  </action>
  <verify>
    - vite.config.ts contains tailwindcss plugin
    - No configuration errors
  </verify>
  <done>Vite configured with Tailwind plugin</done>
</task>

<task type="auto">
  <name>Configure Tailwind CSS variables and content paths</name>
  <files>tailwind.config.js</files>
  <action>
    1. Create tailwind.config.js in web directory
    2. Configure:
       - darkMode: ['class']
       - content: ["./index.html", "./src/**/*.{js,ts,jsx,tsx}"]
       - theme.extend with shadcn/ui color variables
       - borderRadius with CSS variable
    3. Add all shadcn/ui color tokens (background, foreground, primary, secondary, etc.)
  </action>
  <verify>
    - tailwind.config.js exists
    - Contains darkMode configuration
    - Contains all color variables
  </verify>
  <done>Tailwind configuration created</done>
</task>

<task type="auto">
  <name>Import Tailwind styles and configure base CSS</name>
  <files>src/index.css</files>
  <action>
    1. Edit src/index.css:
       - Add: @import "tailwindcss";
       - Add CSS variables for light/dark themes
       - Add base layer styles
       - Configure smooth transitions
    2. Include all shadcn/ui CSS variables:
       - --background, --foreground
       - --primary, --primary-foreground
       - --secondary, --secondary-foreground
       - --muted, --muted-foreground
       - --accent, --accent-foreground
       --border, --input, --ring
  </action>
  <verify>
    - index.css imports tailwindcss
    - All CSS variables defined
    - Transition effects configured
  </verify>
  <done>Base CSS configured with Tailwind and CSS variables</done>
</task>

<task type="auto">
  <name>Initialize shadcn/ui</name>
  <files>components.json, src/lib/utils.ts</files>
  <action>
    1. Run: npx shadcn@latest init
    2. Use interactive prompts with these answers:
       - Style: "new-york"
       - Base color: "neutral"
       - CSS variables: "yes"
       - Tailwind config: "tailwind.config.js"
       - CSS path: "src/index.css"
       - TypeScript: "yes"
       - Aliases: "@/components", "@/lib", "@/utils"
    3. Verify components.json and src/lib/utils.ts are created
  </action>
  <verify>
    - components.json exists in web directory
    - src/lib/utils.ts exists with cn() function
    - TypeScript compilation succeeds
  </verify>
  <done>shadcn/ui initialized</done>
</task>

<task type="auto">
  <name>Add base UI components from shadcn/ui</name>
  <files>src/components/ui/button.tsx, src/components/ui/input.tsx, src/components/ui/card.tsx, src/components/ui/badge.tsx, src/components/ui/dialog.tsx</files>
  <action>
    1. Add Button component: npx shadcn@latest add button
    2. Add Input component: npx shadcn@latest add input
    3. Add Card component: npx shadcn@latest add card
    4. Add Badge component: npx shadcn@latest add badge
    5. Add Dialog component: npx shadcn@latest add dialog
    6. Verify all components are in src/components/ui/
  </action>
  <verify>
    - src/components/ui/button.tsx exists
    - src/components/ui/input.tsx exists
    - src/components/ui/card.tsx exists
    - src/components/ui/badge.tsx exists
    - src/components/ui/dialog.tsx exists
    - All components compile without errors
  </verify>
  <done>Base UI components added</done>
</task>

<task type="auto">
  <name>Create component test page</name>
  <files>src/pages/ComponentTest.tsx</files>
  <action>
    1. Create src/pages/ComponentTest.tsx:
       - Import Button, Input, Card, Badge, Dialog components
       - Create visual test showing all component variants
       - Include:
         * Button: default, secondary, destructive, outline, ghost, link
         * Input: default, with placeholder
         * Card: with title, description, content, footer
         * Badge: default, secondary, destructive, outline
         * Dialog: with trigger and content
       - Use responsive grid layout
    2. Export component
  </action>
  <verify>
    - ComponentTest.tsx exists
    - All component variants are represented
    - Responsive grid configured
  </verify>
  <done>Component test page created</done>
</task>

<task type="auto">
  <name>Add test route for component verification</name>
  <files>src/router/index.tsx</files>
  <action>
    1. Edit src/router/index.tsx:
       - Import ComponentTest page
       - Add /components route for testing
       - Keep existing routes (/ and /sessions/:id)
    2. Ensure route is accessible
  </action>
  <verify>
    - Router includes /components route
    - TypeScript compilation succeeds
  </verify>
  <done>Test route added to router</done>
</task>

<task type="auto">
  <name>Verify component rendering</name>
  <files></files>
  <action>
    1. Start dev server: npm run dev
    2. Navigate to http://localhost:3000/components
    3. Verify all components render:
       - Buttons in all variants
       - Input field with placeholder
       - Card with title, description, footer
       - Badges in different styles
       - Dialog can be opened
    4. Check responsive behavior (resize browser window)
    5. Stop server
  </action>
  <verify>
    - All components render without errors
    - Components display correctly
    - Responsive layout works
    - No console errors
  </verify>
  <done>Component rendering verified</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Tailwind CSS v4 + shadcn/ui integration

Installed:
- Tailwind CSS v4.1.x with @tailwindcss/vite plugin
- shadcn/ui with "new-york" style, neutral base color
- CSS variables for theming (light/dark mode support)

Components added:
- Button (6 variants: default, secondary, destructive, outline, ghost, link)
- Input (with placeholder support)
- Card (with title, description, content, footer)
- Badge (4 variants: default, secondary, destructive, outline)
- Dialog (modal with overlay)

Configuration:
- tailwind.config.js with darkMode: ['class']
- src/index.css with @import "tailwindcss" and CSS variables
- src/lib/utils.ts with cn() className utility
- components.json for shadcn CLI

Test page: http://localhost:3000/components</what-built>
  <how-to-verify>
    1. Navigate to web directory: cd C:\WorkSpace\ai-bridge\web
    2. Start dev server: npm run dev
    3. Visit http://localhost:3000/components
    4. Verify all button variants are displayed and styled
    5. Click "Open Dialog" button, confirm dialog opens with overlay
    6. Close dialog, verify it closes smoothly
    7. Test responsive behavior:
       - Resize browser to mobile width (< 640px)
       - Confirm components stack vertically
       - Resize to desktop (> 1024px)
       - Confirm grid layout shows multiple columns
    8. Check browser DevTools Console: No errors
    9. Check computed styles: CSS variables are applied
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>
</tasks>

<verification>
After all tasks complete, verify:
- [ ] Tailwind CSS v4 is installed and configured
- [ ] Vite plugin loads Tailwind correctly
- [ ] shadcn/ui is initialized with correct configuration
- [ ] All base components (Button, Input, Card, Badge, Dialog) are installed
- [ ] Components render correctly in all variants
- [ ] Responsive layout works on mobile, tablet, and desktop
- [ ] CSS variables are defined for theming
- [ ] No console errors on component test page
</verification>

<must_haves>
## From Phase Goal (Foundation & UI Infrastructure)

The following MUST be TRUE after this plan:

1. **Tailwind CSS Configured**
   - v4.1.x with @tailwindcss/vite plugin
   - PostCSS processing works
   - Responsive breakpoints (sm, md, lg, xl) available
   - darkMode: ['class'] for theme switching

2. **shadcn/ui Integrated**
   - components.json configured
   - CLI (npx shadcn) works for adding components
   - Path aliases (@/components, @/lib) resolve

3. **Base Components Available**
   - Button: All variants render
   - Input: Text input with placeholder
   - Card: Container with header/content/footer
   - Badge: Status indicators
   - Dialog: Modal overlay component

4. **Responsive Design Works**
   - Components render on mobile (< 640px)
   - Components render on tablet (640px - 1024px)
   - Components render on desktop (> 1024px)
   - No horizontal scroll at any breakpoint

5. **Theming Foundation**
   - CSS variables defined for light/dark modes
   - Colors use HSL format for easy theming
   - Transition effects configured (300ms)
</must_haves>

<success_criteria>
- [ ] All tasks executed successfully
- [ ] Tailwind CSS processes styles without errors
- [ ] shadcn/ui components render correctly
- [ ] Responsive layout verified on all breakpoints
- [ ] User verified component rendering
- [ ] No blocking issues remaining
</success_criteria>
