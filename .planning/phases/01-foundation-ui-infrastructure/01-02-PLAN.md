---
wave: 2
depends_on: [01-01]
files_modified:
  - package.json
  - src/router/index.tsx
  - src/layouts/RootLayout.tsx
  - src/layouts/MainLayout.tsx
  - src/pages/SessionList.tsx
  - src/pages/SessionDetail.tsx
  - src/components/Breadcrumb.tsx
  - src/App.tsx
autonomous: true
gap_closure: false
---

# Plan 01-02: React Router Setup

<objective>
Configure React Router 7 with declarative routing, create layout components, and establish navigation structure for session list and detail views.
</objective>

<context>
@C:\WorkSpace\ai-bridge\.planning\phases\01-foundation-ui-infrastructure\01-CONTEXT.md
@C:\WorkSpace\ai-bridge\.planning\phases\01-foundation-ui-infrastructure\01-RESEARCH.md
@C:\WorkSpace\ai-bridge\.planning\phases\01-foundation-ui-infrastructure\01-01-PLAN.md
@C:\WorkSpace\ai-bridge\.planning\REQUIREMENTS.md
</context>

<tasks>
<task type="auto">
  <name>Install React Router 7</name>
  <files>package.json, package-lock.json</files>
  <action>
    1. Navigate to web directory
    2. Install React Router: npm install react-router@7.9
    3. Verify installation completes successfully
  </action>
  <verify>
    - package.json contains react-router@7.9.x
    - npm ls react-router shows correct version
  </verify>
  <done>React Router 7 installed</done>
</task>

<task type="auto">
  <name>Create router configuration with routes</name>
  <files>src/router/index.tsx</files>
  <action>
    1. Create src/router directory
    2. Create src/router/index.tsx with route configuration:
       - Import createBrowserRouter from react-router
       - Define routes array:
         * Root path (/) → SessionList page
         * /sessions/:id → SessionDetail page
       - Export router instance
    3. Use declarative configuration (not file-system routes)
  </action>
  <verify>
    - src/router/index.tsx exists
    - Exports configured router
    - TypeScript compilation succeeds
  </verify>
  <done>Router configuration created</done>
</task>

<task type="auto">
  <name>Create page components</name>
  <files>src/pages/SessionList.tsx, src/pages/SessionDetail.tsx</files>
  <action>
    1. Create src/pages directory
    2. Create src/pages/SessionList.tsx:
       - Placeholder component with heading "Session List"
       - Empty state message (no sessions yet)
       - Full-height container for layout
    3. Create src/pages/SessionDetail.tsx:
       - Placeholder component with heading "Session Detail"
       - Display session ID from URL params
       - Full-height container for layout
  </action>
  <verify>
    - Both page components exist
    - TypeScript compilation succeeds
    - Components render without errors
  </verify>
  <done>Page components created</done>
</task>

<task type="auto">
  <name>Create RootLayout component</name>
  <files>src/layouts/RootLayout.tsx</files>
  <action>
    1. Create src/layouts directory
    2. Create src/layouts/RootLayout.tsx:
       - Full-height container (h-screen)
       - Outlet for child routes
       - Basic styling with max-width and centering
       - Background color from theme variables
    3. Export as default
  </action>
  <verify>
    - RootLayout.tsx exists
    - Uses Outlet from react-router
    - Full-height container configured
  </verify>
  <done>RootLayout component created</done>
</task>

<task type="auto">
  <name>Create MainLayout component with TopNav</name>
  <files>src/layouts/MainLayout.tsx, src/components/TopNav.tsx</files>
  <action>
    1. Create src/layouts/MainLayout.tsx:
       - Flex container (flex-col, h-screen)
       - TopNav component at top
       - Main content area with Outlet
       - Proper spacing and overflow handling
    2. Create src/components/TopNav.tsx:
       - Fixed header with shadow
       - Logo text on left
       - Placeholder for breadcrumb (center)
       - Placeholder for connection status (right)
       - Responsive padding
  </action>
  <verify>
    - MainLayout.tsx exists with TopNav import
    - TopNav component created
    - Layout structure uses flex-col
  </verify>
  <done>MainLayout and TopNav created</done>
</task>

<task type="auto">
  <name>Create Breadcrumb component</name>
  <files>src/components/Breadcrumb.tsx</files>
  <action>
    1. Create src/components/Breadcrumb.tsx:
       - Accept current location as prop
       - Display navigation hierarchy
       - Show "Sessions" for root path
       - Show "Sessions > Detail" for session detail pages
       - Use separators between levels
       - Hidden on mobile (md:hidden for breadcrumb)
    2. Export as component
  </action>
  <verify>
    - Breadcrumb.tsx exists
    - TypeScript types defined
    - Component renders without errors
  </verify>
  <done>Breadcrumb component created</done>
</task>

<task type="auto">
  <name>Update router with layout nesting</name>
  <files>src/router/index.tsx</files>
  <action>
    1. Edit src/router/index.tsx:
       - Import RootLayout and MainLayout
       - Nest routes: RootLayout > MainLayout > pages
       - Configure path structure:
         * / → MainLayout → SessionList
         * /sessions/:id → MainLayout → SessionDetail
    2. Ensure proper route element hierarchy
  </action>
  <verify>
    - Router uses nested layouts
    - Path hierarchy matches requirements
    - TypeScript compilation succeeds
  </verify>
  <done>Router configured with nested layouts</done>
</task>

<task type="auto">
  <name>Update App.tsx to use RouterProvider</name>
  <files>src/App.tsx, src/main.tsx</files>
  <action>
    1. Edit src/App.tsx:
       - Import RouterProvider from react-router
       - Import router from @/router
       - Render RouterProvider with router prop
       - Remove any previous content
    2. Ensure main.tsx imports and renders App
  </action>
  <verify>
    - App.tsx uses RouterProvider
    - No TypeScript errors
    - Build completes successfully
  </verify>
  <done>App configured with RouterProvider</done>
</task>

<task type="auto">
  <name>Test routing functionality</name>
  <files></files>
  <action>
    1. Start dev server: npm run dev
    2. Navigate to http://localhost:3000
    3. Verify SessionList page renders
    4. Navigate to http://localhost:3000/sessions/123
    5. Verify SessionDetail page renders with "123" displayed
    6. Confirm no page reload (client-side routing works)
    7. Stop server
  </action>
  <verify>
    - Both routes render correctly
    - Navigation is client-side (no full page reload)
    - Session ID displays in detail page
  </verify>
  <done>Routing functionality verified</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete React Router 7 setup with nested layouts

Routing structure:
- / (root) → Session List page
- /sessions/:id → Session Detail page

Layouts:
- RootLayout: Full-height container
- MainLayout: TopNav + content area with Outlet
- Breadcrumb: Navigation hierarchy (desktop only)

Components created:
- TopNav: Fixed header with logo, breadcrumb placeholder, status placeholder
- Breadcrumb: Shows navigation path (hidden on mobile)
- SessionList: Placeholder page for session list
- SessionDetail: Placeholder page showing session ID</what-built>
  <how-to-verify>
    1. Navigate to web directory: cd C:\WorkSpace\ai-bridge\web
    2. Start dev server: npm run dev
    3. Visit http://localhost:3000
    4. Confirm: "Session List" heading is displayed
    5. Check: TopNav is visible at top of page
    6. Change URL to http://localhost:3000/sessions/test-123
    7. Confirm: Page changes without reload (client-side routing)
    8. Confirm: "Session Detail" heading with "test-123" ID is shown
    9. Check browser console: No errors
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>
</tasks>

<verification>
After all tasks complete, verify:
- [ ] React Router 7 is installed
- [ ] Router configuration uses declarative approach
- [ ] Client-side navigation works (no page reloads)
- [ ] Both routes (/ and /sessions/:id) render correctly
- [ ] Layouts are properly nested
- [ ] TopNav is visible on all pages
- [ ] Breadcrumb shows navigation hierarchy
- [ ] Session ID displays in detail page
</verification>

<must_haves>
## From Phase Goal (Foundation & UI Infrastructure)

The following MUST be TRUE after this plan:

1. **Client-Side Routing Works**
   - Navigation between pages does NOT reload the browser
   - URL updates correctly
   - Back/forward browser buttons work

2. **Route Structure Matches Requirements**
   - Root path (/) shows session list
   - /sessions/:id shows session detail
   - All routes render without errors

3. **Layout Structure**
   - RootLayout provides full-height container
   - MainLayout includes TopNav and content area
   - Outlet renders child routes correctly

4. **Navigation Components**
   - TopNav displays on all pages
   - Breadcrumb shows navigation path
   - Components are responsive (breadcrumb hidden on mobile)

5. **Session ID Handling**
   - Detail page extracts and displays session ID from URL
   - ID parameter is properly typed in TypeScript
</must_haves>

<success_criteria>
- [ ] All tasks executed successfully
- [ ] Client-side routing works without page reloads
- [ ] Both routes render correctly
- [ ] Layout nesting functions properly
- [ ] User verified routing functionality
- [ ] No blocking issues remaining
</success_criteria>
