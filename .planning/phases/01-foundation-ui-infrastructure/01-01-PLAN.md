---
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - tsconfig.node.json
  - vite.config.ts
  - .eslintrc.cjs
  - .prettierrc
  - src/main.tsx
  - src/App.tsx
  - src/vite-env.d.ts
autonomous: true
gap_closure: false
---

# Plan 01-01: Project Scaffolding

<objective>
Create Vite + React 19 + TypeScript project with proper build configuration, path aliases, and development tooling.
</objective>

<context>
@C:\WorkSpace\ai-bridge\.planning\phases\01-foundation-ui-infrastructure\01-CONTEXT.md
@C:\WorkSpace\ai-bridge\.planning\phases\01-foundation-ui-infrastructure\01-RESEARCH.md
@C:\WorkSpace\ai-bridge\.planning\REQUIREMENTS.md
@C:\WorkSpace\ai-bridge\planning\PROJECT.md
</context>

<tasks>
<task type="auto">
  <name>Create Vite project with React TypeScript template</name>
  <files>package.json, tsconfig.json, tsconfig.node.json, vite.config.ts, index.html, src/main.tsx, src/App.tsx, src/vite-env.d.ts</files>
  <action>
    1. Create web directory at project root: ai-bridge/web/
    2. Run: npm create vite@latest web -- --template react-ts
    3. Accept all default prompts
    4. Navigate into web directory and install dependencies: npm install
  </action>
  <verify>
    - web/package.json exists with react and vite dependencies
    - web/src/App.tsx exists with basic React component
    - npm run dev starts without errors
  </verify>
  <done>Vite project created with React TypeScript template</done>
</task>

<task type="auto">
  <name>Upgrade to React 19.2 and configure dependencies</name>
  <files>package.json, package-lock.json</files>
  <action>
    1. Install React 19.2: npm install react@19.2 react-dom@19.2
    2. Install latest Vite React plugin: npm install -D @vitejs/plugin-react@latest
    3. Update TypeScript to latest: npm install -D typescript@latest
    4. Verify all peer dependencies are compatible
  </action>
  <verify>
    - package.json shows react@19.2.x and react-dom@19.2.x
    - npm run build succeeds with no dependency warnings
  </verify>
  <done>React 19.2 and dependencies configured</done>
</task>

<task type="auto">
  <name>Configure TypeScript path aliases</name>
  <files>tsconfig.json, tsconfig.app.json, vite.config.ts</files>
  <action>
    1. Edit tsconfig.json - add baseUrl and paths under compilerOptions:
       "baseUrl": ".",
       "paths": {
         "@/*": ["./src/*"]
       }
    2. Create tsconfig.app.json if not exists, extend base tsconfig
    3. Edit vite.config.ts - add resolve.alias:
       import path from 'path'
       export default defineConfig({
         resolve: {
           alias: {
             '@': path.resolve(__dirname, './src')
           }
         }
       })
  </action>
  <verify>
    - Can import from '@/App' in main.tsx without TypeScript errors
    - tsconfig.json contains paths configuration
  </verify>
  <done>TypeScript path aliases configured</done>
</task>

<task type="auto">
  <name>Configure Vite dev server and proxy</name>
  <files>vite.config.ts</files>
  <action>
    1. Edit vite.config.ts - set server port to 3000
    2. Add proxy configuration for Phase 2 preparation:
       server: {
         port: 3000,
         proxy: {
           '/api': {
             target: 'http://localhost:8080',
             changeOrigin: true
           },
           '/socket.io': {
             target: 'http://localhost:8080',
             changeOrigin: true,
             ws: true
           }
         }
       }
    3. Add strictPort: true to avoid port conflicts
  </action>
  <verify>
    - vite.config.ts contains server configuration
    - npm run dev starts on port 3000
  </verify>
  <done>Vite server configured with port and proxy</done>
</task>

<task type="auto">
  <name>Configure ESLint and Prettier</name>
  <files>.eslintrc.cjs, .prettierrc, .prettierignore</files>
  <action>
    1. Create .prettierrc with sensible defaults:
       {
         "semi": true,
         "singleQuote": false,
         "tabWidth": 2,
         "trailingComma": "es5",
         "printWidth": 100
       }
    2. Create .prettierignore:
       node_modules
       dist
       build
       coverage
    3. Update .eslintrc.cjs - ensure React and TypeScript rules are enabled
    4. Add format script to package.json: "format": "prettier --write \"src/**/*.{ts,tsx,css}\""
  </action>
  <verify>
    - .prettierrc exists with configuration
    - npm run format runs without errors
  </verify>
  <done>Code formatting tools configured</done>
</task>

<task type="auto">
  <name>Update App.tsx to use path alias</name>
  <files>src/App.tsx, src/main.tsx</files>
  <action>
    1. Edit src/App.tsx - replace content with clean component using @ alias if applicable
    2. Ensure file uses proper TypeScript types
    3. Add basic CSS reset import if not present
  </action>
  <verify>
    - npm run build succeeds
    - No TypeScript errors
  </verify>
  <done>App component updated with path alias support</done>
</task>

<task type="auto">
  <name>Create project README with setup instructions</name>
  <files>web/README.md</files>
  <action>
    1. Create web/README.md with:
       - Project description
       - Prerequisites (Node.js version)
       - Installation steps
       - Development server instructions
       - Build commands
    2. Document the proxy configuration for future reference
  </action>
  <verify>
    - README.md exists in web directory
    - Contains clear setup instructions
  </verify>
  <done>Project documentation created</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Vite + React 19 + TypeScript project scaffolding

Project location: C:\WorkSpace\ai-bridge\web/

Configuration completed:
- React 19.2 with TypeScript
- Path aliases (@/ mapping to src/)
- Vite dev server on port 3000
- API proxy configured for localhost:8080
- ESLint and Prettier formatting
- Project README with setup instructions</what-built>
  <how-to-verify>
    1. Open terminal at C:\WorkSpace\ai-bridge\web
    2. Run: npm run dev
    3. Verify server starts on http://localhost:3000
    4. Open browser to http://localhost:3000
    5. Confirm: "Vite + React" page loads without errors
    6. Check browser console: No errors or warnings
    7. Stop server with Ctrl+C
    8. Run: npm run build
    9. Confirm: Build completes successfully
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>
</tasks>

<verification>
After all tasks complete, verify:
- [ ] npm run dev starts server on port 3000
- [ ] npm run build completes without errors
- [ ] TypeScript compilation succeeds (no type errors)
- [ ] Path alias @/ works in imports
- [ ] ESLint and Prettier are configured
- [ ] Browser renders default Vite React page
</verification>

<must_haves>
## From Phase Goal (Foundation & UI Infrastructure)

The following MUST be TRUE after this plan:

1. **Build System Works**
   - `npm run dev` starts development server
   - `npm run build` creates production bundle
   - Hot module replacement (HMR) functions

2. **TypeScript Configuration**
   - Path aliases (`@/*`) resolve correctly
   - No type errors in default app
   - Strict mode enabled

3. **Development Tools**
   - ESLint configured for React + TypeScript
   - Prettier configured with project style
   - Format script available

4. **Proxy Ready for Backend**
   - Vite proxy configured for `/api` → `http://localhost:8080`
   - Socket.IO proxy configured for WebSocket connections

5. **Project Documentation**
   - README with setup instructions
   - Clear next steps for developers
</must_haves>

<success_criteria>
- [ ] All tasks executed successfully
- [ ] Development server runs without errors
- [ ] Production build completes
- [ ] TypeScript path aliases work
- [ ] User verified the running application
- [ ] No blocking issues remaining
</success_criteria>
