---
wave: 4
depends_on: [01-01, 01-02, 01-03]
files_modified:
  - src/providers/ThemeProvider.tsx
  - src/hooks/useTheme.ts
  - src/components/ThemeToggle.tsx
  - src/App.tsx
  - index.html
  - src/layouts/MainLayout.tsx
  - src/components/Sidebar.tsx
autonomous: true
gap_closure: false
---

# Plan 01-04: Theme System

<objective>
Implement dark/light mode theme switching with system preference detection, localStorage persistence, and smooth transitions without FOUC (Flash of Unstyled Content).
</objective>

<context>
@C:\WorkSpace\ai-bridge\.planning\phases\01-foundation-ui-infrastructure\01-CONTEXT.md
@C:\WorkSpace\ai-bridge\.planning\phases\01-foundation-ui-infrastructure\01-RESEARCH.md
@C:\WorkSpace\ai-bridge\.planning\phases\01-foundation-ui-infrastructure\01-03-PLAN.md
@C:\WorkSpace\ai-bridge\.planning\REQUIREMENTS.md
</context>

<tasks>
<task type="auto">
  <name>Create useTheme hook</name>
  <files>src/hooks/useTheme.ts</files>
  <action>
    1. Create src/hooks directory
    2. Create src/hooks/useTheme.ts:
       - Define Theme type: 'light' | 'dark' | 'system'
       - Create getSystemTheme() helper using matchMedia
       - Implement useState for theme with 'system' default
       - Implement useEffect to:
         * Read from localStorage on mount
         * Apply theme class to document.documentElement
         * Listen for system theme changes (prefers-color-scheme)
         * Clean up listener on unmount
       - Return [theme, setTheme] tuple
    3. Export hook
  </action>
  <verify>
    - useTheme.ts exists with TypeScript types
    - Hook returns theme and setTheme function
    - System preference detection implemented
  </verify>
  <done>useTheme hook created</done>
</task>

<task type="auto">
  <name>Create ThemeProvider component</name>
  <files>src/providers/ThemeProvider.tsx</files>
  <action>
    1. Create src/providers directory
    2. Create src/providers/ThemeProvider.tsx:
       - Accept children prop
       - Use useTheme hook internally
       - Provide theme context to children
       - Create ThemeContext with proper TypeScript types
       - Export ThemeProvider and useTheme hook (context version)
    3. Ensure proper re-render on theme change
  </action>
  <verify>
    - ThemeProvider.tsx exists
    - Exports ThemeProvider and useTheme
    - Context types are properly defined
  </verify>
  <done>ThemeProvider component created</done>
</task>

<task type="auto">
  <name>Add anti-FOUC script to index.html</name>
  <files>index.html</files>
  <action>
    1. Edit web/index.html:
       - Add inline script in <head> BEFORE any other scripts
       - Script should:
         * Read theme from localStorage (default: 'system')
         * Detect system preference if theme is 'system'
         * Apply 'dark' class to document.documentElement if needed
         * Execute immediately to prevent flash
       - Use IIFE pattern for immediate execution
    2. Ensure script runs before React loads
  </action>
  <verify>
    - index.html contains anti-FOUC script in <head>
    - Script reads localStorage
    - Script applies dark class before page renders
  </verify>
  <done>Anti-FOUC script added to prevent flash</done>
</task>

<task type="auto">
  <name>Create ThemeToggle component</name>
  <files>src/components/ThemeToggle.tsx</files>
  <action>
    1. Create src/components/ThemeToggle.tsx:
       - Use useTheme hook from context
       - Display current theme with icon:
         * Sun icon for 'light'
         * Moon icon for 'dark'
         * Auto (circle-half) icon for 'system'
       - On click, cycle: light → dark → system → light
       - Use Button component from shadcn/ui
       - Add tooltip showing current theme
       - Small size, ghost variant
    2. Export component
  </action>
  <verify>
    - ThemeToggle.tsx exists
    - Uses lucide-react icons (sun, moon, monitor)
    - Cycles through three states
  </verify>
  <done>ThemeToggle component created</done>
</task>

<task type="auto">
  <name>Create Sidebar component with theme toggle</name>
  <files>src/components/Sidebar.tsx</files>
  <action>
    1. Create src/components/Sidebar.tsx:
       - Full-height container
       - Header with app name/logo
       - Placeholder content for session list
       - Footer with ThemeToggle component
       - Border on right side
       - Background and text colors from theme variables
       - Fixed width (w-80, 320px)
    2. Export component
  </action>
  <verify>
    - Sidebar.tsx exists
    - ThemeToggle is positioned at bottom
    - Responsive to theme changes
  </verify>
  <done>Sidebar component created with theme toggle</done>
</task>

<task type="auto">
  <name>Integrate ThemeProvider in App</name>
  <files>src/App.tsx</files>
  <action>
    1. Edit src/App.tsx:
       - Import ThemeProvider
       - Wrap RouterProvider with ThemeProvider
       - Ensure ThemeProvider is outermost wrapper
    2. Verify component hierarchy:
       ThemeProvider → RouterProvider → Routes
  </action>
  <verify>
    - App.tsx uses ThemeProvider
    - ThemeProvider wraps entire app
    - No TypeScript errors
  </verify>
  <done>ThemeProvider integrated in App</done>
</task>

<task type="auto">
  <name>Add Sidebar to MainLayout</name>
  <files>src/layouts/MainLayout.tsx</files>
  <action>
    1. Edit src/layouts/MainLayout.tsx:
       - Import Sidebar component
       - Add Sidebar to left side of layout
       - Use flex layout: Sidebar (fixed) + Main content (flex-1)
       - Add responsive classes:
         * Sidebar hidden on mobile (hidden md:flex)
         * Main content takes full width
    2. Ensure proper overflow handling
  </action>
  <verify>
    - MainLayout includes Sidebar
    - Sidebar is visible on desktop (md+)
    - Sidebar is hidden on mobile
  </verify>
  <done>Sidebar added to MainLayout</done>
</task>

<task type="auto">
  <name>Configure theme transition effects</name>
  <files>src/index.css</files>
  <action>
    1. Edit src/index.css:
       - Add transition to html element:
         * transition: background-color 300ms ease, color 300ms ease
       - Ensure transitions don't apply on initial load
       - Add CSS class for disabling transitions during theme switch
    2. Configure smooth color transitions
  </action>
  <verify>
    - index.css has transition styles
    - Transitions are 300ms duration
    - No jarring color changes
  </verify>
  <done>Theme transition effects configured</done>
</task>

<task type="auto">
  <name>Test theme switching functionality</name>
  <files></files>
  <action>
    1. Start dev server: npm run dev
    2. Navigate to http://localhost:3000
    3. Test theme toggle:
       - Click theme toggle button
       - Verify theme cycles: light → dark → system
       - Check page background changes
       - Check text color updates
    4. Test system preference:
       - Set theme to 'system'
       - Change OS theme preference
       - Verify app follows system theme
    5. Test persistence:
       - Set theme to 'dark'
       - Refresh page
       - Verify dark theme persists
    6. Stop server
  </action>
  <verify>
    - Theme toggle cycles correctly
    - Colors transition smoothly (300ms)
    - System preference is detected
    - Theme persists across page reloads
    - No FOUC on initial load
  </verify>
  <done>Theme switching verified</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete theme system with dark/light/system modes

Components created:
- useTheme hook: Manages theme state with localStorage persistence
- ThemeProvider: Context provider for app-wide theme access
- ThemeToggle: Button component in Sidebar footer, cycles through themes
- Sidebar: Desktop sidebar with theme toggle at bottom

Features:
- Three theme modes: light, dark, system (follows OS preference)
- Smooth transitions: 300ms color transitions
- Anti-FOUC script: Prevents flash of unstyled content on load
- localStorage persistence: Theme preference saved across sessions
- System preference detection: Real-time OS theme change detection

Theme toggle location: Sidebar footer (desktop only in this phase, moves to mobile drawer in 01-05)</what-built>
  <how-to-verify>
    1. Navigate to web directory: cd C:\WorkSpace\ai-bridge\web
    2. Start dev server: npm run dev
    3. Visit http://localhost:3000
    4. Find theme toggle button at bottom of sidebar (left side on desktop)
    5. Click button repeatedly and verify:
       - Icon cycles: Sun → Moon → Auto icon
       - Page background changes smoothly
       - Text color updates to maintain contrast
    6. Test system theme:
       - Click until "Auto" icon shows
       - Change your OS theme (light/dark mode)
       - Verify app theme follows OS theme
    7. Test persistence:
       - Set to dark theme
       - Refresh browser (F5)
       - Verify dark theme loads immediately (no flash)
    8. Test on mobile:
       - Resize browser to < 768px
       - Note: Sidebar hidden (drawer comes in 01-05)
    9. Check browser console: No errors
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>
</tasks>

<verification>
After all tasks complete, verify:
- [ ] Theme toggle button works and cycles through 3 states
- [ ] Light mode: Colors are bright, text is dark
- [ ] Dark mode: Colors are dark, text is light
- [ ] System mode: Follows OS preference
- [ ] Transitions are smooth (300ms)
- [ ] No FOUC (flash of unstyled content) on page load
- [ ] Theme persists across page reloads (localStorage)
- [ ] System preference changes are detected in real-time
- [ ] All components (Button, Card, etc.) render correctly in both themes
</verification>

<must_haves>
## From Phase Goal (Foundation & UI Infrastructure)

The following MUST be TRUE after this plan:

1. **Theme Toggle Works**
   - User can toggle between light and dark themes
   - Toggle button is visible and accessible
   - Button cycles: light → dark → system → light

2. **System Preference Detection**
   - Theme set to 'system' follows OS preference
   - Real-time detection when OS theme changes
   - matchMedia listener is active

3. **Persistence**
   - Theme preference saved to localStorage
   - Preference restored on page load
   - Default is 'system' if no preference saved

4. **No Flash (Anti-FOUC)**
   - Script in index.html applies theme before React renders
   - No visible flash of wrong theme on load
   - Smooth transitions (300ms)

5. **All Components Work in Both Themes**
   - Buttons render correctly in light/dark
   - Cards maintain contrast
   - Text remains readable
   - Borders and backgrounds use CSS variables

6. **Decision Compliance**
   - Theme toggle button located in sidebar footer (per CONTEXT.md decision)
</must_haves>

<success_criteria>
- [ ] All tasks executed successfully
- [ ] Theme switching works smoothly
- [ ] System preference detection functions
- [ ] Theme persists across sessions
- [ ] No FOUC on initial load
- [ ] User verified theme functionality
- [ ] No blocking issues remaining
</success_criteria>
