# 06-07: Accessibility Audit and Improvements

**Wave:** 3
**Depends on:** All previous 06-X plans
**Files Modified:**
- `src/features/accessibility/` (new)
- `src/components/` (modify)

---

## Objective

提升应用无障碍性,符合WCAG 2.2 AA标准,确保所有用户(包括使用辅助技术的用户)都能有效使用应用。

## Success Criteria

1. ✅ 所有交互元素可通过键盘访问(Tab, Enter, Space, Arrow keys)
2. ✅ 屏幕阅读器正确朗读内容和交互元素
3. ✅ 颜色对比度符合WCAG AA标准(4.5:1 for text)
4. ✅ 焦点可见且易于跟踪
5. ✅ 表单错误与输入框正确关联
6. ✅ 模态框正确管理焦点

## Tasks

### Task 1: 添加跳过导航链接

**File:** `src/components/accessibility/SkipLink.tsx`

```typescript
export function SkipLink() {
  return (
    <a
      href="#main-content"
      className="sr-only focus:not-sr-only focus:absolute focus:top-4 focus:left-4 focus:z-50 focus:px-4 focus:py-2 focus:bg-primary focus:text-primary-foreground focus:rounded focus:shadow-lg"
    >
      跳过导航,直接进入主内容
    </a>
  );
}
```

**Integration:**
```typescript
// src/App.tsx
function App() {
  return (
    <>
      <SkipLink />
      <TopNav />
      <main id="main-content" tabIndex={-1}>
        {/* content */}
      </main>
    </>
  );
}
```

**Verify:** Tab键首次聚焦时显示跳过链接,回车跳到主内容

---

### Task 2: 创建焦点陷阱Hook

**File:** `src/features/accessibility/hooks/useFocusTrap.ts`

```typescript
import { useEffect, useRef } from 'react';

export function useFocusTrap(isOpen: boolean) {
  const previousActiveElement = useRef<HTMLElement | null>(null);

  useEffect(() => {
    if (!isOpen) return;

    // 保存当前焦点元素
    previousActiveElement.current = document.activeElement as HTMLElement;

    // 监听焦点,限制在模态框内
    const handleTab = (e: KeyboardEvent) => {
      if (e.key !== 'Tab') return;

      const focusableElements = document.querySelectorAll(
        '#modal-content a[href], button:not([disabled]), textarea:not([disabled]), input:not([disabled]), select:not([disabled]), [tabindex]:not([tabindex="-1"])'
      );

      if (focusableElements.length === 0) return;

      const firstElement = focusableElements[0] as HTMLElement;
      const lastElement = focusableElements[
        focusableElements.length - 1
      ] as HTMLElement;

      if (e.shiftKey) {
        if (document.activeElement === firstElement) {
          e.preventDefault();
          lastElement.focus();
        }
      } else {
        if (document.activeElement === lastElement) {
          e.preventDefault();
          firstElement.focus();
        }
      }
    };

    document.addEventListener('keydown', handleTab);

    return () => {
      document.removeEventListener('keydown', handleTab);
      // 恢复焦点
      previousActiveElement.current?.focus();
    };
  }, [isOpen]);
}
```

**Verify:** 模态框打开时焦点被限制,关闭时恢复

---

### Task 3: 增强模态框无障碍性

**File:** `src/components/ui/dialog.tsx` (modify shadcn/ui Dialog)

**Add:**
```typescript
<Dialog
  role="dialog"
  aria-modal="true"
  aria-labelledby="dialog-title"
  aria-describedby="dialog-description"
>
  <DialogTitle id="dialog-title">标题</DialogTitle>
  <DialogDescription id="dialog-description">
    描述
  </DialogDescription>
  {/* content */}
</Dialog>
```

**Verify:** 屏幕阅读器朗读"对话框,标题,描述"

---

### Task 4: 添加ARIA属性到交互元素

**File:** Various component files

**Examples:**

**按钮(图标按钮):**
```typescript
<Button
  aria-label="创建新会话"
  title="创建新会话"
>
  <Plus />
</Button>
```

**搜索框:**
```typescript
<Input
  type="search"
  aria-label="搜索会话和消息"
  aria-describedby="search-help"
/>
<span id="search-help" className="sr-only">
  输入关键词搜索会话和消息内容
</span>
```

**下拉菜单:**
```typescript
<DropdownMenu>
  <DropdownMenuTrigger aria-haspopup="true" aria-expanded={isOpen}>
    选项
  </DropdownMenuTrigger>
  <DropdownMenuContent role="menu">
    <DropdownMenuItem role="menuitem">
      操作1
    </DropdownMenuItem>
  </DropdownMenuContent>
</DropdownMenu>
```

**Verify:** 屏幕阅读器正确朗读交互元素

---

### Task 5: 改进表单无障碍性

**File:** `src/features/auth/components/CreateSessionForm.tsx` (example)

**Add:**
```typescript
<form onSubmit={handleSubmit}>
  <div>
    <Label htmlFor="session-name">
      会话名称
      <span className="text-destructive" aria-label="必填">
        *
      </span>
    </Label>
    <Input
      id="session-name"
      name="sessionName"
      required
      aria-required="true"
      aria-invalid={errors.sessionName ? 'true' : 'false'}
      aria-describedby={
        errors.sessionName ? 'session-name-error' : undefined
      }
    />
    {errors.sessionName && (
      <p id="session-name-error" className="text-destructive text-sm">
        {errors.sessionName}
      </p>
    )}
  </div>
</form>
```

**Verify:** 表单错误与输入框关联,屏幕阅读器朗读错误

---

### Task 6: 添加实时区域公告

**File:** `src/components/accessibility/LiveAnnouncer.tsx`

```typescript
import { useEffect, useState } from 'react';

export function LiveAnnouncer() {
  const [announcement, setAnnouncement] = useState('');

  useEffect(() => {
    // 监听应用事件并公告
    const handleAnnouncement = (e: CustomEvent) => {
      setAnnouncement(e.detail.message);
      setTimeout(() => setAnnouncement(''), 1000);
    };

    window.addEventListener('announce', handleAnnouncement as EventListener);
    return () =>
      window.removeEventListener(
        'announce',
        handleAnnouncement as EventListener
      );
  }, []);

  return (
    <div
      role="status"
      aria-live="polite"
      aria-atomic="true"
      className="sr-only"
    >
      {announcement}
    </div>
  );
}

// 使用示例
function sendMessage() {
  // ...发送逻辑
  window.dispatchEvent(
    new CustomEvent('announce', {
      detail: { message: '消息已发送' }
    })
  );
}
```

**Verify:** 屏幕阅读器公告重要状态变化

---

### Task 7: 改进键盘导航

**File:** Various component files

**Ensure:**
- 所有交互元素可Tab聚焦
- 下拉菜单使用Arrow键导航
- 列表项使用Home/End键跳转
- Escape键关闭模态框/下拉菜单

**Example (命令面板):**
```typescript
const handleKeyDown = (e: React.KeyboardEvent) => {
  switch (e.key) {
    case 'ArrowDown':
      e.preventDefault();
      focusNextItem();
      break;
    case 'ArrowUp':
      e.preventDefault();
      focusPreviousItem();
      break;
    case 'Home':
      e.preventDefault();
      focusFirstItem();
      break;
    case 'End':
      e.preventDefault();
      focusLastItem();
      break;
    case 'Escape':
      e.preventDefault();
      closeCommandPalette();
      break;
  }
};
```

**Verify:** 键盘导航流畅,符合预期

---

### Task 8: 添加焦点样式

**File:** `tailwind.config.js` (modify)

**Current:** 默认焦点样式

**After:**
```javascript
module.exports = {
  theme: {
    extend: {
      ring: {
        DEFAULT: '2px 2px 0 0 var(--focus-ring)',
      }
    }
  }
};
```

**CSS:**
```css
/* 应用全局焦点样式 */
*:focus-visible {
  outline: 2px solid hsl(var(--primary));
  outline-offset: 2px;
}

/* 移除默认outline,使用自定义样式 */
*:focus {
  outline: none;
}
```

**Verify:** 焦点可见,易于跟踪

---

### Task 9: 检查颜色对比度

**Task:** 使用工具检查所有文本和背景颜色对比度

**Tools:**
- Chrome DevTools > Color contrast
- axe DevTools
- WAVE browser extension

**Fixes:**
- 确保文本对比度≥4.5:1 (WCAG AA)
- 确保大文本对比度≥3:1
- 确保交互元素对比度≥3:1

**Verify:** 所有颜色符合WCAG AA标准

---

### Task 10: 添加语义化HTML

**File:** Various component files

**Improvements:**
- 使用`<nav>`包裹导航
- 使用`<main>`包裹主内容
- 使用`<article>`包裹独立内容
- 使用`<section>`分隔内容区域
- 使用`<header>`和`<footer>`标记页眉页脚

**Example:**
```typescript
<header role="banner">
  <TopNav />
</header>

<main role="main" id="main-content">
  <SessionList />
</main>

<footer role="contentinfo">
  <p>© 2026 AI-Bridge</p>
</footer>
```

**Verify:** 页面结构清晰,屏幕阅读器易于导航

---

### Task 11: 无障碍测试

**Manual Testing:**
- [ ] 只使用键盘导航整个应用
- [ ] 使用屏幕阅读器(NVDA/VoiceOver)
- [ ] 使用高对比度模式
- [ ] 放大文本到200%
- [ ] 禁用CSS,检查内容顺序

**Automated Testing:**
```bash
npm install -D @axe-core/react
```

```typescript
import { axe } from '@axe-core/react';

it('should have no accessibility violations', async () => {
  const { container } = render(<App />);
  const results = await axe(container);
  expect(results).toHaveNoViolations();
});
```

**Verify:** 无障碍测试通过

---

## Testing Checklist

- [ ] Tab键可访问所有交互元素
- [ ] Shift+Tab反向导航工作
- [ ] Enter/Space激活按钮
- [ ] Escape关闭模态框/下拉菜单
- [ ] 箭头键在下拉菜单/列表中导航
- [ ] 屏幕阅读器朗读所有内容
- [ ] 焦点可见且易于跟踪
- [ ] 表单错误与输入框关联
- [ ] 模态框焦点陷阱工作
- [ ] 模态框关闭后焦点恢复
- [ ] 颜色对比度≥4.5:1
- [ ] 所有图标按钮有aria-label
- [ ] 跳过导航链接工作

---

## Must Haves (Goal-backward Verification)

1. ✅ **键盘导航**:所有交互元素可通过键盘访问
2. ✅ **屏幕阅读器支持**:ARIA属性和语义化HTML正确
3. ✅ **焦点管理**:焦点陷阱,焦点恢复,可见焦点样式
4. ✅ **颜色对比度**:符合WCAG AA标准
5. ✅ **表单关联**:错误与输入框正确关联

---

## Notes

- 无障碍是持续改进,不是一次性任务
- 定期使用屏幕阅读器测试应用
- 考虑添加无障碍声明页面
- 遵循"Progressive Enhancement"原则
