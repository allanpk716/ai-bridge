# 06-04: Performance Optimization (Code Splitting, Lazy Loading, Bundle Analysis)

**Wave:** 2
**Depends on:** 06-01, 06-02, 06-03
**Files Modified:**
- `vite.config.ts` (modify)
- `src/App.tsx` (modify)
- `src/features/performance/` (new)

---

## Objective

优化应用性能,通过代码分割、懒加载和Bundle分析减少初始加载时间,提升用户体验。

## Success Criteria

1. ✅ 主bundle大小<500KB (gzip后)
2. ✅ 首次内容绘制(FCP)<2秒(3G网络)
3. ✅ 路由级组件懒加载,按需加载代码
4. ✅ Vendor代码分割,提高缓存命中率
5. ✅ Bundle分析报告生成,可视化依赖关系

## Tasks

### Task 1: 安装rollup-plugin-visualizer

```bash
npm install -D rollup-plugin-visualizer
```

**Verify:** package.json中包含devDependency

---

### Task 2: 配置Vite manualChunks

**File:** `vite.config.ts`

```typescript
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import { visualizer } from 'rollup-plugin-visualizer';

export default defineConfig({
  plugins: [
    react(),
    visualizer({
      open: true,
      filename: './dist/stats.html',
      gzipSize: true,
      brotliSize: true,
    })
  ],
  build: {
    rollupOptions: {
      output: {
        manualChunks: {
          // React核心(稳定,缓存友好)
          'react-vendor': ['react', 'react-dom', 'react-router-dom'],

          // UI库(大但稳定)
          'ui-vendor': [
            '@radix-ui/react-dialog',
            '@radix-ui/react-dropdown-menu',
            '@radix-ui/react-select',
            'cmdk',
            'sonner'
          ],

          // 数据处理(中等大小)
          'data-vendor': [
            '@tanstack/react-query',
            '@tanstack/react-query-devtools',
            'fuse.js',
            'zustand'
          ],

          // 实时通信(可能需要更新)
          'socket-vendor': ['socket.io-client'],

          // Markdown渲染(只在聊天时需要)
          'markdown-vendor': [
            'react-markdown',
            'remark-gfm',
            'react-syntax-highlighter',
            'streamdown'
          ],

          // 工具库(小但常用)
          'utils': [
            'date-fns',
            'clsx',
            'tailwind-merge'
          ]
        }
      }
    }
  }
});
```

**Verify:** 构建时生成多个chunk文件

---

### Task 3: 实现路由级懒加载

**File:** `src/App.tsx`

**Current Code:**
```typescript
import SessionDetail from './features/sessions/SessionDetail';
import Settings from './features/settings/Settings';
```

**After:**
```typescript
import { lazy, Suspense } from 'react';
import { Loader2 } from 'lucide-react';

const SessionDetail = lazy(() =>
  import('./features/sessions/SessionDetail')
);
const Settings = lazy(() =>
  import('./features/settings/Settings')
);

function LoadingFallback() {
  return (
    <div className="flex items-center justify-center min-h-[400px]">
      <Loader2 className="animate-spin h-8 w-8" />
      <span className="ml-2">加载中...</span>
    </div>
  );
}

function App() {
  return (
    <Suspense fallback={<LoadingFallback />}>
      <Routes>
        <Route path="/sessions/:id" element={<SessionDetail />} />
        <Route path="/settings" element={<Settings />} />
      </Routes>
    </Suspense>
  );
}
```

**Verify:** 路由切换时显示加载状态

---

### Task 4: 懒加载重型组件

**File:** `src/features/chat/components/CodeBlock.tsx` (modify)

**Current:** 直接import react-syntax-highlighter

**After:**
```typescript
import { lazy, Suspense } from 'react';

const SyntaxHighlighter = lazy(() =>
  import('react-syntax-highlighter').then(module => ({
    default: module.Prism as any
  }))
);

export function CodeBlock({ language, code }: Props) {
  return (
    <Suspense fallback={<pre className="p-4 bg-muted">{code}</pre>}>
      <SyntaxHighlighter language={language}>
        {code}
      </SyntaxHighlighter>
    </Suspense>
  );
}
```

**Verify:** 代码块延迟加载,不阻塞初始渲染

---

### Task 5: 创建Bundle分析脚本

**File:** `scripts/analyze-bundle.sh` (Windows: `.bat`)

```bash
#!/bin/bash
npm run build
npm run analyze
```

**package.json:**
```json
{
  "scripts": {
    "build": "vite build",
    "analyze": "rollup-plugin-visualizer dist/stats.html"
  }
}
```

**Verify:** 构建后自动打开可视化报告

---

### Task 6: 添加性能监控

**File:** `src/lib/performance.ts`

```typescript
export function logPerformanceMetric(name: string, duration: number) {
  if (import.meta.env.DEV) {
    console.log(`[Performance] ${name}: ${duration}ms`);
  }
  // 可以发送到分析服务(如Google Analytics)
}

export function measureRender(componentName: string) {
  return performance.mark(`${componentName}-render-start`);
}

export function measureRenderEnd(componentName: string) {
  performance.mark(`${componentName}-render-end`);
  performance.measure(
    componentName,
    `${componentName}-render-start`,
    `${componentName}-render-end`
  );

  const measure = performance.getEntriesByName(componentName)[0];
  logPerformanceMetric(componentName, measure.duration);
}
```

**Verify:** 性能指标在开发控制台显示

---

### Task 7: 优化导入(Cherry-picking)

**File:** Various component files

**Current:**
```typescript
import * as Icons from 'lucide-react';
```

**After:**
```typescript
import { Search, Download, Settings } from 'lucide-react';
```

**Current:**
```typescript
import { Button, Input, Card } from '@/components/ui';
```

**After:** (已有barrel exports,无需修改)

**Verify:** Bundle大小减少

---

### Task 8: 配置Vite build选项

**File:** `vite.config.ts`

```typescript
export default defineConfig({
  build: {
    // 增大chunk大小警告阈值
    chunkSizeWarningLimit: 1000,

    // 启用CSS代码分割
    cssCodeSplit: true,

    // 目标浏览器
    target: 'es2015',

    // 压缩配置
    minify: 'terser',
    terserOptions: {
      compress: {
        drop_console: true, // 生产环境移除console
        drop_debugger: true
      }
    }
  }
});
```

**Verify:** 生产构建体积优化

---

### Task 9: 创建性能基准测试

**File:** `scripts/performance-benchmark.md`

```markdown
# 性能基准测试

## 测试环境
- 网络: Chrome DevTools throttling (Fast 3G)
- 设备: Desktop (4x CPU slowdown)

## 指标目标
| 指标 | 目标 | 当前 |
|------|------|------|
| FCP | <2s | - |
| LCP | <2.5s | - |
| TTI | <3.5s | - |
| Bundle Size | <500KB | - |

## 测试步骤
1. 打开Chrome DevTools > Lighthouse
2. 运行Performance audit
3. 记录指标
4. 与目标对比
```

**Verify:** 基准测试可执行

---

## Testing Checklist

- [ ] `npm run build`成功构建
- [ ] 构建后生成stats.html文件
- [ ] stats.html显示chunk可视化
- [ ] 主bundle大小<500KB
- [ ] 路由切换时显示加载状态
- [ ] Network面板显示按需加载chunk
- [ ] 懒加载组件不阻塞初始渲染
- [ ] Chrome DevTools > Lighthouse评分>90
- [ ] 3G网络下首次加载<5秒

---

## Must Haves (Goal-backward Verification)

1. ✅ **代码分割**:manualChunks配置正确,生成多个chunk
2. ✅ **懒加载**:路由级组件使用React.lazy
3. ✅ **Bundle分析**:rollup-plugin-visualizer生成可视化报告
4. ✅ **性能目标**:主bundle<500KB,FCP<2s
5. ✅ **缓存优化**:Vendor代码独立chunk,提高缓存命中率

---

## Notes

- 使用`npm run build -- --mode production`生成生产构建
- stats.html文件在dist目录,用浏览器打开查看
- 定期分析bundle,移除未使用的依赖
- 考虑使用PurgeCSS移除未使用的Tailwind类
