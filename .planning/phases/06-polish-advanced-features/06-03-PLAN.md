# 06-03: Keyboard Shortcuts System with Help Modal

**Wave:** 1
**Depends on:** None (cmdk已安装)
**Files Modified:**
- `src/features/keyboard/` (new)
- `src/App.tsx` (modify)
- `src/features/chat/components/ChatInput.tsx` (modify)

---

## Objective

扩展全局键盘快捷键系统,在现有cmdk命令面板基础上,添加更多快捷键和帮助模态框,提升高级用户体验。

## Success Criteria

1. ✅ 用户可以按Ctrl+/ (Mac: Cmd+/)打开快捷键帮助模态框
2. ✅ 帮助模态框显示所有可用的快捷键,按功能分组
3. ✅ 聊天输入框中Ctrl+Enter发送消息
4. ✅ 快捷键不与浏览器默认行为冲突(在输入框中)
5. ✅ 快捷键在Windows和Mac上正确显示(Ctrl vs Cmd)

## Tasks

### Task 1: 定义快捷键配置

**File:** `src/features/keyboard/shortcuts.ts`

```typescript
export interface Shortcut {
  keyCombo: string;       // e.g., 'ctrl+k', 'ctrl+/', 'escape'
  name: string;
  description: string;
  scope: 'global' | 'local';
  action: () => void;
  group?: string;
  category?: 'navigation' | 'editing' | 'chat' | 'system';
}

// 检测平台
const isMac = navigator.platform.toUpperCase().indexOf('MAC') >= 0;

// 快捷键显示名称转换
export function formatKeyCombo(keyCombo: string): string {
  return keyCombo
    .replace('ctrl', isMac ? '⌘' : 'Ctrl')
    .replace('meta', '⌘')
    .replace('shift', '⇧')
    .replace('alt', isMac ? '⌥' : 'Alt')
    .replace('+', isMac ? '' : ' + ')
    .toUpperCase();
}

export const globalShortcuts: Shortcut[] = [
  // 导航
  {
    keyCombo: 'ctrl+k',
    name: '打开命令面板',
    description: '快速访问所有命令和操作',
    scope: 'global',
    action: () => {}, // cmdk handles this
    group: '导航',
    category: 'navigation'
  },
  {
    keyCombo: 'ctrl+/',
    name: '快捷键帮助',
    description: '显示所有可用的键盘快捷键',
    scope: 'global',
    action: () => openShortcutHelp(),
    group: '帮助',
    category: 'system'
  },

  // 聊天
  {
    keyCombo: 'ctrl+enter',
    name: '发送消息',
    description: '在聊天输入框中发送消息',
    scope: 'local',
    action: () => submitMessage(),
    group: '聊天',
    category: 'chat'
  },

  // 编辑
  {
    keyCombo: 'ctrl+shift+n',
    name: '新建会话',
    description: '创建新的Claude会话',
    scope: 'global',
    action: () => openCreateSessionDialog(),
    group: '会话',
    category: 'navigation'
  }
];
```

**Verify:** 快捷键配置定义完整,平台检测工作

---

### Task 2: 创建useGlobalShortcuts Hook

**File:** `src/features/keyboard/hooks/useGlobalShortcuts.ts`

```typescript
import { useEffect } from 'react';
import { Shortcut } from '../shortcuts';

export function useGlobalShortcuts(shortcuts: Shortcut[]) {
  useEffect(() => {
    const handleKeyDown = (event: KeyboardEvent) => {
      // 检查是否在输入框中
      const activeElement = document.activeElement;
      const isInput =
        activeElement?.tagName === 'INPUT' ||
        activeElement?.tagName === 'TEXTAREA' ||
        activeElement?.getAttribute('contenteditable') === 'true';

      for (const shortcut of shortcuts) {
        // 跳过本地快捷键(在输入框中)
        if (shortcut.scope === 'local' && !isInput) continue;
        // 跳过全局快捷键(在输入框中,除非明确允许)
        if (shortcut.scope === 'global' && isInput) continue;

        const keys = shortcut.keyCombo.split('+');
        const ctrlKey = keys.includes('ctrl');
        const shiftKey = keys.includes('shift');
        const altKey = keys.includes('alt');
        const metaKey = keys.includes('meta');
        const key = keys[keys.length - 1].toLowerCase();

        if (
          event.ctrlKey === ctrlKey &&
          event.shiftKey === shiftKey &&
          event.altKey === altKey &&
          event.metaKey === metaKey &&
          event.key.toLowerCase() === key
        ) {
          event.preventDefault();
          shortcut.action();
          break;
        }
      }
    };

    document.addEventListener('keydown', handleKeyDown);
    return () => document.removeEventListener('keydown', handleKeyDown);
  }, [shortcuts]);
}
```

**Verify:** Hook在组件中可用,快捷键正确触发

---

### Task 3: 创建ShortcutSheet组件

**File:** `src/features/keyboard/components/ShortcutSheet.tsx`

**UI Layout:**
```
┌─────────────────────────────────┐
│ ⌨️ 键盘快捷键                   │
├─────────────────────────────────┤
│ [搜索快捷键...]                 │
├─────────────────────────────────┤
│ 📍 导航                         │
│   ⌘K            打开命令面板     │
│   ⌘⇧N          新建会话         │
│   ⌘/           快捷键帮助        │
├─────────────────────────────────┤
│ 💬 聊天                         │
│   ⌘Enter        发送消息         │
│   Escape        清空输入         │
├─────────────────────────────────┤
│ 🎯 系统                         │
│   ⌘,           打开设置         │
└─────────────────────────────────┘
```

**Features:**
- 按分组显示快捷键
- 搜索框:过滤快捷键
- 快捷键组合键格式化显示(⌘ vs Ctrl)
- 使用shadcn/ui的Sheet组件(侧边栏)
- 或使用Dialog组件(模态框)

**Verify:** 快捷键列表正确显示,搜索过滤工作

---

### Task 4: 创建ShortcutHelpModal组件

**File:** `src/features/keyboard/components/ShortcutHelpModal.tsx`

**Features:**
- 使用shadcn/ui的Dialog组件
- 触发方式:Ctrl+/或点击帮助按钮
- 包含ShortcutSheet组件
- 添加"关闭说明"文本:"按ESC或点击外部区域关闭"

**Verify:** 模态框正确打开/关闭,快捷键触发

---

### Task 5: 集成Ctrl+Enter到ChatInput

**File:** `src/features/chat/components/ChatInput.tsx`

**Modifications:**
- 添加keydown事件监听
- 检测Ctrl+Enter (Mac: Cmd+Enter)
- 触发sendMessage函数
- Shift+Enter保持换行行为

**Code:**
```typescript
const handleKeyDown = (e: React.KeyboardEvent<HTMLTextAreaElement>) => {
  if (e.key === 'Enter') {
    if (e.ctrlKey || e.metaKey) {
      e.preventDefault();
      onSubmit(); // 发送消息
    }
    // Shift+Enter默认换行
  }
};

<textarea
  onKeyDown={handleKeyDown}
  placeholder="输入消息... (Ctrl+Enter发送)"
/>
```

**Verify:** Ctrl+Enter发送消息,Shift+Enter换行

---

### Task 6: 添加快捷键提示Tooltip

**Locations:**
- 发送按钮:"快捷键: Ctrl+Enter"
- 命令面板按钮:"快捷键: Ctrl+K"
- 帮助按钮:"快捷键: Ctrl+/"

**Implementation:**
- 使用shadcn/ui的Tooltip组件
- 在相应按钮上添加

**Verify:** Tooltip在悬停时显示快捷键

---

### Task 7: 创建ShortcutProvider

**File:** `src/features/keyboard/ShortcutProvider.tsx`

```typescript
import { createContext, useContext } from 'react';
import { Shortcut } from './shortcuts';
import { useGlobalShortcuts } from './hooks/useGlobalShortcuts';

interface ShortcutContextValue {
  registerShortcut: (shortcut: Shortcut) => void;
  unregisterShortcut: (keyCombo: string) => void;
}

const ShortcutContext = createContext<ShortcutContextValue | null>(null);

export function ShortcutProvider({ children }: { children: React.ReactNode }) {
  const [shortcuts, setShortcuts] = useState<Shortcut[]>([]);

  const registerShortcut = (shortcut: Shortcut) => {
    setShortcuts(prev => [...prev, shortcut]);
  };

  const unregisterShortcut = (keyCombo: string) => {
    setShortcuts(prev => prev.filter(s => s.keyCombo !== keyCombo));
  };

  useGlobalShortcuts(shortcuts);

  return (
    <ShortcutContext.Provider value={{ registerShortcut, unregisterShortcut }}>
      {children}
    </ShortcutContext.Provider>
  );
}

export function useShortcuts() {
  const context = useContext(ShortcutContext);
  if (!context) {
    throw new Error('useShortcuts must be used within ShortcutProvider');
  }
  return context;
}
```

**Verify:** Provider正确包装应用,快捷键注册工作

---

### Task 8: 在App.tsx中集成快捷键系统

**File:** `src/App.tsx`

**Modifications:**
- 在Provider层级添加ShortcutProvider
- 注册全局快捷键
- 添加ShortcutHelpModal到全局模态框区域

**Provider顺序:**
```
<ShortcutProvider>
  <QueryProvider>
    <SocketProvider>
      <ThemeProvider>
        <RouterProvider>
```

**Verify:** 全局快捷键在应用各处可用

---

### Task 9: 添加快捷键帮助按钮到TopNav

**File:** `src/components/TopNav.tsx`

**Location:** 右侧,搜索按钮旁边

**Icon:** Keyboard from lucide-react
**Tooltip:** "键盘快捷键 (Ctrl+/)"

**Verify:** 按钮显示正确,点击打开帮助模态框

---

## Testing Checklist

- [ ] Ctrl+/打开快捷键帮助模态框
- [ ] 帮助模态框显示所有快捷键
- [ ] 快捷键按分组正确显示
- [ ] 搜索框过滤快捷键
- [ ] Ctrl+Enter在聊天输入框中发送消息
- [ ] Ctrl+K打开命令面板(已存在)
- [ ] 快捷键在输入框外工作,在输入框内不冲突
- [ ] Mac上显示⌘,Windows上显示Ctrl
- [ ] ESC关闭帮助模态框
- [ ] 快捷键提示Tooltip显示正确

---

## Must Haves (Goal-backward Verification)

1. ✅ **全局快捷键系统**:useGlobalShortcuts Hook正确注册和处理快捷键
2. ✅ **帮助模态框**:ShortcutHelpModal显示所有快捷键,按功能分组
3. ✅ **发送消息快捷键**:Ctrl+Enter在ChatInput中工作
4. ✅ **平台适配**:Mac显示⌘,Windows/Linux显示Ctrl
5. ✅ **输入框安全**:快捷键在输入框中不触发(除非明确定义为local)

---

## Notes

- 现有cmdk已处理Ctrl+K,无需修改
- 未来可扩展:用户自定义快捷键、快捷键录制
- 快捷键冲突检测:如果多个快捷键使用相同组合,显示警告
