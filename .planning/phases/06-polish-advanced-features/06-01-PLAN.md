# 06-01: Search Interface with Session and Message Search

**Wave:** 1
**Depends on:** None
**Files Modified:**
- `src/features/search/` (new)
- `src/features/sessions/components/SessionList.tsx` (modify)
- `src/features/chat/components/ChatMessageList.tsx` (modify)
- `src/components/TopNav.tsx` (modify)

---

## Objective

实现全局搜索功能,允许用户在所有会话和消息中进行模糊搜索,快速找到历史对话内容。搜索结果应支持关键词高亮,并提供实时搜索反馈。

## Success Criteria

1. ✅ 用户可以通过TopNav中的搜索框输入关键词
2. ✅ 搜索框实时显示匹配的会话和消息数量
3. ✅ 搜索结果显示会话名称、消息预览,关键词高亮显示
4. ✅ 点击搜索结果可以跳转到对应会话和消息位置
5. ✅ 搜索支持模糊匹配(Fuse.js),容错拼写错误
6. ✅ 搜索性能良好,1000条消息下<100ms响应时间

## Tasks

### Task 1: 安装Fuse.js依赖

```bash
npm install fuse.js
```

**Verify:** 检查package.json中fuse.js@^7.0.0已添加

---

### Task 2: 创建useFuseSearch自定义Hook

**File:** `src/features/search/hooks/useFuseSearch.ts`

```typescript
import Fuse from 'fuse.js';
import { useMemo } from 'react';

interface SearchOptions {
  keys: string[];
  threshold?: number;
  includeScore?: boolean;
}

export function useFuseSearch<T>(
  data: T[],
  searchQuery: string,
  options: SearchOptions
) {
  const fuse = useMemo(() => {
    return new Fuse(data, {
      includeScore: true,
      threshold: options.threshold ?? 0.3,
      keys: options.keys,
    });
  }, [data, options.keys, options.threshold]);

  const results = useMemo(() => {
    if (!searchQuery.trim()) {
      return data;
    }
    return fuse.search(searchQuery).map(result => result.item);
  }, [fuse, searchQuery, data]);

  return results;
}
```

**Verify:** TypeScript编译通过,Hook在组件中可用

---

### Task 3: 创建SearchBar组件

**File:** `src/features/search/components/SearchBar.tsx`

- 使用shadcn/ui的Input组件作为基础
- 添加搜索图标(Search from lucide-react)
- 添加清除按钮(x图标),只在有输入时显示
- 集成debounce(300ms),避免频繁搜索
- 显示匹配数量提示(例如:"找到5个结果")

**Key Features:**
- `placeholder`: "搜索会话和消息..."
- 自动聚焦(Ctrl+K已由cmdk处理)
- 输入框状态:empty | searching | hasResults | noResults

**Verify:** 组件渲染正确,输入触发搜索,清除按钮工作

---

### Task 4: 创建SearchHighlight组件

**File:** `src/features/search/components/SearchHighlight.tsx`

```typescript
export function SearchHighlight({
  text,
  searchQuery
}: {
  text: string;
  searchQuery: string;
}) {
  if (!searchQuery.trim()) {
    return <>{text}</>;
  }

  const regex = new RegExp(`(${searchQuery})`, 'gi');
  const parts = text.split(regex);

  return (
    <>
      {parts.map((part, i) =>
        regex.test(part) ? (
          <mark key={i} className="bg-yellow-200 dark:bg-yellow-800 rounded px-0.5">
            {part}
          </mark>
        ) : (
          <span key={i}>{part}</span>
        )
      )}
    </>
  );
}
```

**Verify:** 关键词在文本中高亮显示,保留大小写

---

### Task 5: 创建SearchResults组件

**File:** `src/features/search/components/SearchResults.tsx`

**UI结构:**
```
┌─────────────────────────────┐
│ 搜索结果 (5)                 │
├─────────────────────────────┤
│ 👤 会话: API设计讨论          │
│    "...关于RESTful <mark>API</mark>..." │
│    2024-02-09 10:30          │
├─────────────────────────────┤
│ 🤖 消息: 如何实现认证?         │
│    "...JWT <mark>认证</mark>流程..." │
│    会话: 后端开发 | 2月8日     │
└─────────────────────────────┘
```

**Features:**
- 分类显示:会话结果 / 消息结果
- 每个结果显示:图标 + 标题 + 预览(高亮) + 时间戳
- 空状态:"未找到匹配结果,尝试其他关键词"
- 加载状态:骨架屏(Skeleton)

**Verify:** 结果正确分组,点击跳转工作

---

### Task 6: 集成搜索到会话列表

**File:** `src/features/sessions/components/SessionList.tsx`

**Modifications:**
- 添加搜索状态管理(searchQuery state)
- 集成useFuseSearch Hook
- keys: `['name', 'metadata.workingDir']`
- 搜索时过滤会话列表
- 保持现有状态过滤(status, sort)与搜索共存

**Verify:** 搜索与现有过滤器协同工作

---

### Task 7: 集成搜索到TopNav

**File:** `src/components/TopNav.tsx`

**Modifications:**
- 在右侧添加SearchBar组件
- 移动端:点击搜索图标展开全屏搜索框
- 桌面端:搜索框固定在TopNav右侧

**Verify:** 搜索框在不同屏幕尺寸下正确显示

---

### Task 8: 创建搜索路由(可选)

**File:** `src/pages/Search.tsx` (new)

提供专门的搜索页面:
- URL: `/search?q=关键词`
- 支持深度链接(分享搜索结果)
- 显示更详细的搜索结果(分页、过滤器)

**Verify:** URL参数正确解析,搜索结果可分享

---

### Task 9: 性能优化

**Optimizations:**
1. **useMemo缓存Fuse实例**:只在data变化时重建
2. **Debounce搜索输入**:300ms延迟
3. **限制结果数量**:最多显示100条结果
4. **虚拟滚动**:如果结果>50,使用react-virtuoso

**Verify:** Performance监控显示<100ms搜索时间(1000条消息)

---

## Testing Checklist

- [ ] 搜索空状态显示"输入关键词开始搜索"
- [ ] 搜索无结果显示"未找到匹配结果"
- [ ] 搜索结果显示正确的匹配数量
- [ ] 关键词在结果中正确高亮
- [ ] 点击会话结果跳转到SessionDetail
- [ ] 点击消息结果滚动到对应消息并高亮
- [ ] 搜索与状态过滤器(status)协同工作
- [ ] 搜索性能测试(100/1000/10000条消息)
- [ ] 拼写容错测试("auth"匹配"authentication")
- [ ] 特殊字符处理(测试正则表达式字符)

---

## Must Haves (Goal-backward Verification)

1. ✅ **Fuse.js集成**:搜索功能使用Fuse.js实现模糊匹配
2. ✅ **实时搜索**:输入后300ms内显示结果
3. ✅ **结果高亮**:搜索词在预览中高亮显示
4. ✅ **结果导航**:点击结果跳转到对应位置
5. ✅ **性能**:1000条消息下<100ms响应

---

## Notes

- 搜索索引存储在内存中,不需要后端支持
- 如需搜索大量数据(>10000),考虑使用Web Worker避免阻塞UI
- 未来可扩展:搜索历史记录、热门搜索、搜索建议
