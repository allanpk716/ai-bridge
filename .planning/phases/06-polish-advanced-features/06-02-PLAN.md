# 06-02: Export to Markdown Functionality

**Wave:** 1
**Depends on:** None
**Files Modified:**
- `src/features/export/` (new)
- `src/features/sessions/components/SessionDetail.tsx` (modify)
- `src/features/chat/components/ChatMessageList.tsx` (modify)

---

## Objective

实现会话导出功能,允许用户将会话对话历史导出为Markdown格式文件,便于保存、分享或存档。

## Success Criteria

1. ✅ 用户可以在会话详情页点击"导出"按钮
2. ✅ 导出的Markdown文件包含完整的对话历史(用户+助手消息)
3. ✅ Markdown格式规范,包含标题、时间戳、角色标识
4. ✅ 代码块正确格式化,保留语言标识
5. ✅ 文件自动下载,文件名为会话名称(清理特殊字符)
6. ✅ 导出前显示确认对话框,包含文件预览

## Tasks

### Task 1: 创建markdownExporter工具函数

**File:** `src/features/export/utils/markdownExporter.ts`

```typescript
import { Message } from '@/types';

export function exportSessionToMarkdown(
  sessionName: string,
  messages: Message[]
) {
  // 1. 构建Markdown内容
  let markdown = `# ${sessionName}\n\n`;
  markdown += `*导出时间: ${new Date().toLocaleString('zh-CN')}*\n\n---\n\n`;

  messages.forEach(msg => {
    const role = msg.role === 'user' ? '👤 用户' : '🤖 Claude';
    const timestamp = new Date(msg.createdAt).toLocaleString('zh-CN');

    markdown += `## ${role}\n\n`;
    markdown += `${msg.content}\n\n`;
    markdown += `*时间: ${timestamp}*\n\n`;
    markdown += `---\n\n`;
  });

  // 2. 创建Blob并触发下载
  const blob = new Blob([markdown], {
    type: 'text/markdown;charset=utf-8'
  });
  const url = URL.createObjectURL(blob);
  const link = document.createElement('a');
  link.href = url;

  // 清理文件名:移除特殊字符,替换为下划线
  const safeFileName = sessionName
    .replace(/[^a-z0-9\u4e00-\u9fa5]/gi, '_')
    .replace(/_+/g, '_')
    .substring(0, 100);

  link.download = `${safeFileName}_export.md`;
  document.body.appendChild(link);
  link.click();
  document.body.removeChild(link);
  URL.revokeObjectURL(url);

  return markdown; // 返回用于预览
}
```

**Verify:** 函数生成有效的Markdown内容

---

### Task 2: 创建ExportButton组件

**File:** `src/features/export/components/ExportButton.tsx`

**Features:**
- 使用shadcn/ui的Button + DropdownMenu
- 图标:Download from lucide-react
- 位置:SessionDetail页面的操作栏
- 菜单选项:
  - "导出为Markdown"
  - "导出选中的消息" (未来扩展)

**Verify:** 按钮显示正确,点击打开菜单

---

### Task 3: 创建ExportPreviewModal组件

**File:** `src/features/export/components/ExportPreviewModal.tsx`

**UI Layout:**
```
┌─────────────────────────────────┐
│ 导出预览                        │
├─────────────────────────────────┤
│ 文件名: session_export.md       │
│ 文件大小: ~25 KB                 │
│ 消息数量: 42                     │
├─────────────────────────────────┤
│ [Markdown预览区域]              │
│ # 会话名称                       │
│                                 │
│ ## 👤 用户                       │
│ 如何实现认证?                    │
│                                 │
│ ## 🤖 Claude                    │
│ 推荐使用JWT...                   │
│                                 │
│ [滚动预览...]                   │
├─────────────────────────────────┤
│ [取消] [确认导出]                │
└─────────────────────────────────┘
```

**Features:**
- 显示导出统计(文件大小、消息数量)
- Markdown预览区域(只读,滚动)
- 使用react-markdown渲染预览
- 确认按钮:触发实际下载
- 取消按钮:关闭模态框

**Verify:** 预览正确显示,确认下载工作

---

### Task 4: 创建useExportMutation Hook

**File:** `src/features/export/hooks/useExportMutation.ts`

```typescript
import { useMutation } from '@tanstack/react-query';
import { toast } from 'sonner';
import { exportSessionToMarkdown } from '../utils/markdownExporter';

export function useExportMutation() {
  return useMutation({
    mutationFn: async ({
      sessionName,
      messages
    }: {
      sessionName: string;
      messages: Message[];
    }) => {
      return exportSessionToMarkdown(sessionName, messages);
    },
    onSuccess: (markdown, variables) => {
      toast.success(`已导出: ${variables.sessionName}`);
    },
    onError: (error) => {
      toast.error('导出失败,请重试');
      console.error('Export error:', error);
    }
  });
}
```

**Verify:** Hook在组件中可用,成功/失败toast显示

---

### Task 5: 集成导出功能到SessionDetail

**File:** `src/features/sessions/components/SessionDetail.tsx`

**Modifications:**
- 在操作栏添加ExportButton组件
- 导入useExportMutation Hook
- 获取会话数据(session, messages)
- 点击导出时:
  1. 打开ExportPreviewModal
  2. 用户确认后调用mutation
  3. 成功后显示toast

**Verify:** 导出流程完整工作

---

### Task 6: 添加导出历史记录(可选)

**File:** `src/features/export/utils/exportHistory.ts`

**Features:**
- LocalStorage存储导出历史
- 记录:sessionId, fileName, exportTime
- 提供"最近导出"列表
- 快速重新导出

**Verify:** 历史记录正确保存和读取

---

### Task 7: 导出选项扩展(未来)

**Optional Enhancements:**
- "导出选中消息":多选消息后导出
- "导出为JSON":包含元数据(metadata)
- "导出为TXT":纯文本格式
- "导出时间范围":日期选择器

**Verify:** 不在当前阶段实现

---

## Testing Checklist

- [ ] 导出按钮在SessionDetail页显示
- [ ] 点击导出打开预览模态框
- [ ] 预览显示正确的Markdown内容
- [ ] 文件名正确生成(特殊字符已清理)
- [ ] 确认导出触发文件下载
- [ ] 下载的文件可以正常打开(文本编辑器)
- [ ] 代码块在Markdown中正确格式化
- [ ] 时间戳格式正确(中文本地化)
- [ ] 空会话导出生成有效文件
- [ ] 包含特殊字符的消息正确导出(转义)

---

## Must Haves (Goal-backward Verification)

1. ✅ **Markdown格式**:导出的文件符合Markdown规范
2. ✅ **文件下载**:浏览器自动下载生成的.md文件
3. ✅ **完整历史**:包含所有用户和助手消息
4. ✅ **用户确认**:导出前显示预览和确认对话框
5. ✅ **错误处理**:导出失败时显示友好提示

---

## Notes

- Markdown使用UTF-8编码,支持中文
- 未来可扩展:导出为PDF、导出到云服务
- 考虑添加导出进度指示(如果会话很大)
