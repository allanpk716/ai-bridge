# 06-06: Loading Skeletons and Optimistic UI Updates

**Wave:** 2
**Depends on:** 06-01, 06-02, 06-03
**Files Modified:**
- `src/components/skeletons/` (new)
- `src/features/sessions/` (modify)
- `src/features/chat/` (modify)

---

## Objective

使用骨架屏和乐观UI更新提升加载体验,减少用户等待焦虑,让应用感觉更快更流畅。

## Success Criteria

1. ✅ 会话列表、消息列表使用骨架屏代替spinner
2. ✅ 骨架屏动画流畅(shimmer效果)
3. ✅ 发送消息时立即显示(乐观更新)
4. ✅ 发送失败时回滚并显示错误
5. ✅ 加载状态与主题自适应(深色/浅色模式)

## Tasks

### Task 1: 安装react-loading-skeleton

```bash
npm install react-loading-skeleton
```

**Verify:** package.json中包含react-loading-skeleton@^3.4.0

---

### Task 2: 创建会话列表骨架屏

**File:** `src/components/skeletons/SessionListSkeleton.tsx`

```typescript
import Skeleton from 'react-loading-skeleton';
import 'react-loading-skeleton/dist/skeleton.css';

export function SessionListSkeleton({ count = 5 }: { count?: number }) {
  return (
    <div className="space-y-3 p-4">
      {Array.from({ length: count }).map((_, i) => (
        <div key={i} className="flex items-center gap-3 p-3 border rounded-lg">
          <Skeleton circle width={40} height={40} />
          <div className="flex-1 space-y-2">
            <Skeleton width="60%" height={20} />
            <Skeleton width="40%" height={14} />
          </div>
          <Skeleton width={60} height={24} />
        </div>
      ))}
    </div>
  );
}
```

**Verify:** 骨架屏匹配会话列表项布局

---

### Task 3: 创建消息列表骨架屏

**File:** `src/components/skeletons/ChatMessageListSkeleton.tsx`

```typescript
import Skeleton from 'react-loading-skeleton';

export function ChatMessageListSkeleton({ count = 3 }: { count?: number }) {
  return (
    <div className="space-y-4 p-4">
      {Array.from({ length: count }).map((_, i) => (
        <div
          key={i}
          className={`flex gap-3 ${i % 2 === 0 ? 'justify-end' : 'justify-start'}`}
        >
          <Skeleton
            circle
            width={32}
            height={32}
            className="flex-shrink-0"
          />
          <div className={`max-w-[70%] space-y-2 ${i % 2 === 0 ? 'order-first' : ''}`}>
            <Skeleton width={120} height={14} />
            <Skeleton width="100%" height={16} count={3} />
          </div>
        </div>
      ))}
    </div>
  );
}
```

**Verify:** 骨架屏匹配消息气泡布局

---

### Task 4: 创建通用骨架屏组件

**File:** `src/components/skeletons/CardSkeleton.tsx`

```typescript
import Skeleton from 'react-loading-skeleton';

export function CardSkeleton() {
  return (
    <div className="p-6 border rounded-lg space-y-4">
      <div className="flex items-center justify-between">
        <Skeleton width={120} height={24} />
        <Skeleton width={80} height={32} />
      </div>
      <Skeleton width="100%" height={16} count={2} />
      <Skeleton width="60%" height={16} />
    </div>
  );
}

export function TableSkeleton({ rows = 5 }: { rows?: number }) {
  return (
    <div className="space-y-2">
      <div className="flex gap-4 p-3 border-b">
        <Skeleton width="20%" height={20} count={4} />
      </div>
      {Array.from({ length: rows }).map((_, i) => (
        <div key={i} className="flex gap-4 p-3">
          <Skeleton width="20%" height={16} />
          <Skeleton width="30%" height={16} />
          <Skeleton width="25%" height={16} />
          <Skeleton width="15%" height={16} />
          <Skeleton width={50} height={32} />
        </div>
      ))}
    </div>
  );
}
```

**Verify:** 通用骨架屏可在多处使用

---

### Task 5: 集成骨架屏到会话列表

**File:** `src/features/sessions/components/SessionList.tsx`

**Current:**
```typescript
{isLoading && <Loader2 className="animate-spin" />}
```

**After:**
```typescript
import { SessionListSkeleton } from '@/components/skeletons/SessionListSkeleton';

{isLoading && <SessionListSkeleton />}
{!isLoading && sessions.length === 0 && <EmptyState />}
{!isLoading && sessions.map(session => (
  <SessionListItem key={session.id} session={session} />
))}
```

**Verify:** 加载时显示骨架屏,加载完成后显示实际内容

---

### Task 6: 集成骨架屏到消息列表

**File:** `src/features/chat/components/ChatMessageList.tsx`

**Current:** 使用LoadingSpinner

**After:**
```typescript
import { ChatMessageListSkeleton } from '@/components/skeletons/ChatMessageListSkeleton';

<Suspense fallback={<ChatMessageListSkeleton />}>
  <ChatMessageListContent />
</Suspense>
```

**Verify:** 首次加载和切换会话时显示骨架屏

---

### Task 7: 实现乐观UI更新

**File:** `src/features/chat/hooks/useSendMessage.ts`

**Current:** 等待服务器响应后显示消息

**After:**
```typescript
const mutation = useMutation({
  mutationFn: sendMessageAPI,
  onMutate: async (newMessage) => {
    // 取消正在进行的查询
    await queryClient.cancelQueries({ queryKey: ['messages', sessionId] });

    // 保存当前数据
    const previousMessages = queryClient.getQueryData([
      'messages',
      sessionId
    ]);

    // 乐观添加消息
    queryClient.setQueryData(
      ['messages', sessionId],
      (old: Message[]) => [
        ...old,
        {
          ...newMessage,
          id: `temp-${Date.now()}`,
          status: 'sending',
          createdAt: new Date().toISOString()
        }
      ]
    );

    return { previousMessages };
  },
  onError: (err, newMessage, context) => {
    // 回滚到之前的状态
    queryClient.setQueryData(
      ['messages', sessionId],
      context?.previousMessages
    );
    toast.error('消息发送失败');
  },
  onSettled: () => {
    // 重新获取数据以确认
    queryClient.invalidateQueries({ queryKey: ['messages', sessionId] });
  }
});
```

**Verify:** 消息立即显示,发送失败时回滚

---

### Task 8: 创建加载状态指示器

**File:** `src/components/loading/StreamingIndicator.tsx` (已存在,增强)

**Enhancements:**
- 添加进度条(可选)
- 添加"正在思考..."文本
- 改进动画流畅度

**Code:**
```typescript
export function StreamingIndicator({ onStop }: { onStop: () => void }) {
  return (
    <div className="flex items-center gap-2 px-4 py-2 bg-muted/50 rounded">
      <div className="flex gap-1">
        <span className="w-2 h-2 bg-primary rounded-full animate-bounce" style={{ animationDelay: '0ms' }} />
        <span className="w-2 h-2 bg-primary rounded-full animate-bounce" style={{ animationDelay: '150ms' }} />
        <span className="w-2 h-2 bg-primary rounded-full animate-bounce" style={{ animationDelay: '300ms' }} />
      </div>
      <span className="text-sm text-muted-foreground">Claude正在思考...</span>
      <Button size="sm" variant="ghost" onClick={onStop}>
        停止
      </Button>
    </div>
  );
}
```

**Verify:** 流式指示器显示正确

---

### Task 9: 添加页面级骨架屏

**File:** `src/pages/Skeleton.tsx`

```typescript
import { PageHeaderSkeleton } from '@/components/skeletons/PageHeaderSkeleton';
import { SessionListSkeleton } from '@/components/skeletons/SessionListSkeleton';

export function SessionListPageSkeleton() {
  return (
    <div className="container mx-auto">
      <PageHeaderSkeleton />
      <SessionListSkeleton count={8} />
    </div>
  );
}
```

**Verify:** 路由级骨架屏工作

---

### Task 10: 优化Suspense fallback

**File:** `src/App.tsx`

**Current:** 使用Loader2

**After:**
```typescript
import { SessionListPageSkeleton } from '@/pages/Skeleton';

function App() {
  return (
    <Suspense fallback={<SessionListPageSkeleton />}>
      <Routes>
        {/* routes */}
      </Routes>
    </Suspense>
  );
}
```

**Verify:** 路由切换时显示页面级骨架屏

---

## Testing Checklist

- [ ] 会话列表加载时显示骨架屏
- [ ] 消息列表加载时显示骨架屏
- [ ] 骨架屏动画流畅(shimmer效果)
- [ ] 骨架屏匹配实际内容布局
- [ ] 骨架屏在深色/浅色模式下正确显示
- [ ] 发送消息时立即显示(乐观更新)
- [ ] 发送失败时消息回滚并显示错误
- [ ] 页面切换时显示骨架屏
- [ ] 骨架屏在<500ms内消失(加载快时)

---

## Must Haves (Goal-backward Verification)

1. ✅ **骨架屏组件**:SessionListSkeleton, ChatMessageListSkeleton创建并使用
2. ✅ **乐观更新**:消息发送立即显示,失败回滚
3. ✅ **Suspense集成**:lazy路由使用骨架屏作为fallback
4. ✅ **主题适配**:骨架屏在深色/浅色模式下正确显示
5. ✅ **流畅体验**:加载状态转换平滑,无闪烁

---

## Notes

- react-loading-skeleton自动适配主题,使用CSS变量
- 骨架屏应尽可能匹配实际内容布局
- 对于快速加载(<500ms),考虑跳过骨架屏
- 乐观更新适用于大多数操作,但要注意一致性
