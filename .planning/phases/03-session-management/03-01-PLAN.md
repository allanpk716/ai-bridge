---
phase: 03-session-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/router/index.tsx
  - web/src/layouts/MainLayout.tsx
  - web/src/components/Sidebar.tsx
autonomous: true

must_haves:
  truths:
    - User can navigate between session list (/) and session detail (/sessions/:id)
    - Session list route renders SessionList component
    - Session detail route renders SessionDetail component with dynamic id
    - Navigation works without page reloads (client-side routing)
    - Mobile drawer navigation opens session list
  artifacts:
    - path: web/src/router/index.tsx
      provides: Route definitions for / and /sessions/:id
      exports: ["routes", "RouterProvider"]
    - path: web/src/pages/SessionList.tsx
      provides: Session list page component
      min_lines: 20
    - path: web/src/pages/SessionDetail.tsx
      provides: Session detail page component
      min_lines: 20
  key_links:
    - from: web/src/router/index.tsx
      to: web/src/pages/SessionList.tsx
      via: "import and route definition"
      pattern: "import.*SessionList.*from.*pages"
    - from: web/src/router/index.tsx
      to: web/src/pages/SessionDetail.tsx
      via: "dynamic route with :id param"
      pattern: "path.*:.*id"
---

<objective>
Establish client-side routing structure for session navigation between list and detail views.

Purpose: Provides the navigation foundation for all session management features. Without this, users cannot navigate between different session views.

Output: Working React Router setup with routes for session list and session detail pages.
</objective>

<execution_context>
@C:\Users\allan716\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\allan716\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-session-management/03-CONTEXT.md

# Phase 1 Summary (existing router setup)
@.planning/phases/01-foundation-ui-infrastructure/01-02-SUMMARY.md

# Existing files (read to understand current state)
@web/src/router/index.tsx
@web/src/pages/SessionList.tsx
@web/src/pages/SessionDetail.tsx
@web/src/components/Sidebar.tsx
@web/src/layouts/MainLayout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update router configuration with session routes</name>
  <files>web/src/router/index.tsx</files>
  <action>
    Update the router configuration in web/src/router/index.tsx to ensure proper routing:

    1. Keep the existing route structure (/ for SessionList, /sessions/:id for SessionDetail)
    2. Add a navigation utility function useNavigateToSession(sessionId) for programmatic navigation
    3. Add a navigation utility function useNavigateToSessionList() for returning to list
    4. Export these utilities from the router module

    DO NOT change the route paths - they are already defined correctly in Phase 1.
    Only add the navigation utility functions.

    Navigation utilities should:
    - useNavigateToSession(sessionId): navigates to `/sessions/${sessionId}`
    - useNavigateToSessionList(): navigates to `/`
  </action>
  <verify>Check that web/src/router/index.tsx exports navigation utility functions</verify>
  <done>Router configuration includes navigation utilities for session and session list routes</done>
</task>

<task type="auto">
  <name>Task 2: Update SessionList to use navigation utilities</name>
  <files>web/src/pages/SessionList.tsx</files>
  <action>
    Update SessionList.tsx to use the new navigation utilities:

    1. Import useNavigateToSession from router
    2. Store reference for future use (when clicking on session items)
    3. Keep existing UI structure (header with title and New Session button, empty state)
    4. Add a TODO comment for integrating session list data in plan 03-02

    DO NOT add actual session list rendering yet - that's plan 03-02.
    This task only prepares the navigation hooks.
  </action>
  <verify>Check that SessionList.tsx imports and stores navigation utility</verify>
  <done>SessionList component has navigation utility ready for session item clicks</done>
</task>

<task type="auto">
  <name>Task 3: Update SessionDetail to use navigation utilities</name>
  <files>web/src/pages/SessionDetail.tsx</files>
  <action>
    Update SessionDetail.tsx to use the new navigation utilities:

    1. Replace direct useNavigate hook with useNavigateToSessionList utility
    2. Keep existing back button behavior (mobile only, goes to session list)
    3. Keep existing session ID display from URL params
    4. Add a TODO comment for integrating session data fetching in plan 03-06

    DO NOT add actual session data fetching yet - that's plan 03-06.
    This task only updates the navigation to use the new utility.
  </action>
  <verify>Check that SessionDetail.tsx uses useNavigateToSessionList utility</verify>
  <done>SessionDetail component uses navigation utility for back navigation</done>
</task>

</tasks>

<verification>
After completing all tasks, verify:

1. Start dev server: `cd web && npm run dev`
2. Visit http://localhost:3000 - should see SessionList page with "Sessions" title and "New Session" button
3. Check browser console for no errors
4. Navigation utilities should be exported from router module
5. TypeScript compilation succeeds: `cd web && npx tsc --noEmit`
</verification>

<success_criteria>
1. Application routes render correctly for / and /sessions/:id
2. Navigation utilities are available throughout the app
3. No TypeScript errors
4. Navigation works without page reloads
5. Ready for next plan (03-02) to add session list data
</success_criteria>

<output>
After completion, create `.planning/phases/03-session-management/03-01-SUMMARY.md`
</output>
