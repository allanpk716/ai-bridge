---
phase: 03-session-management
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - web/src/components/session/CreateSessionDialog.tsx
  - web/src/components/session/WorkingDirectoryPicker.tsx
  - web/src/components/session/ModelSelector.tsx
  - web/src/pages/SessionList.tsx
  - web/src/components/Sidebar.tsx
autonomous: false

must_haves:
  truths:
    - User can open create session dialog from "New Session" button (list page and sidebar)
    - Dialog shows 4-step wizard (Working Directory -> Model -> Parameters -> Confirm)
    - Step indicator shows current step (e.g., "1/4")
    - Working directory picker supports: manual input, file picker, recent directories dropdown
    - Recent directories persist in localStorage (max 5, deduplicated)
    - Model selection uses card layout with 3 options (Haiku, Sonnet, Opus)
    - Sonnet is selected by default
    - User can navigate steps with Back/Next buttons (sequential only)
    - Each step validates before allowing next
  artifacts:
    - path: web/src/components/session/CreateSessionDialog.tsx
      provides: Multi-step wizard dialog for session creation
      min_lines: 200
    - path: web/src/components/session/WorkingDirectoryPicker.tsx
      provides: Working directory input with file picker and recent directories
      min_lines: 100
    - path: web/src/components/session/ModelSelector.tsx
      provides: Card-based model selection (Haiku/Sonnet/Opus)
      min_lines: 80
  key_links:
    - from: web/src/components/session/CreateSessionDialog.tsx
      to: web/src/lib/api/sessions.ts
      via: "useCreateSession mutation"
      pattern: "useCreateSession"
    - from: web/src/components/session/CreateSessionDialog.tsx
      to: web/src/components/session/WorkingDirectoryPicker.tsx
      via: "import and component usage"
      pattern: "import.*WorkingDirectoryPicker"
    - from: web/src/components/session/CreateSessionDialog.tsx
      to: web/src/components/session/ModelSelector.tsx
      via: "import and component usage"
      pattern: "import.*ModelSelector"
    - from: web/src/components/session/WorkingDirectoryPicker.tsx
      to: window.localStorage
      via: "recent directories persistence"
      pattern: "localStorage.*recent"
---

<objective>
Build a 4-step wizard dialog for creating new sessions with working directory picker, model selection, and validation.

Purpose: Provides the primary interface for users to create new Claude Code sessions with proper configuration.

Output: Fully functional create session dialog with multi-step wizard, directory picker, model selection, and validation.
</objective>

<execution_context>
@C:\Users\allan716\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\allan716\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-session-management/03-CONTEXT.md
@.planning/phases/03-session-management/03-01-PLAN.md

# Existing API hooks
@web/src/lib/api/sessions.ts
@web/src/types/api.ts

# Existing UI components
@web/src/components/ui/dialog.tsx
@web/src/components/ui/button.tsx
@web/src/components/ui/input.tsx
@web/src/components/ui/card.tsx
@web/src/components/ui/badge.tsx
@web/src/components/ui/tooltip.tsx

# Existing pages
@web/src/pages/SessionList.tsx
@web/src/components/Sidebar.tsx
@web/src/router/index.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create WorkingDirectoryPicker component</name>
  <files>web/src/components/session/WorkingDirectoryPicker.tsx</files>
  <action>
    Create WorkingDirectoryPicker component at web/src/components/session/WorkingDirectoryPicker.tsx:

    Props:
    - value: string (current directory path)
    - onChange: (path: string) => void
    - error: string | null (validation error)
    - recentDirectories: string[] (from localStorage)
    - onRecentDirectorySelect: (path: string) => void

    Features (per CONTEXT.md):
    1. Manual text input (Input component) with placeholder "Select working directory..."
    2. "Browse" button that triggers:
       - On desktop: Use <input type="file" webkitdirectory directory> for folder picker
       - Show folder picker dialog
    3. Recent directories dropdown (Combobox or simple select):
       - Show last 5 used directories (from localStorage)
       - Deduplicated, sorted by recency
       - Format: show last folder name, tooltip shows full path
    4. Drag and drop zone (shows when dragging folder over)
    5. Real-time validation:
       - Check path not empty
       - Check path validity (basic format check)
       - Non-Git repos: silent (no warning)
       - No permissions: show error (block next)

    Git detection (async):
    - When path selected, check if it's a git repo
    - If git repo, detect branch (store for display in confirm step)
    - Use metadata object to store: { path, isGit, branch, hasPermission }

    localStorage key: "ai-bridge.recent-directories"
    Format: JSON array of strings

    Use lucide-react icons: FolderOpen, Clock, AlertCircle, GitBranch
  </action>
  <verify>Check that WorkingDirectoryPicker.tsx exists with all required features</verify>
  <done>WorkingDirectoryPicker supports manual input, file picker, recent directories, and validation</done>
</task>

<task type="auto">
  <name>Task 2: Create ModelSelector component</name>
  <files>web/src/components/session/ModelSelector.tsx</files>
  <action>
    Create ModelSelector component at web/src/components/session/ModelSelector.tsx:

    Props:
    - value: "haiku" | "sonnet" | "opus" | null
    - onChange: (model: "haiku" | "sonnet" | "opus") => void

    Model cards (per CONTEXT.md):
    1. Haiku:
       - Name: "Haiku"
       - Description: "Fast and efficient for quick tasks"
       - Use case: "Best for simple queries and rapid iteration"
    2. Sonnet (default):
       - Name: "Sonnet"
       - Description: "Balanced performance for most tasks"
       - Use case: "Recommended for general-purpose coding"
    3. Opus:
       - Name: "Opus"
       - Description: "Most capable for complex tasks"
       - Use case: "Best for complex problem-solving"

    Card layout (Card component):
    - Grid layout (3 columns on desktop, 1 on mobile)
    - Each card is clickable
    - Selected card has border/accent color
    - Hover effect on cards
    - No pricing info per CONTEXT.md

    Default: Sonnet selected
  </action>
  <verify>Check that ModelSelector.tsx exists with 3 model cards and Sonnet as default</verify>
  <done>ModelSelector displays 3 model cards in selectable grid layout</done>
</task>

<task type="auto">
  <name>Task 3: Create CreateSessionDialog component (wizard)</name>
  <files>web/src/components/session/CreateSessionDialog.tsx</files>
  <action>
    Create CreateSessionDialog component at web/src/components/session/CreateSessionDialog.tsx:

    Props:
    - open: boolean
    - onOpenChange: (open: boolean) => void

    State management:
    - currentStep: 1 | 2 | 3 | 4
    - workingDir: string
    - workingDirMetadata: { isGit: boolean, branch: string | null, hasPermission: boolean }
    - model: "haiku" | "sonnet" | "opus" (default: "sonnet")
    - sessionName: string (auto-generated, editable)
    - cliParams: { dangerouslySkipPermissions: boolean, permissionMode: string, diff: boolean }

    4 steps (per CONTEXT.md):
    1. Working Directory: Render WorkingDirectoryPicker
    2. Model Selection: Render ModelSelector
    3. CLI Parameters: Form with switches/inputs (detailed in plan 03-04)
       - For now, create placeholder for parameters
    4. Confirm: Summary of all selections, allow back to edit

    UI structure:
    - Step indicator at top: "Step N of 4" or "1/4 • 2/4 • 3/4 • 4/4" style
    - Dialog content area shows current step
    - Dialog footer: Back button (disabled on step 1), Next/Create button (text changes)
    - Validation: Each step validates before enabling Next

    Navigation rules:
    - Sequential only (no skipping steps)
    - Back always available (except step 1)
    - Next validates current step before proceeding
    - Create button triggers useCreateSession mutation

    On success:
    - Show success toast
    - Close dialog
    - Navigate to new session (useNavigateToSession)
    - Save working directory to recent directories (localStorage)

    Use lucide-react icons: ChevronLeft, ChevronRight, Check
  </action>
  <verify>Check that CreateSessionDialog.tsx exists with 4-step wizard structure</verify>
  <done>CreateSessionDialog implements multi-step wizard with validation and navigation</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete create session dialog with 4-step wizard (working directory picker, model selector, CLI parameters placeholder, confirm step)</what-built>
  <how-to-verify>
    1. Start dev server: `cd web && npm run dev`
    2. Visit http://localhost:3000
    3. Click "New Session" button
    4. Verify dialog opens with step indicator "1/4"
    5. Test working directory picker:
       - Type a path manually
       - Click Browse button (test folder picker)
       - Select from recent directories dropdown
    6. Click Next to go to step 2 (should validate path is not empty)
    7. Verify model selector shows 3 cards (Haiku, Sonnet, Opus)
    8. Click Sonnet card (should be selected by default)
    9. Click Next to go to step 3 (CLI parameters placeholder)
    10. Click Next to go to step 4 (confirm)
    11. Verify confirm step shows all selections
    12. Click Back to return to previous steps
    13. Try clicking Create (will fail without backend, but should show validation)

    Verify:
    - Step indicator updates correctly
    - Back/Next buttons work as expected
    - Validation prevents empty working directory
    - Model selection visual feedback works
    - Dialog can be closed with Escape or X button
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Wire up create dialog in SessionList and Sidebar</name>
  <files>web/src/pages/SessionList.tsx, web/src/components/Sidebar.tsx</files>
  <action>
    Connect CreateSessionDialog to trigger buttons:

    1. In SessionList.tsx:
       - Import CreateSessionDialog
       - Add state: dialogOpen, setDialogOpen
       - Wire "New Session" button onClick to open dialog
       - Render CreateSessionDialog component

    2. In Sidebar.tsx:
       - Import CreateSessionDialog
       - Add state: dialogOpen, setDialogOpen
       - Add "New Session" button in sidebar footer (below theme toggle)
       - Use Plus icon, button text "New Session" or icon-only on mobile
       - Wire button onClick to open dialog
       - Render CreateSessionDialog component

    Both components should have their own dialog instance state.
  </action>
  <verify>Check that both SessionList.tsx and Sidebar.tsx import and render CreateSessionDialog</verify>
  <done>Create session dialog opens from both SessionList "New Session" button and Sidebar button</done>
</task>

</tasks>

<verification>
After completing all tasks and checkpoint approval:

1. TypeScript compilation succeeds: `cd web && npx tsc --noEmit`
2. Dialog opens from both locations (SessionList page and Sidebar)
3. All 4 steps are accessible with proper navigation
4. Working directory picker validates input
5. Model selector defaults to Sonnet
6. Recent directories persist in localStorage
7. Step indicator shows correct step number
</verification>

<success_criteria>
1. Create session dialog with 4-step wizard is fully functional
2. Working directory picker supports all input methods (manual, browse, recent)
3. Model selector shows 3 models with Sonnet as default
4. Step navigation is sequential with validation
5. Dialog opens from SessionList and Sidebar
6. Recent directories persist across sessions
7. Ready for plan 03-04 to add CLI parameters form
</success_criteria>

<output>
After completion, create `.planning/phases/03-session-management/03-03-SUMMARY.md`
</output>
