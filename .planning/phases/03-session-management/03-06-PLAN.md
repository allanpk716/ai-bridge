---
phase: 03-session-management
plan: 06
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - web/src/lib/api/sessions.ts
  - web/src/pages/SessionDetail.tsx
  - web/src/components/session/SessionHeader.tsx
  - web/src/components/session/ResumeSessionDialog.tsx
  - web/src/components/session/SessionMetadata.tsx
autonomous: false

must_haves:
  truths:
    - User can view session detail page with session metadata
    - Session detail displays: session name, status, working directory, model, git branch, message count
    - User can resume stopped session with options (--continue, --resume, or new session)
    - Resume dialog shows three options with descriptions
    - Resume with --continue continues existing session
    - Resume with --resume restores session state
    - "Start new" creates fresh session with same working directory
    - User can delete session from detail page
    - Deleting from detail page navigates back to session list
    - Stopped sessions show resume button
    - Running sessions show status indicator
  artifacts:
    - path: web/src/lib/api/sessions.ts
      provides: fetchSession (single session), useSession hook, resumeSession, useResumeSession
      exports: ["fetchSession", "useSession", "resumeSession", "useResumeSession"]
      min_lines: 50 (added)
    - path: web/src/components/session/SessionHeader.tsx
      provides: Session detail header with status, actions, and metadata
      min_lines: 80
    - path: web/src/components/session/SessionMetadata.tsx
      provides: Session metadata display (working dir, model, git info, message count)
      min_lines: 60
    - path: web/src/components/session/ResumeSessionDialog.tsx
      provides: Resume options dialog (--continue, --resume, new session)
      min_lines: 80
    - path: web/src/pages/SessionDetail.tsx
      provides: Updated session detail page with data fetching and actions
      min_lines: 120 (updated)
  key_links:
    - from: web/src/pages/SessionDetail.tsx
      to: web/src/lib/api/sessions.ts
      via: "useSession hook"
      pattern: "useSession"
    - from: web/src/components/session/SessionHeader.tsx
      to: web/src/components/session/ResumeSessionDialog.tsx
      via: "resume button and dialog state"
      pattern: "resumeDialogOpen|onResume"
    - from: web/src/lib/api/sessions.ts
      to: "/api/v1/sessions/:id"
      via: "GET request for single session"
      pattern: "GET.*sessions.*:id"
---

<objective>
Build the session detail page with metadata display, resume functionality for stopped sessions, and session actions.

Purpose: Provides the detailed view for individual sessions where users can see session information, resume stopped sessions, and manage the session.

Output: Fully functional session detail page with metadata display, resume options, and session management actions.
</objective>

<execution_context>
@C:\Users\allan716\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\allan716\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-session-management/03-CONTEXT.md
@.planning/phases/03-session-management/03-01-PLAN.md

# Existing API service
@web/src/lib/api/sessions.ts

# Existing UI components
@web/src/components/ui/dialog.tsx
@web/src/components/ui/button.tsx
@web/src/components/ui/badge.tsx
@web/src/components/ui/card.tsx

# Existing pages
@web/src/pages/SessionDetail.tsx
@web/src/router/index.tsx

# Types
@web/src/types/api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add single session API functions</name>
  <files>web/src/lib/api/sessions.ts</files>
  <action>
    Add single session and resume functionality to web/src/lib/api/sessions.ts:

    1. Create fetchSession function:
       - Accepts sessionId: string
       - Makes GET request to /api/v1/sessions/:sessionId
       - Returns Session object
       - Validates with SessionSchema

    2. Create useSession hook:
       - Accepts sessionId: string
       - Uses useQuery with queryKey ['session', sessionId]
       - Enabled only when sessionId is defined
       - 5-second stale time
       - Returns UseQueryResult<Session, Error>

    3. Create resumeSession function:
       - Accepts sessionId: string and mode: 'continue' | 'resume' | 'new'
       - Makes POST request to /api/v1/sessions/:sessionId/resume
       - Body: { mode }
       - Returns Session object (new or resumed session)
       - Note: Backend API may differ - adjust based on actual endpoint

    4. Create useResumeSession hook:
       - Uses useMutation with resumeSession as mutationFn
       - onSuccess: Invalidate sessions query
       - onSuccess: Navigate to new/resumed session
       - onError: Show error toast

    Export all new functions and hooks.
  </action>
  <verify>Check that sessions.ts exports fetchSession, useSession, resumeSession, useResumeSession</verify>
  <done>Single session fetch and resume API functions are available</done>
</task>

<task type="auto">
  <name>Task 2: Create SessionMetadata component</name>
  <files>web/src/components/session/SessionMetadata.tsx</files>
  <action>
    Create SessionMetadata component at web/src/components/session/SessionMetadata.tsx:

    Props:
    - session: Session

    Display sections (Card-based layout):
    1. Status:
       - Badge component with status color
       - Same variants as list: idle→default, processing→secondary, waiting→outline, stopped→destructive

    2. Working Directory:
       - Label: "Working Directory"
       - Value: Last folder name (bold), full path in subtitle
       - Icon: Folder

    3. Model:
       - Label: "Model"
       - Value: Badge with model name (from metadata.model or "Default")
       - Icon: Cpu or similar

    4. Git Information (if available):
       - Label: "Git Branch"
       - Value: Branch name from metadata.gitBranch
       - Icon: GitBranch
       - If not git repo: "Not a git repository" in muted text

    5. Message Count (if available):
       - Label: "Messages"
       - Value: Count from metadata.messageCount
       - Icon: MessageSquare

    6. Created At:
       - Label: "Created"
       - Value: Formatted date/time (relative if < 24h)
       - Icon: Calendar

    Layout:
    - Grid layout (2 columns on desktop, 1 on mobile)
    - Each section in a Card sub-component
    - Consistent icon usage

    Use lucide-react icons: Folder, GitBranch, MessageSquare, Calendar, Cpu
  </action>
  <verify>Check that SessionMetadata.tsx exists and displays all session info</verify>
  <done>SessionMetadata displays session information in card layout</done>
</task>

<task type="auto">
  <name>Task 3: Create ResumeSessionDialog component</name>
  <files>web/src/components/session/ResumeSessionDialog.tsx</files>
  <action>
    Create ResumeSessionDialog component at web/src/components/session/ResumeSessionDialog.tsx:

    Props:
    - open: boolean
    - onOpenChange: (open: boolean) => void
    - session: Session
    - onResume: (mode: 'continue' | 'resume' | 'new') => void
    - isResuming: boolean

    Dialog content (per CONTEXT.md):
    - Title: "Resume Session"

    Three options (Card-based selection):

    1. Continue (--continue):
       - Title: "Continue"
       - Description: "Continue this session where it left off"
       - Icon: Play
       - Primary action (recommended)

    2. Resume (--resume):
       - Title: "Resume"
       - Description: "Restore session to previous state"
       - Icon: RotateCcw

    3. Start New:
       - Title: "Start New Session"
       - Description: "Create a new session with the same working directory"
       - Icon: Plus
       - Note: Will navigate to create dialog with pre-filled directory

    Actions:
    - Each option is clickable
    - Selected option has visual highlight
    - Confirm button at bottom triggers selected action
    - Cancel button closes dialog

    On confirm:
    - Call onResume(mode) with selected mode
    - Show loading state if isResuming

    Use lucide-react icons: Play, RotateCcw, Plus, Check
  </action>
  <verify>Check that ResumeSessionDialog.tsx exists with three resume options</verify>
  <done>ResumeSessionDialog shows continue, resume, and new session options</done>
</task>

<task type="auto">
  <name>Task 4: Create SessionHeader component</name>
  <files>web/src/components/session/SessionHeader.tsx</files>
  <action>
    Create SessionHeader component at web/src/components/session/SessionHeader.tsx:

    Props:
    - session: Session
    - onResume: () => void
    - onDelete: () => void
    - isResuming: boolean
    - isDeleting: boolean
    - onBack: () => void (for mobile)

    Component structure:
    1. Back button (mobile only):
       - Left side
       - Ghost variant
       - ArrowLeft icon

    2. Title area:
       - Session name (metadata.name or truncated id)
       - Session ID in muted text

    3. Action buttons (right side):
       - Resume button (if status is stopped):
         - Primary variant
         - Play icon
         - Text: "Resume"
       - Stop button (if status is processing/waiting):
         - Destructive variant
         - Square icon
         - Text: "Stop"
       - Delete button (always):
         - Ghost variant with destructive hover
         - Trash icon
         - Text: "Delete" (desktop) or icon only (mobile)

    Responsive behavior:
       - Desktop: All buttons visible with text
       - Mobile: Icon-only buttons or dropdown menu

    Use lucide-react icons: ArrowLeft, Play, Square, Trash, MoreVertical
  </action>
  <verify>Check that SessionHeader.tsx exists with session title and action buttons</verify>
  <done>SessionHeader displays session info and context-aware action buttons</done>
</task>

<task type="auto">
  <name>Task 5: Update SessionDetail page with data fetching and components</name>
  <files>web/src/pages/SessionDetail.tsx</files>
  <action>
    Update SessionDetail.tsx to implement full session detail functionality:

    1. Import hooks and components:
       - useSession from API
       - useResumeSession, useDeleteSession from API
       - useNavigateToSessionList from router
       - SessionHeader, SessionMetadata, ResumeSessionDialog

    2. Add state:
       - resumeDialogOpen: boolean
       - deleteDialogOpen: boolean
       - deleteConfirmation: Session | null

    3. Fetch session data:
       - Use useSession(id) hook
       - Handle loading state: Show skeleton or spinner
       - Handle error state: Show error with retry/back options
       - Handle not found: Navigate back to list with toast

    4. Replace existing placeholder with:
       - SessionHeader component at top
       - SessionMetadata component below header
       - Messages placeholder (ready for Phase 4)

    5. Wire up actions:
       - Resume button: Opens resume dialog
       - Delete button: Opens delete confirmation
       - Resume dialog confirm: Calls useResumeSession.mutate()
       - Delete confirm: Calls useDeleteSession.mutate()

    6. Handle navigation:
       - On delete success: Navigate back to session list
       - On resume success: Navigate to resumed session (or stay if same session)
       - Start new: Navigate to create dialog with pre-filled working directory

    7. Error handling:
       - Show error toast on resume failure
       - Show error toast on delete failure
       - Keep dialogs open on failure for retry

    Loading state: Full-page skeleton loader
    Error state: Error message with "Go Back" button
  </action>
  <verify>Check that SessionDetail.tsx uses useSession and renders new components</verify>
  <done>SessionDetail page fetches session data and displays with full functionality</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete session detail page with metadata display, resume functionality, and session management actions</what-built>
  <how-to-verify>
    1. Start dev server: `cd web && npm run dev`
    2. Visit http://localhost:3000
    3. Click on a session in the list (or navigate to /sessions/{id} directly)

    Verify session detail page:
    4. Page title shows session name/ID
    5. Back button works (mobile)
    6. Session metadata displays all info:
       - Status badge (correct color)
       - Working directory
       - Model
       - Git info (if applicable)
       - Message count
       - Created date

    Test resume (for stopped sessions):
    7. Click "Resume" button
    8. Verify resume dialog opens with 3 options
    9. Select each option and verify description
    10. Click "Continue" to attempt resume

    Test delete:
    11. Click "Delete" button
    12. Verify confirmation dialog appears
    13. Verify dialog shows session details
    14. Cancel and verify dialog closes

    Test navigation:
    15. Click back button (mobile)
    16. Verify navigates to session list

    Test error states (if no backend):
    17. Verify loading state shows while fetching
    18. Verify error state shows on fetch failure
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

<task type="auto">
  <name>Task 6: Add stop session functionality for running sessions</name>
  <files>web/src/lib/api/sessions.ts</files>
  <action>
    Add stop session functionality to web/src/lib/api/sessions.ts:

    1. Create stopSession function:
       - Accepts sessionId: string
       - Makes POST request to /api/v1/sessions/:sessionId/stop
       - Returns updated Session object
       - Note: Backend API may differ - adjust based on actual endpoint

    2. Create useStopSession hook:
       - Uses useMutation with stopSession as mutationFn
       - onSuccess: Invalidate session and sessions queries
       - onSuccess: Show success toast
       - onError: Show error toast

    3. Update SessionHeader to handle stop action:
       - Wire up stop button to useStopSession
       - Disable button while stopping
       - Show loading state

    Export stopSession and useStopSession.
  </action>
  <verify>Check that sessions.ts exports stopSession and useStopSession</verify>
  <done>Stop session functionality is available for running sessions</done>
</task>

</tasks>

<verification>
After completing all tasks and checkpoint approval:

1. TypeScript compilation succeeds: `cd web && npx tsc --noEmit`
2. Session detail page loads with session data
3. All metadata displays correctly
4. Resume dialog shows three options
5. Delete confirmation shows session details
6. Stop button works for running sessions
7. Navigation back to list works
8. Error states display appropriately
9. Loading states show during operations
</verification>

<success_criteria>
1. Session detail page displays all session metadata
2. Resume functionality works for stopped sessions (3 options)
3. Stop functionality works for running sessions
4. Delete from detail page navigates back to list
5. Error handling shows user-friendly messages
6. Loading states prevent double-actions
7. Back navigation works correctly
8. Ready for Phase 4 (Real-Time Chat)
</success_criteria>

<output>
After completion, create `.planning/phases/03-session-management/03-06-SUMMARY.md`
</output>
