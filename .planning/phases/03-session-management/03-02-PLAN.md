---
phase: 03-session-management
plan: 02
type: execute
wave: 1
depends_on: ["03-01"]
files_modified:
  - web/src/pages/SessionList.tsx
  - web/src/components/session/SessionListItem.tsx
  - web/src/components/session/SessionListFilters.tsx
autonomous: true

must_haves:
  truths:
    - User can see all sessions in a list with status indicators (Idle/Processing/Waiting/Stopped)
    - Each session item displays: session name/ID, working directory, status badge, message count, last activity time
    - Session status badges use correct variants: idle→default, processing→secondary, waiting→outline, stopped→destructive
    - Status badges include Lucide icons (Loader for processing, Circle for idle)
    - List updates when sessions change (via TanStack Query refetch)
    - Loading state shows spinner while fetching sessions
    - Empty state shows message when no sessions exist
  artifacts:
    - path: web/src/components/session/SessionListItem.tsx
      provides: Individual session list item component with status badge and metadata
      min_lines: 60
    - path: web/src/components/session/SessionListFilters.tsx
      provides: Filter controls (search by name, filter by status, sort options)
      min_lines: 40
    - path: web/src/pages/SessionList.tsx
      provides: Updated session list page with data fetching and rendering
      min_lines: 80
  key_links:
    - from: web/src/pages/SessionList.tsx
      to: web/src/lib/api/sessions.ts
      via: "useSessions hook"
      pattern: "useSessions"
    - from: web/src/components/session/SessionListItem.tsx
      to: web/src/types/api.ts
      via: "Session type import"
      pattern: "import.*Session.*from.*types/api"
    - from: web/src/components/session/SessionListItem.tsx
      to: web/src/components/ui/badge.tsx
      via: "Badge component for status display"
      pattern: "import.*Badge"
---

<objective>
Build the session list UI with real data fetching, status indicators, metadata display, and filtering capabilities.

Purpose: This is the primary interface for users to view and navigate their sessions. The list displays all critical session information at a glance.

Output: Fully functional session list with real-time data, status badges, filtering, and search.
</objective>

<execution_context>
@C:\Users\allan716\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\allan716\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-session-management/03-CONTEXT.md
@.planning/phases/03-session-management/03-01-PLAN.md

# Existing API hooks from Phase 2
@web/src/lib/api/sessions.ts
@web/src/types/api.ts

# Existing UI components
@web/src/components/ui/badge.tsx
@web/src/components/ui/card.tsx
@web/src/components/ui/button.tsx
@web/src/components/ui/input.tsx

# Existing pages
@web/src/pages/SessionList.tsx
@web/src/router/index.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SessionListItem component</name>
  <files>web/src/components/session/SessionListItem.tsx</files>
  <action>
    Create SessionListItem component at web/src/components/session/SessionListItem.tsx:

    Props:
    - session: Session object
    - onClick: () => void callback for navigation

    Component structure:
    1. Card/row layout with hover effect
    2. Primary area (clickable):
       - Session name (use metadata.name or truncated id)
       - Working directory path (from metadata.workingDir, show last folder name, full path on hover)
       - Metadata row: message count icon + number, last activity time (relative if < 24h, absolute otherwise)
    3. Status badge (from CONTEXT.md):
       - idle: default variant, Circle icon, text "Idle"
       - processing: secondary variant, Loader icon (animated), text "Processing"
       - waiting: outline variant, Clock icon, text "Waiting"
       - stopped: destructive variant, X icon, text "Stopped"
    4. Model badge (from metadata.model or "Default")
    5. Git branch (from metadata.gitBranch, if present)

    Styling per CONTEXT.md:
    - Compact list row layout, medium density
    - Desktop: list items with hover state
    - Mobile: card layout
    - Status badges with icon + text (English)

    Use lucide-react icons: Circle, Loader, Clock, X, MessageSquare, Clock as icons.
  </action>
  <verify>Check that SessionListItem.tsx exists and exports SessionListItem component</verify>
  <done>SessionListItem component renders session data with status badge and metadata</done>
</task>

<task type="auto">
  <name>Task 2: Create SessionListFilters component</name>
  <files>web/src/components/session/SessionListFilters.tsx</files>
  <action>
    Create SessionListFilters component at web/src/components/session/SessionListFilters.tsx:

    Props:
    - searchQuery: string
    - onSearchChange: (query: string) => void
    - statusFilter: "all" | "running" | "stopped"
    - onStatusFilterChange: (filter: "all" | "running" | "stopped") => void
    - sortBy: "lastActivity" | "createdAt"
    - onSortChange: (sort: "lastActivity" | "createdAt") => void

    Component structure:
    1. Search input (with Search icon from lucide-react)
       - Placeholder: "Search sessions..."
       - Filters by session name/id
    2. Status filter dropdown or tabs (all/running/stopped)
       - running: sessions with status processing or waiting
       - stopped: sessions with status stopped
    3. Sort dropdown (Last activity / Created time)

    Use shadcn/ui Input for search field.
    Use simple button group for status filter (all/running/stopped).
  </action>
  <verify>Check that SessionListFilters.tsx exists and exports SessionListFilters component</verify>
  <done>SessionListFilters component provides search, status filter, and sort controls</done>
</task>

<task type="auto">
  <name>Task 3: Update SessionList page with data fetching and list rendering</name>
  <files>web/src/pages/SessionList.tsx</files>
  <action>
    Update SessionList.tsx to implement full session list functionality:

    1. Import and use useSessions() hook from web/src/lib/api/sessions.ts
    2. Add local state for filters: searchQuery, statusFilter, sortBy
    3. Implement filtering logic:
       - Filter by search query (matches session name/id)
       - Filter by status (all/running/stopped)
       - Sort by lastActivity or createdAt (from metadata or use createdAt as fallback)
    4. Replace empty state section with conditional rendering:
       - isLoading: Show loading spinner (use lucide-react Loader2 with animation)
       - error: Show error message with retry option
       - empty (no sessions): Show "No sessions yet" message
       - has sessions: Show SessionListItem components
    5. Add SessionListFilters component above the list
    6. Wire up SessionListItem onClick to useNavigateToSession utility

    Loading state: Center the spinner in the list area
    Error state: Show error message and retry button that calls refetch()
  </action>
  <verify>Check that SessionList.tsx imports useSessions and renders SessionListItem components</verify>
  <done>SessionList page fetches and displays sessions with filtering and search</done>
</task>

</tasks>

<verification>
After completing all tasks, verify:

1. Start dev server: `cd web && npm run dev`
2. Visit http://localhost:3000
3. Check that session list renders (will be empty if backend not running - that's OK)
4. Verify no TypeScript errors: `cd web && npx tsc --noEmit`
5. Test filters (search, status, sort) work with UI controls
6. Check console for no runtime errors
</verification>

<success_criteria>
1. Session list displays sessions from API with all metadata
2. Status badges use correct colors and icons per CONTEXT.md
3. Filters (search, status, sort) update the displayed list
4. Loading state shows during data fetch
5. Error state handles API failures gracefully
6. Empty state shows when no sessions exist
7. Clicking a session item is ready for navigation (will work when plan 03-01 navigation is wired)
</success_criteria>

<output>
After completion, create `.planning/phases/03-session-management/03-02-SUMMARY.md`
</output>
