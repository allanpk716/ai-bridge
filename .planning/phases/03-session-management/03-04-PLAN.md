---
phase: 03-session-management
plan: 04
type: execute
wave: 2
depends_on: ["03-03"]
files_modified:
  - web/src/components/session/CreateSessionDialog.tsx
  - web/src/components/session/CliParametersForm.tsx
  - web/src/components/session/ConfirmStep.tsx
autonomous: true

must_haves:
  truths:
    - User can configure session name (auto-generated based on time or directory name)
    - User can toggle --dangerously-skip-permissions switch
    - User can select --permission-mode from dropdown (normal/strict)
    - User can toggle --diff switch
    - Advanced parameters are displayed by default (expandable section)
    - Each parameter has tooltip with help icon (question mark)
    - Confirm step shows configuration summary with all selections
    - User can click Back from confirm step to modify any previous step
  artifacts:
    - path: web/src/components/session/CliParametersForm.tsx
      provides: CLI parameters configuration form with switches and tooltips
      min_lines: 100
    - path: web/src/components/session/ConfirmStep.tsx
      provides: Confirmation step showing all configuration summary
      min_lines: 80
    - path: web/src/components/session/CreateSessionDialog.tsx
      provides: Updated dialog with parameters form and confirm step
      min_lines: 250 (updated)
  key_links:
    - from: web/src/components/session/CliParametersForm.tsx
      to: web/src/components/ui/tooltip.tsx
      via: "Tooltip component for parameter help"
      pattern: "import.*Tooltip"
    - from: web/src/components/session/ConfirmStep.tsx
      to: web/src/components/session/CreateSessionDialog.tsx
      via: "props for configuration data"
      pattern: "config|selections|summary"
---

<objective>
Complete the CLI parameters form and confirm step for the create session wizard.

Purpose: Allows users to configure CLI startup parameters and review all selections before creating the session.

Output: Fully functional parameters form with tooltips and confirmation step showing configuration summary.
</objective>

<execution_context>
@C:\Users\allan716\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\allan716\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-session-management/03-CONTEXT.md
@.planning/phases/03-session-management/03-03-PLAN.md

# Existing dialog component (to be updated)
@web/src/components/session/CreateSessionDialog.tsx

# Existing UI components
@web/src/components/ui/tooltip.tsx
@web/src/components/ui/switch.tsx (may need to be added if not exists)
@web/src/components/ui/label.tsx (may need to be added if not exists)

# API types
@web/src/types/api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add missing shadcn/ui components (Switch, Label)</name>
  <files>web/src/components/ui/switch.tsx, web/src/components/ui/label.tsx</files>
  <action>
    Check if Switch and Label components exist. If not, add them using shadcn/ui CLI:

    First check what exists:
    ```bash
    cd web && npx shadcn@latest add switch label --yes
    ```

    Or manually create the components if the CLI fails:
    - Create web/src/components/ui/switch.tsx (based on shadcn/ui switch component)
    - Create web/src/components/ui/label.tsx (based on shadcn/ui label component)

    Switch should have:
    - Base styles for unchecked/checked states
    - Focus ring for accessibility
    - Smooth transition animation

    Label should have:
    - Typography styles for form labels
    - Proper htmlFor association with inputs
  </action>
  <verify>Check that switch.tsx and label.tsx exist in web/src/components/ui/</verify>
  <done>Switch and Label components are available for form controls</done>
</task>

<task type="auto">
  <name>Task 2: Create CliParametersForm component</name>
  <files>web/src/components/session/CliParametersForm.tsx</files>
  <action>
    Create CliParametersForm component at web/src/components/session/CliParametersForm.tsx:

    Props:
    - sessionName: string
    - onSessionNameChange: (name: string) => void
    - dangerouslySkipPermissions: boolean
    - onDangerouslySkipPermissionsChange: (value: boolean) => void
    - permissionMode: "normal" | "strict"
    - onPermissionModeChange: (mode: "normal" | "strict") => void
    - diff: boolean
    - onDiffChange: (value: boolean) => void
    - errors: Record<string, string>

    Common parameters (always visible):
    1. Session name (Input):
       - Auto-generated: Use folder name or "Session {timestamp}"
       - Editable text input
       - Help tooltip: "A name to identify this session"

    2. --dangerously-skip-permissions (Switch + Label):
       - Default: false (off)
       - Help tooltip: "Skip all permission prompts. Use with caution."

    3. --permission-mode (Select/Radio):
       - Options: "normal", "strict"
       - Default: "normal"
       - Help tooltip: "normal: prompt for permissions; strict: require explicit approval"

    4. --diff (Switch + Label):
       - Default: false (off)
       - Help tooltip: "Show diff output when files are modified"

    Advanced parameters (expandable, default expanded per CONTEXT.md):
    - Collapsible section with "Advanced" header
    - Currently empty (reserved for future use)
    - Contains message about "More options coming soon"

    Tooltip implementation:
    - Use Question icon from lucide-react next to each parameter label
    - Wrap in Tooltip component from shadcn/ui
    - Tooltip content shows help text

    Layout:
    - Vertical stack of form groups
    - Each group: Label + Help Icon + Control (Switch/Input/Select)
    - Spacing between groups

    Use lucide-react icons: HelpCircle, ChevronDown, ChevronUp
  </action>
  <verify>Check that CliParametersForm.tsx exists with all parameter controls and tooltips</verify>
  <done>CliParametersForm displays all CLI parameters with help tooltips</done>
</task>

<task type="auto">
  <name>Task 3: Create ConfirmStep component</name>
  <files>web/src/components/session/ConfirmStep.tsx</files>
  <action>
    Create ConfirmStep component at web/src/components/session/ConfirmStep.tsx:

    Props:
    - workingDir: string
    - model: "haiku" | "sonnet" | "opus"
    - sessionName: string
    - cliParams: {
        dangerouslySkipPermissions: boolean
        permissionMode: string
        diff: boolean
      }
    - gitInfo?: { branch: string }
    - onEdit: (step: number) => void (callback to jump to specific step)

    Display sections:
    1. "Working Directory"
       - Show path (last folder name bold, full path in subtitle)
       - Edit button (clicks jumps to step 1)

    2. "Model"
       - Show model name with badge styling
       - Edit button (clicks jumps to step 2)

    3. "Session Name"
       - Show session name
       - Edit button (clicks jumps to step 3)

    4. "CLI Parameters"
       - List enabled parameters as badges
       - Show --dangerously-skip-permissions if enabled
       - Show --permission-mode value
       - Show --diff if enabled
       - Edit button (clicks jumps to step 3)

    5. "Git Information" (if git repo)
       - Show branch name with GitBranch icon
       - "Not a git repository" message if not git

    Styling:
    - Card-based layout for each section
    - Edit buttons use Ghost variant
    - Summary header: "Review your session configuration"
    - Create button in dialog footer

    Use lucide-react icons: GitBranch, Pencil, Check
  </action>
  <verify>Check that ConfirmStep.tsx exists with configuration summary display</verify>
  <done>ConfirmStep shows all configuration with edit buttons for each section</done>
</task>

<task type="auto">
  <name>Task 4: Update CreateSessionDialog to use new components</name>
  <files>web/src/components/session/CreateSessionDialog.tsx</files>
  <action>
    Update CreateSessionDialog.tsx to integrate CliParametersForm and ConfirmStep:

    1. Import CliParametersForm and ConfirmStep

    2. Update state to include:
       - sessionName with auto-generation logic
       - All CLI parameters state

    3. Replace step 3 placeholder with CliParametersForm:
       - Pass all state props
       - Handle onChange callbacks
       - Validate before allowing next

    4. Replace step 4 placeholder with ConfirmStep:
       - Pass all configuration data
       - Handle onEdit to jump to specific step

    5. Update step 2 (model) validation:
       - Ensure model is selected before allowing next

    6. Update create button logic:
       - On step 4, button text is "Create Session"
       - On create, call useCreateSession.mutate() with full config
       - Include workingDir, model, and CLI params in request

    7. Auto-generate session name:
       - Default: Folder name from workingDir or "Session {timestamp}"
       - Format: "Session YYYY-MM-DD HH:MM" if no folder name

    8. Handle create success:
       - Close dialog
       - Navigate to new session page
       - Save working directory to recent directories localStorage
  </action>
  <verify>Check that CreateSessionDialog.tsx imports and uses CliParametersForm and ConfirmStep</verify>
  <done>CreateSessionDialog has complete 4-step wizard with parameters and confirmation</done>
</task>

</tasks>

<verification>
After completing all tasks, verify:

1. TypeScript compilation succeeds: `cd web && npx tsc --noEmit`
2. Open create session dialog
3. Navigate through all 4 steps
4. Step 3 (Parameters): Verify all controls work
   - Toggle switches update state
   - Session name is auto-generated and editable
   - Permission mode dropdown works
   - Tooltips show on hover
5. Step 4 (Confirm): Verify summary shows all selections
   - Working directory with edit button
   - Model badge with edit button
   - Session name with edit button
   - CLI parameters listed
   - Git info (if applicable)
6. Click edit buttons to jump back to specific steps
7. Click Create (will fail without backend, but should attempt API call)
</verification>

<success_criteria>
1. CLI parameters form displays all controls with tooltips
2. Session name auto-generates from folder name or timestamp
3. Confirm step shows complete configuration summary
4. Edit buttons navigate back to specific steps
5. Create button sends full configuration to API
6. All validation prevents proceeding with invalid data
7. Ready for plan 03-05 (session deletion)
</success_criteria>

<output>
After completion, create `.planning/phases/03-session-management/03-04-SUMMARY.md`
</output>
