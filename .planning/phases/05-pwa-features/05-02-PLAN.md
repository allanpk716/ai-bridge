---
phase: 05-pwa-features
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - web/src/hooks/useOnlineStatus.ts
  - web/src/components/pwa/OfflineBanner.tsx
  - web/src/components/pwa/index.ts
  - web/src/App.tsx
autonomous: true

must_haves:
  truths:
    - "Application detects network status changes (online/offline)"
    - "Red banner appears at top of screen when offline"
    - "Offline banner contains warning icon and descriptive text"
    - "Offline banner disappears when network is restored"
    - "Interactive features are visually disabled when offline (read-only UI)"
  artifacts:
    - path: "web/src/hooks/useOnlineStatus.ts"
      provides: "Online/offline status detection"
      exports: ["useOnlineStatus"]
    - path: "web/src/components/pwa/OfflineBanner.tsx"
      provides: "Offline status banner component"
      exports: ["OfflineBanner"]
    - path: "web/src/components/pwa/index.ts"
      provides: "PWA component barrel export"
      exports: ["OfflineBanner"]
    - path: "web/src/App.tsx"
      provides: "App integration of offline banner"
      imports: ["OfflineBanner"]
  key_links:
    - from: "web/src/hooks/useOnlineStatus.ts"
      to: "navigator.onLine"
      via: "Browser API"
      pattern: "navigator\\.onLine"
    - from: "web/src/components/pwa/OfflineBanner.tsx"
      to: "web/src/hooks/useOnlineStatus.ts"
      via: "Hook import"
      pattern: "useOnlineStatus"
    - from: "web/src/App.tsx"
      to: "web/src/components/pwa/OfflineBanner.tsx"
      via: "Component import"
      pattern: "OfflineBanner"
---

<objective>
Create offline detection hook and offline banner component. When network becomes unavailable, display a red warning banner at the top of the screen with clear messaging. Enable read-only access to historical sessions and messages when offline.

Purpose: Provide clear visual feedback when network is unavailable and prevent confusion about why features don't work.

Output: useOnlineStatus hook, OfflineBanner component, and App integration.
</objective>

<execution_context>
@C:\Users\allan716\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\allan716\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-pwa-features/05-CONTEXT.md
@.planning/phases/05-pwa-features/05-RESEARCH.md

@web/src/App.tsx
@web/src/hooks/useConnectionStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create useOnlineStatus hook</name>
  <files>web/src/hooks/useOnlineStatus.ts</files>
  <action>
    Create web/src/hooks/useOnlineStatus.ts with browser-native online/offline detection:

    1. Import useState, useEffect from 'react'
    2. Initialize state with navigator.onLine (or true if navigator undefined)
    3. Add event listeners for 'online' and 'offline' events
    4. Clean up event listeners on unmount
    5. Return boolean isOnline state

    Pattern from RESEARCH.md:
    - Use navigator.onLine API (no external dependencies)
    - Handle SSR edge case (check typeof navigator !== 'undefined')
  </action>
  <verify>grep -q "navigator.onLine" web/src/hooks/useOnlineStatus.ts && grep -q "addEventListener" web/src/hooks/useOnlineStatus.ts</verify>
  <done>useOnlineStatus hook exists with event listeners for online/offline</done>
</task>

<task type="auto">
  <name>Task 2: Create OfflineBanner component</name>
  <files>web/src/components/pwa/OfflineBanner.tsx</files>
  <action>
    Create web/src/components/pwa/OfflineBanner.tsx with:

    1. Import useOnlineStatus from '@/hooks/useOnlineStatus'
    2. Import WifiOff icon from lucide-react
    3. Return null when isOnline is true
    4. When offline, render fixed banner:
       - position: fixed, top: 0, left: 0, right: 0
       - z-index: 50 (above other content)
       - red background (bg-red-500 for light, bg-red-900 for dark)
       - white text
       - flex layout with WifiOff icon, main text, and subtext
       - Main text: "您当前处于离线状态"
       - Subtext: "部分功能不可用"

    Styling per CONTEXT.md decision:
    - Prominent banner (red background, white text)
    - Fixed at top of viewport
    - Clear icon and messaging

    Do NOT add close/dismiss button - banner should persist while offline.
  </action>
  <verify>grep -q "WifiOff" web/src/components/pwa/OfflineBanner.tsx && grep -q "bg-red" web/src/components/pwa/OfflineBanner.tsx</verify>
  <done>OfflineBanner component renders red banner with WifiOff icon when offline</done>
</task>

<task type="auto">
  <name>Task 3: Create PWA component barrel export</name>
  <files>web/src/components/pwa/index.ts</files>
  <action>
    Create web/src/components/pwa/index.ts barrel export:

    Export OfflineBanner component for clean imports.

    Pattern will be used for additional PWA components in plan 05-03.
  </action>
  <verify>grep -q "OfflineBanner" web/src/components/pwa/index.ts</verify>
  <done>index.ts exports OfflineBanner from pwa components directory</done>
</task>

<task type="auto">
  <name>Task 4: Integrate OfflineBanner in App.tsx</name>
  <files>web/src/App.tsx</files>
  <action>
    Add OfflineBanner to App.tsx:

    1. Import OfflineBanner from '@/components/pwa'
    2. Render OfflineBanner inside ThemeProvider but before RouterProvider
    3. Ensure banner appears above all other content (z-index in component)

    The banner should be visible on all routes when offline.
  </action>
  <verify>grep -q "OfflineBanner" web/src/App.tsx</verify>
  <done>OfflineBanner is rendered in App.tsx above RouterProvider</done>
</task>

</tasks>

<verification>
After completing all tasks:

1. Run `cd web && npm run dev` - dev server starts without errors
2. Open browser DevTools Network tab
3. Select "Offline" throttling option
4. Verify red banner appears at top of screen with WifiOff icon
5. Select "Online" throttling option
6. Verify banner disappears
7. Test on mobile viewport - banner should be full width and readable

</verification>

<success_criteria>
1. useOnlineStatus hook correctly detects navigator.onLine state
2. OfflineBanner appears with red background when offline
3. OfflineBanner disappears when network is restored
4. Banner is visible on all routes
5. No console errors related to offline detection
</success_criteria>

<output>
After completion, create `.planning/phases/05-pwa-features/05-02-SUMMARY.md` with:
- Files created
- useOnlineStatus implementation details
- OfflineBanner styling choices
- Verification results (offline/online toggle testing)
</output>
