---
phase: 05-pwa-features
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - web/src/components/pwa/UpdatePrompt.tsx
  - web/src/components/pwa/index.ts
  - web/src/App.tsx
autonomous: true

must_haves:
  truths:
    - "Application prompts user to update when new Service Worker is available"
    - "Update prompt is a modal dialog (not inline notification)"
    - "Dialog has single '立即更新' button (no 'later' or 'dismiss' options)"
    - "Clicking update button reloads the application with new version"
    - "Dialog content explains why update is needed"
  artifacts:
    - path: "web/src/components/pwa/UpdatePrompt.tsx"
      provides: "Service Worker update prompt dialog"
      exports: ["UpdatePrompt"]
    - path: "web/src/components/pwa/index.ts"
      provides: "PWA component barrel export"
      exports: ["UpdatePrompt", "OfflineBanner"]
    - path: "web/src/App.tsx"
      provides: "App integration of update prompt"
      imports: ["UpdatePrompt"]
  key_links:
    - from: "web/src/components/pwa/UpdatePrompt.tsx"
      to: "virtual:pwa-register/react"
      via: "useRegisterSW hook"
      pattern: "useRegisterSW"
    - from: "web/src/components/pwa/UpdatePrompt.tsx"
      to: "web/src/components/ui/dialog"
      via: "shadcn/ui Dialog import"
      pattern: "Dialog"
    - from: "web/src/App.tsx"
      to: "web/src/components/pwa/UpdatePrompt.tsx"
      via: "Component import"
      pattern: "UpdatePrompt"
---

<objective>
Create Service Worker update prompt component using useRegisterSW hook from vite-plugin-pwa. Display a modal dialog when a new version is available, requiring user to click "立即更新" to reload and apply the update.

Purpose: Ensure users are aware of updates and can apply them at appropriate time, while preventing automatic updates that could interrupt work.

Output: UpdatePrompt component integrated into App.tsx.
</object>

<execution_context>
@C:\Users\allan716\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\allan716\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-pwa-features/05-CONTEXT.md
@.planning/phases/05-pwa-features/05-RESEARCH.md

@web/src/App.tsx
@web/src/components/ui/dialog.tsx
@web/vite.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create UpdatePrompt component</name>
  <files>web/src/components/pwa/UpdatePrompt.tsx</files>
  <action>
    Create web/src/components/pwa/UpdatePrompt.tsx with:

    1. Import useRegisterSW from 'virtual:pwa-register/react'
    2. Import Button, Dialog, DialogContent, DialogDescription, DialogFooter, DialogHeader, DialogTitle from '@/components/ui/dialog'
    3. Use useRegisterSW hook to get:
       - offlineReady state (not used in this component, for future use)
       - needRefresh state array [needRefresh, setNeedRefresh]
       - updateServiceWorker function
    4. Configure onRegisteredSW callback to log SW registration
    5. Configure onRegisterError callback to log errors
    6. Return null if needRefresh is false
    7. Render Dialog with:
       - open={needRefresh} onOpenChange={() => setNeedRefresh(false)}
       - DialogTitle: "新版本可用"
       - DialogDescription: "应用已更新,请刷新以获取最新版本。"
       - DialogFooter with single Button: "立即更新"
       - Button onClick calls updateServiceWorker(true)

    Per CONTEXT.md decision:
    - Single "立即更新" button (no dismiss/later)
    - Modal dialog (not inline notification)
    - Simple, clear messaging

    Pattern from RESEARCH.md with useRegisterSW hook.
  </action>
  <verify>grep -q "useRegisterSW" web/src/components/pwa/UpdatePrompt.tsx && grep -q "新版本可用" web/src/components/pwa/UpdatePrompt.tsx</verify>
  <done>UpdatePrompt component exists with useRegisterSW hook and Dialog UI</done>
</task>

<task type="auto">
  <name>Task 2: Export UpdatePrompt from barrel</name>
  <files>web/src/components/pwa/index.ts</files>
  <action>
    Add UpdatePrompt to web/src/components/pwa/index.ts barrel export.

    Export both OfflineBanner and UpdatePrompt for clean imports.
  </action>
  <verify>grep -q "UpdatePrompt" web/src/components/pwa/index.ts</verify>
  <done>index.ts exports both UpdatePrompt and OfflineBanner</done>
</task>

<task type="auto">
  <name>Task 3: Integrate UpdatePrompt in App.tsx</name>
  <files>web/src/App.tsx</files>
  <action>
    Add UpdatePrompt to App.tsx:

    1. Import UpdatePrompt from '@/components/pwa'
    2. Render UpdatePrompt inside ThemeProvider (same level as ConnectionDialog)
    3. Place before RouterProvider to ensure modal overlay works correctly

    The update prompt should be visible on all routes when update is available.
  </action>
  <verify>grep -q "UpdatePrompt" web/src/App.tsx</verify>
  <done>UpdatePrompt is rendered in App.tsx with ConnectionDialog and RouterProvider</done>
</task>

</tasks>

<verification>
After completing all tasks:

1. Build the application: `cd web && npm run build`
2. Preview build: `cd web && npm run preview`
3. Open browser DevTools Application tab
4. Verify Service Worker is registered
5. To test update flow (requires actual deployment or complex local setup):
   - In production build, update prompt appears when new SW is detected
   - For local testing, note that full update testing requires production environment

Note: Update flow testing is complex in development. The key verification is that:
- Component renders without errors
- TypeScript types are correct
- Dialog UI follows shadcn/ui patterns
</verification>

<success_criteria>
1. UpdatePrompt component uses useRegisterSW hook correctly
2. Dialog displays "新版本可用" title and description
3. Single "立即更新" button calls updateServiceWorker(true)
4. UpdatePrompt is integrated into App.tsx
5. No TypeScript or console errors
6. Dialog follows shadcn/ui styling patterns
</success_criteria>

<output>
After completion, create `.planning/phases/05-pwa-features/05-03-SUMMARY.md` with:
- Files created/modified
- UpdatePrompt dialog configuration
- Integration approach in App.tsx
- Notes on update testing limitations in dev environment
</output>
